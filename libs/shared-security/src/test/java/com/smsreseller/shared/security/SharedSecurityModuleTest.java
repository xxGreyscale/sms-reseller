package com.smsreseller.shared.security;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * Wave 0 placeholder test for the shared-security module.
 *
 * <p>This test exists to verify that the module compiles and that the test
 * source set is non-empty, giving {@code ./gradlew test} a green baseline
 * before any real security logic is added in Phase 2+.
 */
class SharedSecurityModuleTest {

    @Test
    void moduleCompiles() {
        // Wave 0 placeholder — proves the module compiles and JUnit 5 resolves
        assertTrue(true);
    }
}
