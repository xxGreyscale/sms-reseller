// Default widget test placeholder — replaced by feature-specific tests in later waves.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
