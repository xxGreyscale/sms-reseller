package com.opendesk.identity;

import org.junit.jupiter.api.Assumptions;
import org.junit.jupiter.api.Test;

/**
 * Covers: IDEN-07 — Reset password via email link; revokes all sessions on success (D-09, D-11, D-13).
 *
 * <p>Placeholder stub. Rewritten in plan 02-05 with full container-backed assertions.
 */
class PasswordResetIT {

    @Test
    void resetsPasswordAndRevokesAllSessions() {
        Assumptions.abort("pending impl: IDEN-07 password reset (D-09, D-11, D-13)");
    }
}
