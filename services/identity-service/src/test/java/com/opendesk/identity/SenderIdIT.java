package com.opendesk.identity;

import org.junit.jupiter.api.Assumptions;
import org.junit.jupiter.api.Test;

/**
 * Covers: SNDR-01 — Default numeric sender ID assigned at NIDA verification (D-03).
 *
 * <p>Placeholder stub. Rewritten in plan 02-03 verifying sender_ids row created in same TX as VERIFIED.
 */
class SenderIdIT {

    @Test
    void assignsDefaultNumericSenderIdOnVerification() {
        Assumptions.abort("pending impl: SNDR-01 default sender ID (D-03)");
    }
}
