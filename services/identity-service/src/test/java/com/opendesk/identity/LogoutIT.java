package com.opendesk.identity;

import org.junit.jupiter.api.Assumptions;
import org.junit.jupiter.api.Test;

/**
 * Covers: IDEN-06 — Log out and revoke current session (D-07).
 *
 * <p>Placeholder stub. Rewritten in plan 02-04 with full container-backed assertions.
 */
class LogoutIT {

    @Test
    void revokesCurrentDeviceSessionOnly() {
        Assumptions.abort("pending impl: IDEN-06 logout/revoke-current (D-07)");
    }
}
