---
phase: 02-identity-auth
plan: 06
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - services/identity-service/src/main/java/com/opendesk/identity/verification/VerificationFinalizerImpl.java
  - services/identity-service/src/main/java/com/opendesk/identity/sender/SenderId.java
  - services/identity-service/src/main/java/com/opendesk/identity/sender/SenderIdRepository.java
  - services/identity-service/src/main/java/com/opendesk/identity/sender/SenderIdService.java
  - services/identity-service/src/main/java/com/opendesk/identity/outbox/OutboxEntry.java
  - services/identity-service/src/main/java/com/opendesk/identity/outbox/OutboxRepository.java
  - services/identity-service/src/main/java/com/opendesk/identity/outbox/OutboxRelay.java
  - services/identity-service/src/main/java/com/opendesk/identity/outbox/UserVerifiedEvent.java
  - services/identity-service/src/main/resources/db/migration/V2__create_sender_ids.sql
  - services/identity-service/src/main/resources/db/migration/V3__create_outbox.sql
autonomous: true
requirements: [IDEN-03, SNDR-01]
must_haves:
  truths:
    - "finalizeVerification flips status to VERIFIED, assigns a numeric sender ID, and writes a UserVerified outbox row in ONE transaction"
    - "Re-running finalize on an already-VERIFIED user is idempotent (no second sender ID, no second outbox row)"
    - "The default sender ID is a platform-internal 6-digit zero-padded numeric shortcode, unique per user"
    - "A scheduled relay publishes unsent UserVerified rows to RabbitMQ and marks them sent (at-least-once)"
  artifacts:
    - path: "services/identity-service/src/main/java/com/opendesk/identity/verification/VerificationFinalizerImpl.java"
      provides: "@Transactional component implementing VerificationFinalizer: status flip + sender ID + outbox insert atomically"
      contains: "@Transactional"
    - path: "services/identity-service/src/main/java/com/opendesk/identity/outbox/OutboxRelay.java"
      provides: "@Scheduled relay publishing UserVerified to RabbitMQ"
      contains: "@Scheduled"
    - path: "services/identity-service/src/main/resources/db/migration/V3__create_outbox.sql"
      provides: "outbox table with event_id, payload, sent flag"
      contains: "create table outbox"
  key_links:
    - from: "services/identity-service/.../verification/VerificationFinalizerImpl.java"
      to: "outbox + sender_ids + users (single TX)"
      via: "@Transactional status flip + INSERT sender_id + INSERT outbox"
      pattern: "@Transactional"
    - from: "services/identity-service/.../outbox/OutboxRelay.java"
      to: "RabbitMQ topic exchange"
      via: "@Scheduled RabbitTemplate publish + mark sent"
      pattern: "@Scheduled"
---

<objective>
Implement the verification finalize transaction (IDEN-03, SNDR-01): the @Transactional `VerificationFinalizerImpl` that flips status→VERIFIED, assigns a default numeric sender ID, and writes a UserVerified(50 credits) transactional-outbox row atomically; plus the @Scheduled RabbitMQ relay that publishes unsent rows.

Purpose: This is the credit-grant boundary to Phase 3. It implements the `VerificationFinalizer` seam defined in 02-03, so the orchestrator (Wave 2) calls into this transactional unit (Wave 3) with no synchronous cross-module call. The outbox keeps the 50-credit grant a clean async boundary.
Output: VerificationFinalizerImpl (the named TX home class — no "inline" optionality), SenderId entity/repo/service (numeric shortcode per resolved Open-Q1), OutboxEntry/repo/relay + UserVerifiedEvent, V2/V3 migrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-auth/02-RESEARCH.md
@CLAUDE.md

<interfaces>
From 02-02 (transitive via 02-03): User entity (UUID id, VerificationStatus status); UserRepository.
From 02-03 (depends_on): VerificationFinalizer interface — void finalizeVerification(UUID userId). THIS plan provides the @Service @Transactional implementation `VerificationFinalizerImpl` (the single committed home class). The VerificationOrchestrator already injects the interface and calls finalizeVerification on NIDA success.

From 02-RESEARCH.md:
- Pattern 4 outbox (lines 259-261), Pitfall 5 dedupe (lines 309-312), System diagram (lines 159-169)
- Open Questions (RESOLVED) Q1: default sender ID = platform-internal 6-digit zero-padded numeric shortcode, uniqueness-checked retry; placeholder until TCRA provisioning.
- RabbitMQ topic exchange pattern (CLAUDE.md). eventId UUID on outbox row for consumer dedupe (Pitfall 5). RabbitTemplate via spring-boot-starter-amqp (added 02-01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SenderId + Outbox entities, repositories, V2/V3 migrations, UserVerifiedEvent</name>
  <files>services/identity-service/src/main/java/com/opendesk/identity/sender/SenderId.java, services/identity-service/src/main/java/com/opendesk/identity/sender/SenderIdRepository.java, services/identity-service/src/main/java/com/opendesk/identity/sender/SenderIdService.java, services/identity-service/src/main/java/com/opendesk/identity/outbox/OutboxEntry.java, services/identity-service/src/main/java/com/opendesk/identity/outbox/OutboxRepository.java, services/identity-service/src/main/java/com/opendesk/identity/outbox/UserVerifiedEvent.java, services/identity-service/src/main/resources/db/migration/V2__create_sender_ids.sql, services/identity-service/src/main/resources/db/migration/V3__create_outbox.sql</files>
  <read_first>
    - .planning/phases/02-identity-auth/02-RESEARCH.md (Pattern 4 outbox lines 259-261; Pitfall 5 lines 309-312; Open Questions (RESOLVED) Q1 sender-ID format lines 396-409)
    - services/identity-service/src/test/java/com/opendesk/identity/SenderIdIT.java (plain placeholder stub from 02-01)
    - CLAUDE.md (jakarta.* not javax.*; per-service Flyway; @EnableJpaAuditing)
  </read_first>
  <behavior>
    - SenderIdService.assign(userId) generates a unique 6-digit zero-padded numeric shortcode; idempotent (returns the existing one if already assigned) — SenderIdIT
    - On collision against the sender_ids unique constraint, regenerate (retry loop)
    - OutboxEntry persists with a fresh event_id (unique) and sent=false default
  </behavior>
  <action>
    SenderId @Entity (jakarta.persistence.*) + V2__create_sender_ids.sql (id uuid pk, user_id uuid unique, sender_id varchar unique not null, created_at timestamptz). Lombok builders.
    SenderIdRepository extends JpaRepository<SenderId,UUID> with findByUserId, existsBySenderId.
    SenderIdService.assign(userId): if a SenderId exists for userId → return it (idempotent); else generate a platform-internal 6-digit, zero-padded numeric shortcode (resolved Open-Q1), uniqueness-checked via existsBySenderId with a bounded regenerate-on-collision loop; persist and return. Javadoc: placeholder until TCRA provisioning.
    OutboxEntry @Entity + V3__create_outbox.sql (id uuid pk, event_id uuid unique, aggregate_type, aggregate_id, event_type, payload text/jsonb, sent boolean default false, created_at, sent_at). OutboxRepository extends JpaRepository with findBySentFalse (bounded).
    UserVerifiedEvent record {eventId, userId, freeCredits=50}.
  </action>
  <verify>
    <automated>./gradlew :services:identity-service:test --tests "*SenderIdIT*" --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - V2 contains "create table sender_ids" with a unique constraint on sender_id; V3 contains "create table outbox" with unique event_id
    - SenderIdIT confirms exactly one unique 6-digit numeric sender ID assigned; re-run produces no duplicate (idempotent)
    - Flyway V1-V3 apply cleanly against the Testcontainers Postgres (ddl-auto=validate passes)
    - No "javax." imports
  </acceptance_criteria>
  <done>sender_ids + outbox schema and aggregates exist; numeric shortcode assignment is unique and idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VerificationFinalizerImpl (single TX) + OutboxRelay (RabbitMQ)</name>
  <files>services/identity-service/src/main/java/com/opendesk/identity/verification/VerificationFinalizerImpl.java, services/identity-service/src/main/java/com/opendesk/identity/outbox/OutboxRelay.java</files>
  <read_first>
    - .planning/phases/02-identity-auth/02-RESEARCH.md (Pattern 4 outbox lines 259-261; Pitfall 5 lines 309-312; System diagram lines 159-169)
    - services/identity-service/src/test/java/com/opendesk/identity/VerificationOutboxIT.java (plain placeholder stub from 02-01)
    - services/identity-service/src/main/java/com/opendesk/identity/verification/VerificationFinalizer.java (the seam interface from 02-03)
    - services/identity-service/src/main/java/com/opendesk/identity/sender/SenderIdService.java (Task 1)
  </read_first>
  <behavior>
    - VerificationFinalizerImpl.finalizeVerification(userId): user → VERIFIED, exactly one SenderId row, exactly one outbox row UserVerified{userId, credits:50, eventId} — all in one TX (VerificationOutboxIT)
    - Re-running finalize on an already-VERIFIED user is idempotent (guard returns early; no second sender ID, no second outbox row)
    - OutboxRelay publishes unsent rows to RabbitMQ topic exchange and marks them sent
  </behavior>
  <action>
    VerificationFinalizerImpl @Service implements VerificationFinalizer (the 02-03 seam). finalizeVerification(UUID userId) annotated @Transactional: load user; guard if already VERIFIED → return (idempotent); set status VERIFIED; call SenderIdService.assign(userId); INSERT an OutboxEntry with a fresh eventId carrying UserVerifiedEvent{userId, freeCredits=50} (event_type "UserVerified", IDEN-03). Single TX (Pattern 4) — no synchronous wallet/credit call. This is the committed TX home class; NO inline alternative.
    OutboxRelay @Scheduled(fixedDelay): fetch sent=false rows (bounded), publish each to the RabbitMQ topic exchange via RabbitTemplate, mark sent=true + sent_at. At-least-once; consumer (Phase 3) dedupes by event_id (Pitfall 5).
  </action>
  <verify>
    <automated>./gradlew :services:identity-service:test --tests "*VerificationOutboxIT*" --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - VerificationFinalizerImpl implements VerificationFinalizer and its finalizeVerification is annotated @Transactional
    - VerificationOutboxIT confirms exactly one outbox row (event_type UserVerified, credits 50) per verification; re-run finalize produces no second row
    - OutboxRelay annotated @Scheduled and publishes via RabbitTemplate, marking rows sent
    - No synchronous call into a wallet/credit module (grep finds no wallet client import)
    - No "javax." imports
  </acceptance_criteria>
  <done>Single-TX VERIFIED flip grants sender ID + writes UserVerified(50) outbox row; scheduled relay publishes to RabbitMQ; idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| identity outbox → RabbitMQ → Phase 3 wallet | async credit-grant boundary; at-least-once + eventId dedupe |
| verification finalize → identity DB | atomic state transition (status + sender ID + outbox) in one TX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | double credit grant | mitigate | idempotent finalize (already-VERIFIED guard) + outbox eventId; Phase 3 consumer dedupes (Pitfall 5) |
| T-02-OB | Tampering | lost/phantom credit event | mitigate | transactional outbox — event reflects exactly committed DB state (Pattern 4) |
| T-02-SNDR | Tampering | duplicate sender ID | mitigate | unique constraint on sender_id + regenerate-on-collision; assign idempotent per user |
| T-02-SC | Tampering | dependencies | accept | BOM-managed per RESEARCH audit |
</threat_model>

<verification>
- `./gradlew :services:identity-service:test --tests "*VerificationOutboxIT*" --tests "*SenderIdIT*" --no-daemon` — green.
- Flyway V1-V3 apply cleanly against Postgres 16 container.
</verification>

<success_criteria>
- VERIFIED flip grants numeric sender ID (SNDR-01) + UserVerified(50) outbox row (IDEN-03), atomic + idempotent.
- @Scheduled relay publishes to RabbitMQ; consumer dedupes by eventId.
</success_criteria>

<output>
Create `.planning/phases/02-identity-auth/02-06-SUMMARY.md` when done.
</output>
