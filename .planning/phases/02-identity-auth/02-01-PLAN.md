---
phase: 02-identity-auth
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - services/identity-service/build.gradle.kts
  - libs/shared-security/build.gradle.kts
  - services/identity-service/src/main/java/com/opendesk/identity/IdentityServiceApplication.java
  - services/identity-service/src/main/resources/application.yml
  - services/identity-service/src/test/resources/application-test.yml
  - services/identity-service/src/test/java/com/opendesk/identity/AbstractIntegrationTest.java
  - services/identity-service/src/test/java/com/opendesk/identity/TestKeys.java
  - services/identity-service/src/test/resources/test-keys/jwt-private.pem
  - services/identity-service/src/test/resources/test-keys/jwt-public.pem
  - services/identity-service/src/test/java/com/opendesk/identity/RegistrationIT.java
  - services/identity-service/src/test/java/com/opendesk/identity/LoginIT.java
  - services/identity-service/src/test/java/com/opendesk/identity/LockoutIT.java
  - services/identity-service/src/test/java/com/opendesk/identity/RefreshRotationIT.java
  - services/identity-service/src/test/java/com/opendesk/identity/LogoutIT.java
  - services/identity-service/src/test/java/com/opendesk/identity/PasswordResetIT.java
  - services/identity-service/src/test/java/com/opendesk/identity/VerificationOutboxIT.java
  - services/identity-service/src/test/java/com/opendesk/identity/NidaDegradedIT.java
  - services/identity-service/src/test/java/com/opendesk/identity/SenderIdIT.java
  - services/identity-service/src/test/java/com/opendesk/identity/JwtIssuerUnitTest.java
  - libs/shared-security/src/test/java/com/opendesk/shared/security/JwtValidationUnitTest.java
autonomous: true
requirements: [IDEN-01, IDEN-02, IDEN-03, IDEN-04, IDEN-05, IDEN-06, IDEN-07, IDEN-08, SNDR-01]
must_haves:
  truths:
    - "./gradlew :services:identity-service:test compiles and runs with a non-empty test set"
    - "One placeholder/failing test exists per phase requirement ID and per the cross-module JWT contract"
    - "Testcontainers Postgres 16 + Redis 7 wire via @ServiceConnection without manual JDBC/Redis config"
    - "A fixed RSA test keypair is shared between identity (sign) and shared-security (verify) tests"
  artifacts:
    - path: "services/identity-service/src/test/java/com/opendesk/identity/AbstractIntegrationTest.java"
      provides: "Testcontainers base (Postgres 16 + Redis 7 @ServiceConnection)"
      min_lines: 20
    - path: "services/identity-service/src/test/java/com/opendesk/identity/TestKeys.java"
      provides: "Loads shared RSA test keypair for sign/verify"
      min_lines: 15
    - path: "gradle/libs.versions.toml"
      provides: "spring-boot-starter-mail + Redis Testcontainers catalog entries"
      contains: "spring-boot-starter-mail"
  key_links:
    - from: "services/identity-service/build.gradle.kts"
      to: "gradle/libs.versions.toml"
      via: "libs.* accessors for new deps"
      pattern: "libs\\.spring\\.boot\\.starter\\.(amqp|data\\.redis|validation)"
---

<objective>
Establish Phase 2 test infrastructure and build dependencies BEFORE any implementation, so the validation map is non-empty and every later task has an automated verify target.

Purpose: Wave 0 makes the Nyquist sampling loop real — Testcontainers (Postgres 16 + Redis 7) wiring, a shared RSA keypair fixture for the cross-module JWT contract, and a failing/placeholder test per requirement ID.
Output: Build deps added, IdentityServiceApplication entrypoint, Testcontainers base + RSA fixtures, one (currently failing/skeleton) test per requirement plus the cross-module JwtValidation test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-auth/02-RESEARCH.md
@.planning/phases/02-identity-auth/02-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From gradle/libs.versions.toml (already present — use these accessors):
- libs.spring.boot.starter.amqp, libs.spring.boot.starter.data.redis, libs.spring.boot.starter.validation
- libs.spring.boot.starter.oauth2.resource.server, libs.spring.retry, libs.resilience4j.spring.boot3
- libs.spring.boot.testcontainers, libs.testcontainers.postgresql, libs.testcontainers.rabbitmq
- libs.mapstruct, libs.mapstruct.processor

MISSING — must be ADDED to the catalog in this plan:
- spring-boot-starter-mail (BOM-managed module: org.springframework.boot:spring-boot-starter-mail)
- a Redis Testcontainers capability — prefer org.testcontainers GenericContainer (already on classpath via testcontainers-postgresql transitive) OR add com.redis:testcontainers-redis with an explicit version. Researcher A2 confirms Redis 7.

From buildSrc convention (01-01-SUMMARY): spring-boot-service plugin sets mainClass.convention("placeholder.MainClass"); adding a real @SpringBootApplication auto-overrides it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 2 build dependencies + catalog entries</name>
  <files>gradle/libs.versions.toml, services/identity-service/build.gradle.kts, libs/shared-security/build.gradle.kts</files>
  <read_first>
    - gradle/libs.versions.toml (current catalog)
    - services/identity-service/build.gradle.kts (current skeleton deps)
    - libs/shared-security/build.gradle.kts (validator side)
    - .planning/phases/02-identity-auth/02-RESEARCH.md (Installation block, lines 112-129)
  </read_first>
  <action>
    Add catalog entries to gradle/libs.versions.toml [libraries]: `spring-boot-starter-mail = { module = "org.springframework.boot:spring-boot-starter-mail" }`. For Redis test containers, add `testcontainers-redis = { module = "com.redis:testcontainers-redis", version = "<pin>" }` ONLY if you confirm the artifact resolves; otherwise document in the plan SUMMARY that the Redis container uses org.testcontainers GenericContainer("redis:7") and add no catalog entry.
    In services/identity-service/build.gradle.kts add implementation deps: libs.spring.boot.starter.security, libs.spring.boot.starter.oauth2.resource.server (needed for NimbusJwtEncoder — Pitfall 1), libs.spring.boot.starter.validation, libs.spring.boot.starter.data.redis, libs.spring.boot.starter.amqp, libs.spring.retry, libs.resilience4j.spring.boot3, libs.mapstruct; annotationProcessor(libs.mapstruct.processor) AFTER lombok ordering rule; and testImplementation: libs.spring.boot.starter.test, libs.spring.boot.testcontainers, libs.testcontainers.postgresql, libs.testcontainers.rabbitmq (+ testcontainers-redis if added). Keep existing flyway + jpa + web + shared lib deps.
    Do NOT add nimbus-jose-jwt directly (transitive via oauth2). Do NOT change shared-security runtime deps unless its test needs the test starter (it already has libs.spring.boot.starter.test).
  </action>
  <verify>
    <automated>./gradlew :services:identity-service:dependencies --configuration testRuntimeClasspath --no-daemon | grep -E "spring-boot-starter-(amqp|data-redis|validation|oauth2)" </automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :services:identity-service:compileTestJava --no-daemon` exits 0
    - dependencies report shows spring-boot-starter-amqp, spring-boot-starter-data-redis, spring-boot-starter-validation, spring-boot-starter-oauth2-resource-server on the identity-service classpath
    - grep -v '^#' gradle/libs.versions.toml | grep -c 'spring-boot-starter-mail' returns 1
    - mapstruct annotationProcessor declared AFTER any lombok annotationProcessor (CLAUDE.md order)
  </acceptance_criteria>
  <done>Identity-service and catalog carry all Phase 2 deps; project compiles test sources.</done>
</task>

<task type="auto">
  <name>Task 2: Application entrypoint, config files, Testcontainers base, RSA test fixture</name>
  <files>services/identity-service/src/main/java/com/opendesk/identity/IdentityServiceApplication.java, services/identity-service/src/main/resources/application.yml, services/identity-service/src/test/resources/application-test.yml, services/identity-service/src/test/java/com/opendesk/identity/AbstractIntegrationTest.java, services/identity-service/src/test/java/com/opendesk/identity/TestKeys.java, services/identity-service/src/test/resources/test-keys/jwt-private.pem, services/identity-service/src/test/resources/test-keys/jwt-public.pem</files>
  <read_first>
    - .planning/phases/02-identity-auth/02-RESEARCH.md (Recommended Project Structure lines 191-218; Wave 0 Gaps lines 456-462)
    - CLAUDE.md (Testcontainers @ServiceConnection pattern; virtual threads; jakarta.* rule)
    - .planning/phases/01-foundation/01-01-SUMMARY.md (placeholder.MainClass note)
  </read_first>
  <action>
    Create IdentityServiceApplication with @SpringBootApplication, @EnableScheduling (needed by later outbox/retry jobs), @EnableJpaAuditing; class in package com.opendesk.identity. This replaces placeholder.MainClass automatically.
    application.yml: set spring.application.name=identity-service, spring.threads.virtual.enabled=true, spring.jpa.hibernate.ddl-auto=validate, flyway enabled, management.endpoint.health.probes.enabled=true, and a `spring.profiles.group` mapping dev/staging to include `stub`. Add placeholder config keys (no real secrets): app.jwt.private-key-location, app.jwt.public-key-location, app.nida.stub.default-outcome=SUCCESS.
    application-test.yml (test resources): activate profiles `stub`, point app.jwt.*-key-location at classpath:test-keys/*.pem, set short TTLs.
    Generate a FIXED RSA-2048 keypair into src/test/resources/test-keys/jwt-private.pem (PKCS#8) and jwt-public.pem (X.509/SPKI) via `openssl`. These are TEST-ONLY keys — never reused in prod.
    TestKeys.java: utility loading the PEM files into RSAPrivateKey / RSAPublicKey (java.security) for both sign and verify tests; place in src/test so identity tests can import it.
    AbstractIntegrationTest.java: @SpringBootTest + @Testcontainers base; a @ServiceConnection PostgreSQLContainer("postgres:16") and a Redis container (GenericContainer("redis:7").withExposedPorts(6379) wired via @DynamicPropertySource for spring.data.redis.host/port, OR @ServiceConnection if testcontainers-redis added). @ActiveProfiles("stub","test").
  </action>
  <verify>
    <automated>./gradlew :services:identity-service:test --tests "*AbstractIntegrationTest*" --no-daemon || ./gradlew :services:identity-service:compileTestJava --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - IdentityServiceApplication.java contains "@SpringBootApplication" and "@EnableScheduling"
    - Both PEM files exist; `openssl rsa -in jwt-private.pem -check -noout` and `openssl rsa -pubin -in jwt-public.pem -noout` succeed
    - AbstractIntegrationTest references "postgres:16" and "redis:7"
    - No `javax.` imports anywhere in created sources (grep returns 0)
    - compileTestJava exits 0
  </acceptance_criteria>
  <done>Spring Boot app boots in tests against ephemeral Postgres 16 + Redis 7 with shared RSA test keys loaded.</done>
</task>

<task type="auto">
  <name>Task 3: One placeholder test per requirement + cross-module JWT contract test</name>
  <files>services/identity-service/src/test/java/com/opendesk/identity/RegistrationIT.java, services/identity-service/src/test/java/com/opendesk/identity/LoginIT.java, services/identity-service/src/test/java/com/opendesk/identity/LockoutIT.java, services/identity-service/src/test/java/com/opendesk/identity/RefreshRotationIT.java, services/identity-service/src/test/java/com/opendesk/identity/LogoutIT.java, services/identity-service/src/test/java/com/opendesk/identity/PasswordResetIT.java, services/identity-service/src/test/java/com/opendesk/identity/VerificationOutboxIT.java, services/identity-service/src/test/java/com/opendesk/identity/NidaDegradedIT.java, services/identity-service/src/test/java/com/opendesk/identity/SenderIdIT.java, services/identity-service/src/test/java/com/opendesk/identity/JwtIssuerUnitTest.java, libs/shared-security/src/test/java/com/opendesk/shared/security/JwtValidationUnitTest.java</files>
  <read_first>
    - .planning/phases/02-identity-auth/02-RESEARCH.md (Phase Requirements → Test Map lines 436-449)
    - .planning/phases/02-identity-auth/02-VALIDATION.md (Per-Task Verification Map)
    - libs/shared-security/src/test/java/com/opendesk/shared/security/SharedSecurityModuleTest.java (existing test style)
  </read_first>
  <action>
    Create one JUnit 5 test class per requirement, extending AbstractIntegrationTest where DB/Redis is needed. Each test must have a real @Test method named per the Test Map (e.g. RegistrationIT.returnsImmediately, LockoutIT, RefreshRotationIT, LogoutIT, PasswordResetIT, VerificationOutboxIT, NidaDegradedIT, SenderIdIT) that currently `Assumptions.abort("pending impl: <REQ-ID>")` (skipped, NOT passing-trivially) so the validation map is non-empty and honest. JwtIssuerUnitTest and shared-security JwtValidationUnitTest are pure unit (no containers).
    JwtValidationUnitTest (load-bearing, D-02): assert that a token signed with TestKeys private RSA key and a `verification_status` claim is decoded by NimbusJwtDecoder.withPublicKey(TestKeys public) and the claim is readable — this proves the cross-module contract. This test SHOULD actually be implementable now (no app logic needed, just Nimbus encode/decode with the shared fixture) — make it a real assertion, not a skip.
    Map each test to its requirement in a class-level Javadoc comment: `// Covers: IDEN-04` etc. Do NOT use bare assertTrue(true).
  </action>
  <verify>
    <automated>./gradlew :services:identity-service:test :libs:shared-security:test --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - 11 test classes exist matching the files list; each contains at least one @Test method
    - grep -rL "Covers:" on the new test files returns nothing (every file references its requirement)
    - JwtValidationUnitTest performs a real encode-with-private / decode-with-public assertion on a verification_status claim and PASSES
    - Skipped placeholder tests use Assumptions.abort (reported as skipped), never assertTrue(true)
    - `./gradlew :libs:shared-security:test --no-daemon` exits 0 with JwtValidationUnitTest green
  </acceptance_criteria>
  <done>Non-empty validation map: every requirement has a named test; cross-module JWT contract proven green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| identity-service (signer) → all modules (verifiers) | RSA public/private key split; test fixture must mirror prod key separation |
| test fixtures → repo | TEST-ONLY RSA keys committed; must never be reused as prod keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | committed test RSA keypair | accept | keys are clearly test-only (src/test/resources/test-keys); prod keys come from K8s Secret (INFR-05); document in SUMMARY |
| T-02-10 | Tampering | cross-module JWT contract | mitigate | JwtValidationUnitTest proves only the public key validates; tampered token fails decode |
| T-02-SC | Tampering | new build deps (spring-boot-starter-mail, testcontainers-redis) | mitigate | all Maven Central / BOM-managed per RESEARCH Package Legitimacy Audit; no npm/pip slop risk |
</threat_model>

<verification>
- `./gradlew :services:identity-service:test :libs:shared-security:test --no-daemon` runs; placeholder tests skipped, JwtValidationUnitTest green.
- No `javax.*` imports; no `RestTemplate`.
</verification>

<success_criteria>
- Validation map non-empty: one named test per requirement ID + cross-module contract.
- Testcontainers Postgres 16 + Redis 7 boot via the base class.
- Shared RSA fixture usable by both identity and shared-security tests.
</success_criteria>

<output>
Create `.planning/phases/02-identity-auth/02-01-SUMMARY.md` when done.
</output>
