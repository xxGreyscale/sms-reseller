---
phase: 02-identity-auth
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-04"]
files_modified:
  - services/identity-service/src/main/java/com/opendesk/identity/password/EmailSender.java
  - services/identity-service/src/main/java/com/opendesk/identity/password/StubEmailSender.java
  - services/identity-service/src/main/java/com/opendesk/identity/password/RealEmailSender.java
  - services/identity-service/src/main/java/com/opendesk/identity/password/PasswordResetService.java
  - services/identity-service/src/main/java/com/opendesk/identity/auth/PasswordResetController.java
  - services/identity-service/src/main/java/com/opendesk/identity/web/dto/ForgotPasswordRequest.java
  - services/identity-service/src/main/java/com/opendesk/identity/web/dto/ResetPasswordRequest.java
autonomous: true
requirements: [IDEN-07]
must_haves:
  truths:
    - "Forgot-password issues a single-use, time-limited reset token stored in Redis and delivers a reset link via the EmailSender (stub records it in dev)"
    - "Reset consumes the token (single-use), sets a new BCrypt password, and revokes ALL of that user's sessions (D-09)"
    - "An invalid/expired/already-used reset token is rejected; forgot-password does not reveal whether the email exists"
  artifacts:
    - path: "services/identity-service/src/main/java/com/opendesk/identity/password/PasswordResetService.java"
      provides: "single-use TTL reset token + revoke-all on success"
      min_lines: 30
    - path: "services/identity-service/src/main/java/com/opendesk/identity/password/EmailSender.java"
      provides: "mock-first email interface (D-13)"
      contains: "interface EmailSender"
    - path: "services/identity-service/src/main/java/com/opendesk/identity/password/StubEmailSender.java"
      provides: "@Profile(stub) records reset link in dev"
      contains: "@Profile(\"stub\")"
  key_links:
    - from: "services/identity-service/.../password/PasswordResetService.java"
      to: "RefreshTokenService.revokeAll"
      via: "revoke all sessions on successful reset (D-09)"
      pattern: "revokeAll"
    - from: "services/identity-service/.../password/PasswordResetService.java"
      to: "Redis reset:{token}"
      via: "SETEX single-use token"
      pattern: "reset:"
---

<objective>
Implement forgotten-password reset via email link (IDEN-07): single-use, time-limited token; mock-first email; revoke-all sessions on success (D-09).

Purpose: Completes the auth surface. Email follows the established mock-first interface+profile pattern (D-13) so the full reset flow is demoable without a real SMTP provider.
Output: EmailSender interface + stub (records link) + real (JavaMailSender) impls, PasswordResetService (Redis single-use token + revoke-all), forgot/reset endpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-auth/02-RESEARCH.md
@CLAUDE.md

<interfaces>
From 02-02: User + UserRepository.findByEmail; PasswordEncoder (BCrypt).
From 02-04: RefreshTokenService.revokeAll(UUID userId) — call on successful reset (D-09).
From 02-02 RedisConfig: StringRedisTemplate.
From 02-01: catalog entry spring-boot-starter-mail added; PasswordResetIT placeholder exists; SecurityConfig permitAll on /auth/forgot,/auth/reset.

From 02-RESEARCH.md:
- Password-reset diagram lines 186-188: forgot → SETEX reset:{token} ttl → EmailSender; reset → validate+delete token → BCrypt new pwd → DEL refresh:{userId}:*
- D-11 email link, single-use, time-limited; D-13 EmailSender interface + Stub(records link)/Real(@Profile prod JavaMailSender); D-09 revoke-all
- Open Q3 lines 408-409: real impl JavaMailSender (SMTP) @Profile("prod"); stub logs/records link in dev
- High-entropy token, short TTL, deleted on use (security domain); never log the reset link in prod (V7)
- spring-boot-starter-mail required only for the real impl; add the build dep here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EmailSender interface + stub + real (mock-first, D-13)</name>
  <files>services/identity-service/src/main/java/com/opendesk/identity/password/EmailSender.java, services/identity-service/src/main/java/com/opendesk/identity/password/StubEmailSender.java, services/identity-service/src/main/java/com/opendesk/identity/password/RealEmailSender.java, services/identity-service/build.gradle.kts</files>
  <read_first>
    - .planning/phases/02-identity-auth/02-RESEARCH.md (D-13; Open Q3 lines 408-409; mock-first pattern)
    - services/identity-service/build.gradle.kts (add spring-boot-starter-mail)
    - CLAUDE.md (mock-first interface + @Profile split; never log secrets in prod)
  </read_first>
  <action>
    Add implementation(libs.spring.boot.starter.mail) to services/identity-service/build.gradle.kts (catalog entry added in 02-01).
    EmailSender interface: sendPasswordResetLink(String toEmail, String resetUrl).
    StubEmailSender @Profile("stub") @Service: records the reset URL so tests/dev can retrieve it (e.g. store last link in an injectable in-memory holder and log it at DEBUG). This is what makes PasswordResetIT able to read the link without real email.
    RealEmailSender @Profile("prod") @Service: uses JavaMailSender (SMTP) to send the link; never log the URL in prod (V7).
  </action>
  <verify>
    <automated>./gradlew :services:identity-service:compileJava --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - EmailSender is an interface; StubEmailSender annotated @Profile("stub"); RealEmailSender annotated @Profile("prod")
    - build.gradle.kts contains libs.spring.boot.starter.mail
    - StubEmailSender exposes the last-sent reset URL for test retrieval
    - compileJava exits 0
  </acceptance_criteria>
  <done>Mock-first EmailSender with stub (records link) + real (JavaMailSender) behind profiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PasswordResetService + forgot/reset endpoints (single-use token, revoke-all)</name>
  <files>services/identity-service/src/main/java/com/opendesk/identity/password/PasswordResetService.java, services/identity-service/src/main/java/com/opendesk/identity/auth/PasswordResetController.java, services/identity-service/src/main/java/com/opendesk/identity/web/dto/ForgotPasswordRequest.java, services/identity-service/src/main/java/com/opendesk/identity/web/dto/ResetPasswordRequest.java</files>
  <read_first>
    - .planning/phases/02-identity-auth/02-RESEARCH.md (reset diagram lines 186-188; security domain reset-token row; D-09/D-11)
    - services/identity-service/src/test/java/com/opendesk/identity/PasswordResetIT.java (placeholder from 02-01)
    - services/identity-service/src/main/java/com/opendesk/identity/token/RefreshTokenService.java (revokeAll)
  </read_first>
  <behavior>
    - POST /auth/forgot {email}: if the email exists, SETEX reset:{token} = userId with short TTL and send the link via EmailSender; ALWAYS return 200/202 (no enumeration) — PasswordResetIT
    - POST /auth/reset {token, newPassword}: valid token → BCrypt-hash + persist new password, DELETE the token (single-use), revokeAll(userId); reused/expired token → 400 — PasswordResetIT
    - After reset, all prior refresh tokens for the user are invalid (D-09)
  </behavior>
  <action>
    ForgotPasswordRequest: email (@Email @NotBlank). ResetPasswordRequest: token (@NotBlank), newPassword (@NotBlank @Size(min=8), D-12).
    PasswordResetService: forgot(email) — lookup user; if present generate a high-entropy token (SecureRandom Base64URL), Redis SETEX reset:{token} = userId with short TTL (e.g. 15-30 min, D-11); call EmailSender.sendPasswordResetLink(email, resetUrl with token). Return without revealing existence (no enumeration). reset(token, newPassword) — GET reset:{token}; if absent → 400; atomically DELETE the key (single-use, before applying so a race can't reuse); BCrypt-hash newPassword via PasswordEncoder, persist on the User; call RefreshTokenService.revokeAll(userId) (D-09).
    PasswordResetController: POST /auth/forgot, POST /auth/reset (@Valid). Never log the token/URL in prod (V7).
  </action>
  <verify>
    <automated>./gradlew :services:identity-service:test --tests "*PasswordResetIT*" --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - PasswordResetIT PASSES: token single-use (second reset with same token → 400) and TTL-bounded
    - After a successful reset, RefreshTokenService.revokeAll was invoked → prior refresh tokens rejected
    - /auth/forgot returns the same response whether or not the email exists (no enumeration)
    - newPassword annotated @Size(min = 8); reset token generated via SecureRandom; not logged in prod
  </acceptance_criteria>
  <done>Single-use, TTL'd email-link password reset that re-hashes the password and revokes all sessions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /auth/forgot,/reset | untrusted email + reset token |
| identity ↔ Redis | single-use reset token store (TTL) |
| identity → email provider (mock-first) | reset link delivery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | reset-token brute force / reuse | mitigate | high-entropy SecureRandom token, short TTL, deleted on use (single-use) |
| T-02-ENUM | Info Disclosure | email enumeration on /auth/forgot | mitigate | identical response regardless of email existence |
| T-02-SESS | Spoofing | stale sessions after reset | mitigate | revokeAll on successful reset (D-09) |
| T-02-LOG | Info Disclosure | reset link in logs | mitigate | stub logs at DEBUG/dev only; real impl never logs URL (V7) |
| T-02-SC | Tampering | spring-boot-starter-mail dep | accept | BOM-managed per RESEARCH audit |
</threat_model>

<verification>
- `./gradlew :services:identity-service:test --no-daemon` — PasswordResetIT green.
- Full suite + shared-security green before /gsd:verify-work.
</verification>

<success_criteria>
- Email-link reset with single-use, time-limited token (IDEN-07).
- Successful reset revokes all sessions (D-09).
- No email enumeration; mock-first email proven via stub.
</success_criteria>

<output>
Create `.planning/phases/02-identity-auth/02-05-SUMMARY.md` when done.
</output>
