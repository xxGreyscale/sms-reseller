---
phase: 03-wallet-payments
plan: 06
type: tdd
wave: 3
depends_on: [03-02, 03-04, 03-05]
files_modified:
  - services/wallet-service/src/main/java/com/opendesk/wallet/consumer/PaymentConfirmedConsumer.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/consumer/PaymentConfirmedEvent.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/refund/RefundService.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/refund/RefundController.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/refund/RefundRequest.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/sweep/ExpirySweepJob.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/sweep/LowCreditAlertJob.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/sweep/ExpiryWarningJob.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/outbox/OutboxEntry.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/outbox/OutboxRepository.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/outbox/OutboxRelay.java
  - services/wallet-service/src/main/resources/db/migration/V4__create_outbox.sql
  - services/wallet-service/src/main/java/com/opendesk/wallet/lot/CreditLotRepository.java
  - services/payment-service/src/main/java/com/opendesk/payment/reconciliation/ReconciliationJob.java
  - services/payment-service/src/main/java/com/opendesk/payment/gateway/AzampayPaymentGateway.java
  - services/payment-service/src/main/java/com/opendesk/payment/gateway/AzampayTokenProvider.java
  - services/payment-service/src/main/java/com/opendesk/payment/config/Resilience4jConfig.java
  - services/wallet-service/src/test/java/com/opendesk/wallet/RefundIT.java
  - services/wallet-service/src/test/java/com/opendesk/wallet/LowCreditAlertIT.java
  - services/wallet-service/src/test/java/com/opendesk/wallet/ExpiryWarningIT.java
  - services/payment-service/src/test/java/com/opendesk/payment/ReconciliationIT.java
autonomous: true
requirements: [PYMT-08, WLET-04, WLET-05]

must_haves:
  truths:
    - "On PaymentConfirmed, the wallet grants a PURCHASED lot (12-month expiry) exactly once (idempotent)"
    - "The refund mechanism credits the wallet via an append-only REFUND lot, idempotently (calling twice does not double-credit)"
    - "A low-credit alert outbox event is emitted when a user's balance drops below the configured threshold"
    - "A 7-day expiry-warning outbox event is emitted for soon-expiring lots"
    - "The reconciliation job confirms stale EXPIRED/PENDING payments and flips confirmed ones to SUCCESS"
  artifacts:
    - path: "services/wallet-service/src/main/java/com/opendesk/wallet/refund/RefundService.java"
      provides: "Idempotent ledger credit-back callable by Phase 4 (PYMT-08, D-07)"
    - path: "services/payment-service/src/main/java/com/opendesk/payment/reconciliation/ReconciliationJob.java"
      provides: "@Scheduled Azampay reconciliation (late-success, idempotent)"
    - path: "services/wallet-service/src/main/java/com/opendesk/wallet/sweep/LowCreditAlertJob.java"
      provides: "Low-credit alert outbox emit (WLET-04, D-08)"
  key_links:
    - from: "services/wallet-service/src/main/java/com/opendesk/wallet/consumer/PaymentConfirmedConsumer.java"
      to: "LotService.grantPurchased"
      via: "12-month purchased grant after idempotency check"
      pattern: "grantPurchased"
    - from: "services/payment-service/src/main/java/com/opendesk/payment/reconciliation/ReconciliationJob.java"
      to: "PaymentGateway.queryTransactionStatus"
      via: "poll Azampay for stale payments"
      pattern: "queryTransactionStatus"
---

<objective>
Closes Phase 3: the wallet grants PURCHASED credits on PaymentConfirmed (12-month expiry, completing the purchase→credit loop via AMQP per D-12), the idempotent refund mechanism (PYMT-08, D-07 — callable by Phase 4), low-credit alerts (WLET-04, D-08) and 7-day expiry warnings (WLET-05) with expiry sweep, and the Azampay reconciliation job + prod gateway impl (D-04 late-success, mock-first).

Purpose: Complete the credit lifecycle, alerting, expiry enforcement, and the reconciliation safety net so no confirmed payment is silently lost.
Output: PaymentConfirmed consumer, refund API, three scheduled wallet jobs, payment reconciliation job, and the AzampayPaymentGateway prod impl.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-wallet-payments/03-CONTEXT.md
@.planning/phases/03-wallet-payments/03-RESEARCH.md
@.planning/phases/03-wallet-payments/03-PATTERNS.md

<interfaces>
<!-- From 03-02, 03-04, 03-05 (this plan depends on them) -->
LotService.grantPurchased(userId, credits, paymentId)/grantBonus/creditBack — from 03-02.
CreditLotRepository.sumAvailableCredits(userId, now) — from 03-02; add findExpiringBefore(cutoff, lotType) here.
ProcessedEventRepository.tryInsert(eventId), wallet OutboxEntry/OutboxRepository/OutboxRelay pattern — wallet outbox is created in THIS plan (copy from identity; 03-04 only added processed_events + RabbitMqConfig). RabbitMqConfig EXCHANGE "wallet.events" — from 03-04.
PaymentConfirmedEvent payload published by payment-service to payment.events with key payment.PaymentConfirmed (from 03-05): {eventId, userId, paymentId, smsCount}.
PaymentGateway.queryTransactionStatus, PaymentRepository.findByStatusInAndCreatedAtBefore, CallbackProcessor/processLateSuccess path — from 03-03/03-05.
app.wallet.low-credit-threshold=20 (D-08) — from 03-01 config.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: PaymentConfirmed consumer (PURCHASED grant) + idempotent refund mechanism</name>
  <files>services/wallet-service/src/main/java/com/opendesk/wallet/consumer/PaymentConfirmedConsumer.java, services/wallet-service/src/main/java/com/opendesk/wallet/consumer/PaymentConfirmedEvent.java, services/wallet-service/src/main/java/com/opendesk/wallet/refund/RefundService.java, services/wallet-service/src/main/java/com/opendesk/wallet/refund/RefundController.java, services/wallet-service/src/main/java/com/opendesk/wallet/refund/RefundRequest.java, services/wallet-service/src/main/java/com/opendesk/wallet/outbox/OutboxEntry.java, services/wallet-service/src/main/java/com/opendesk/wallet/outbox/OutboxRepository.java, services/wallet-service/src/main/java/com/opendesk/wallet/outbox/OutboxRelay.java, services/wallet-service/src/main/resources/db/migration/V4__create_outbox.sql, services/wallet-service/src/test/java/com/opendesk/wallet/RefundIT.java</files>
  <read_first>
    - 03-PATTERNS.md Outbox section (lines 52-193 — copy verbatim), "UserVerifiedConsumer" idempotency (lines 355-403), "Idempotency Guard" (lines 828-833)
    - 03-RESEARCH.md Pattern 4/6 (idempotent grant), Security Domain (refund must reference a real payment; delta > 0)
    - 03-CONTEXT.md D-04 (purchased grant on confirm), D-07 (refund = idempotent ledger credit-back, Phase 4 calls it), D-03 (12-month purchased expiry)
    - 03-02 outputs: LotService.grantPurchased/creditBack; 03-04: ProcessedEventRepository, RabbitMqConfig; 03-05: PaymentConfirmedEvent shape
  </read_first>
  <behavior>
    - Test (PaymentConfirmedConsumer, PYMT-04 grant side): a PaymentConfirmed event (eventId, userId, paymentId, smsCount) grants exactly one PURCHASED lot with 12-month expiry and smsCount credits; re-delivery of the same eventId grants nothing more (processed_events idempotency).
    - Test (RefundIT, PYMT-08, D-07): RefundService.refund(userId, credits, referenceId, idempotencyKey) creates a REFUND lot crediting the wallet; calling twice with the same idempotencyKey credits only once (idempotent). delta must be > 0 (reject non-positive).
    - Test: POST /api/v1/wallet/refunds (internal/admin-scoped) drives the same idempotent path.
  </behavior>
  <action>
    Write RefundIT FIRST (extends AbstractWalletIntegrationTest, converts 03-01 placeholder); include the PaymentConfirmed consumer assertion as a test method INSIDE RefundIT — do NOT create a separate PaymentConfirmedConsumerIT file (keeps a single verify target). Run RED. Then implement:
    Copy OutboxEntry/OutboxRepository/OutboxRelay verbatim from identity-service into com.opendesk.wallet.outbox (package + exchange constant RabbitMqConfig.EXCHANGE "wallet.events"). V4__create_outbox.sql = copy of identity V3__create_outbox.sql.
    PaymentConfirmedEvent record(eventId, userId, paymentId, smsCount) (mirror locally — no payment package import).
    PaymentConfirmedConsumer @Component @RabbitListener @QueueBinding(queue "wallet.payment.PaymentConfirmed" durable, exchange "payment.events" TOPIC durable, key "payment.PaymentConfirmed") @Transactional: if !processedEventRepository.tryInsert(eventId) return; else lotService.grantPurchased(userId, smsCount, paymentId) (12-month expiry per D-03).
    RefundService.refund(userId, credits, referenceId, idempotencyKey) @Transactional: validate credits > 0; if !processedEventRepository.tryInsert("refund:"+idempotencyKey) return existing/no-op; else lotService.creditBack(userId, credits, referenceId) → REFUND lot + REFUND credit_transactions row (append-only, D-07). RefundRequest record(UUID userId, int credits, UUID referenceId, String idempotencyKey) with @Positive credits. RefundController POST /api/v1/wallet/refunds (internal — callable by Phase 4; keep authenticated). Use jakarta.* only.
  </action>
  <verify>
    <automated>./gradlew :services:wallet-service:test --tests "RefundIT" --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - RefundIT GREEN incl. double-call-single-credit and reject non-positive
    - PaymentConfirmed grant is idempotent (re-delivery grants nothing) and uses 12-month expiry
    - RefundService rejects credits <= 0
    - wallet OutboxRelay publishes to `wallet.events` (exchange constant), not identity/payment
    - V4__create_outbox.sql contains `outbox` with unique event_id
    - No `javax.` import; no `com.opendesk.payment` import in wallet sources
  </acceptance_criteria>
  <done>Purchased credits land via PaymentConfirmed (idempotent, 12-month); the refund mechanism is an idempotent append-only credit-back ready for Phase 4.</done>
</task>

<task type="tdd">
  <name>Task 2: Wallet alert/expiry sweep jobs + payment reconciliation + Azampay prod gateway</name>
  <files>services/wallet-service/src/main/java/com/opendesk/wallet/sweep/ExpirySweepJob.java, services/wallet-service/src/main/java/com/opendesk/wallet/sweep/LowCreditAlertJob.java, services/wallet-service/src/main/java/com/opendesk/wallet/sweep/ExpiryWarningJob.java, services/wallet-service/src/main/java/com/opendesk/wallet/lot/CreditLotRepository.java, services/payment-service/src/main/java/com/opendesk/payment/reconciliation/ReconciliationJob.java, services/payment-service/src/main/java/com/opendesk/payment/gateway/AzampayPaymentGateway.java, services/payment-service/src/main/java/com/opendesk/payment/gateway/AzampayTokenProvider.java, services/payment-service/src/main/java/com/opendesk/payment/config/Resilience4jConfig.java, services/wallet-service/src/test/java/com/opendesk/wallet/LowCreditAlertIT.java, services/wallet-service/src/test/java/com/opendesk/wallet/ExpiryWarningIT.java, services/payment-service/src/test/java/com/opendesk/payment/ReconciliationIT.java</files>
  <read_first>
    - 03-PATTERNS.md "ReconciliationJob and ExpirySweepJob/LowCreditAlertJob" (lines 295-351), "PaymentGateway ... AzampayPaymentGateway" prod shape (lines 255-291)
    - 03-RESEARCH.md Pattern 5 (reconciliation, D-04 late-success), Pattern 7 (sweep jobs), Azampay API Shape (mobileCheckout, transactionStatus, token endpoint, amount as String), Code Examples (Azampay STK push)
    - 03-CONTEXT.md D-04 (EXPIRED→SUCCESS via reconciliation), D-08 (fixed 20-credit threshold, in-app + outbox), D-03 (expiry windows)
    - services/identity-service/.../verification/RealNidaVerificationService.java + VerificationRetryJob.java (prod gateway + scheduled job analogs)
    - 03-05 outputs: CallbackProcessor late-success path, PaymentRepository.findByStatusInAndCreatedAtBefore
  </read_first>
  <behavior>
    - Test (LowCreditAlertIT, WLET-04): a user whose derived balance is below app.wallet.low-credit-threshold (20) gets exactly one LowCreditAlert outbox event per alert cycle (not re-alerted every run); users above threshold get none.
    - Test (ExpiryWarningIT, WLET-05): a PURCHASED lot expiring within 7 days produces one ExpiryWarning outbox event; lots expiring later produce none.
    - Test: ExpirySweepJob marks lots past expires_at as expired (writes EXPIRE credit_transactions for remaining; excluded from balance — belt-and-suspenders to the balance filter).
    - Test (ReconciliationIT, D-04): an EXPIRED payment that Azampay (stub) reports success is flipped to SUCCESS and emits PaymentConfirmed exactly once via the callback/late-success path; a still-pending stub stays unchanged.
  </behavior>
  <action>
    Write LowCreditAlertIT + ExpiryWarningIT + ReconciliationIT FIRST (extend the appropriate Abstract base, convert 03-01 placeholders); run RED. Then implement:
    Add to CreditLotRepository: findExpiringBefore(Instant cutoff, LotType lotType, Pageable) for warning sweep; a markExpired query or fetch-then-update for ExpirySweepJob.
    ExpiryWarningJob @Scheduled(cron daily): find PURCHASED lots expiring within 7 days not yet warned → emit ExpiryWarning outbox event (dedupe per lot/user-day). ExpirySweepJob @Scheduled(cron daily): mark expired lots, write EXPIRE transactions. LowCreditAlertJob @Scheduled(fixedDelay ~5min): users with sumAvailableCredits < threshold not yet alerted this cycle → emit LowCreditAlert outbox event. All three follow the VerificationRetryJob structure (bounded page, early-empty exit, per-item try/catch). Dedup via a simple already-alerted/already-warned marker (processed_events keyed per user+cycle, or a lightweight flag) — keep MVP-simple, document the choice.
    ReconciliationJob @Component @Scheduled(fixedDelayString app.reconciliation.fixed-delay-ms 120000) @Transactional: find PENDING+EXPIRED payments older than 5 min (findByStatusInAndCreatedAtBefore, bounded); for each call paymentGateway.queryTransactionStatus(externalId); if success → drive the same idempotent late-success path as CallbackProcessor (flip to SUCCESS + PaymentConfirmed outbox once). Reuse CallbackProcessor.processLateSuccess if exposed, else replicate the idempotent guard.
    AzampayPaymentGateway @Profile("prod") @Service: RestClient (connect 5s/read 30s), @CircuitBreaker(name "azampay", fallback) + @Retryable on initiateStkPush/queryTransactionStatus; POST /azampay/mobileCheckout (amount as String, externalId = payment UUID, provider), GET /azampay/transactionStatus?pgReferenceId={externalId}. AzampayTokenProvider fetches/refreshes bearer token from the GenerateToken endpoint. Resilience4jConfig declares the "azampay" circuit breaker + retry config. AzampayPaymentGateway is prod-only — ITs run under stub profile so it is compile-verified, not executed. Use jakarta.* only; RestClient not RestTemplate.
  </action>
  <verify>
    <automated>./gradlew :services:wallet-service:test --tests "LowCreditAlertIT" --tests "ExpiryWarningIT" :services:payment-service:test --tests "ReconciliationIT" --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - LowCreditAlertIT, ExpiryWarningIT, ReconciliationIT GREEN
    - LowCreditAlert emitted only below threshold and not re-emitted every run (dedupe asserted)
    - ExpiryWarning emitted only for lots within 7 days
    - ReconciliationJob flips EXPIRED→SUCCESS on stub-success and emits exactly one PaymentConfirmed (idempotent, D-04)
    - AzampayPaymentGateway annotated `@Profile("prod")`, uses RestClient + @CircuitBreaker; `grep -c 'RestTemplate' services/payment-service/src/main/java/com/opendesk/payment/gateway/AzampayPaymentGateway.java` returns 0
    - No `javax.` import
  </acceptance_criteria>
  <done>Low-credit and expiry-warning outbox events fire correctly, expired lots are swept, reconciliation rescues late-confirmed payments idempotently, and the prod Azampay gateway compiles behind @Profile("prod").</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payment.events AMQP → wallet PaymentConfirmed consumer | At-least-once; replay possible |
| Phase 4 → refund API | Internal caller; must be idempotent + positive-amount |
| payment-service → Azampay status API | External polling for reconciliation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Tampering | Duplicate PaymentConfirmed double-grants purchased credits | mitigate | processed_events tryInsert before grantPurchased; re-delivery grants nothing (asserted) |
| T-03-17 | Tampering | Refund double-credit / negative refund | mitigate | idempotencyKey via processed_events + credits > 0 validation (Security Domain); RefundIT asserts single credit + rejection |
| T-03-18 | Tampering | Reconciliation double-credits a payment already confirmed by callback | mitigate | Shared idempotent late-success path (SUCCESS guard + outbox event_id UNIQUE); ReconciliationIT asserts single PaymentConfirmed |
| T-03-19 | Denial of Service | Azampay status API slow/down stalls reconciliation | mitigate | Resilience4j @CircuitBreaker + @Retryable + RestClient timeouts (5s/30s) on AzampayPaymentGateway |
| T-03-20 | Spoofing | Alert/expiry spam (re-alerting every run) | accept | Dedupe marker limits to one alert per cycle; residual duplication low-impact at MVP (D-08 in-app + outbox) |
</threat_model>

<verification>
- `./gradlew :services:wallet-service:test :services:payment-service:test` full suite GREEN
- All 14 Phase 3 ITs converted from placeholders are now real and green
- Reconciliation late-success idempotent; refund idempotent; alerts deduped
</verification>

<success_criteria>
Purchase→credit loop closed via PaymentConfirmed (idempotent 12-month grant); idempotent refund mechanism ready for Phase 4; low-credit + 7-day expiry alerts emit outbox events; expiry sweep enforces expiry; reconciliation rescues late-confirmed payments; prod Azampay gateway in place behind @Profile("prod").
</success_criteria>

<output>
Create `.planning/phases/03-wallet-payments/03-06-SUMMARY.md` when done
</output>
