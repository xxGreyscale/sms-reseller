---
phase: 03-wallet-payments
plan: 03
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - services/payment-service/src/main/java/com/smsreseller/payment/bundle/SmsBundle.java
  - services/payment-service/src/main/java/com/smsreseller/payment/bundle/BundleRepository.java
  - services/payment-service/src/main/java/com/smsreseller/payment/bundle/BundleController.java
  - services/payment-service/src/main/java/com/smsreseller/payment/bundle/BundleDto.java
  - services/payment-service/src/main/java/com/smsreseller/payment/payment/Payment.java
  - services/payment-service/src/main/java/com/smsreseller/payment/payment/PaymentStatus.java
  - services/payment-service/src/main/java/com/smsreseller/payment/payment/PaymentRepository.java
  - services/payment-service/src/main/java/com/smsreseller/payment/gateway/PaymentGateway.java
  - services/payment-service/src/main/java/com/smsreseller/payment/gateway/StubPaymentGateway.java
  - services/payment-service/src/main/java/com/smsreseller/payment/gateway/StkPushRequest.java
  - services/payment-service/src/main/java/com/smsreseller/payment/gateway/StkPushResult.java
  - services/payment-service/src/main/java/com/smsreseller/payment/gateway/TransactionStatusResult.java
  - services/payment-service/src/main/java/com/smsreseller/payment/config/SecurityConfig.java
  - services/payment-service/src/main/java/com/smsreseller/payment/config/RabbitMqConfig.java
  - services/payment-service/src/main/resources/db/migration/V1__create_sms_bundles.sql
  - services/payment-service/src/main/resources/db/migration/V2__seed_sms_bundles.sql
  - services/payment-service/src/main/resources/db/migration/V3__create_payments.sql
  - services/payment-service/src/test/java/com/smsreseller/payment/BundleCatalogIT.java
autonomous: true
requirements: [PYMT-01]

must_haves:
  truths:
    - "GET /api/v1/bundles returns the 5 seeded bundles with correct raw-TZS prices"
    - "The Taster bundle is FREE and marked not purchasable via Azampay"
    - "payments table enforces one PENDING payment per user (partial unique index)"
    - "PaymentGateway interface has a stub implementation active under the stub profile"
  artifacts:
    - path: "services/payment-service/src/main/resources/db/migration/V2__seed_sms_bundles.sql"
      provides: "Seeded bundle catalog (Taster/Starter/Growth/Pro/Scale)"
      contains: "Starter"
    - path: "services/payment-service/src/main/resources/db/migration/V3__create_payments.sql"
      provides: "payments table + single-pending partial unique index"
      contains: "uq_payments_user_pending"
    - path: "services/payment-service/src/main/java/com/smsreseller/payment/gateway/PaymentGateway.java"
      provides: "Mock-first gateway contract"
  key_links:
    - from: "services/payment-service/src/main/java/com/smsreseller/payment/bundle/BundleController.java"
      to: "BundleRepository"
      via: "findByIsActiveTrue"
      pattern: "BundleRepository"
---

<objective>
Payment-service foundation: the Flyway-seeded bundle catalog (PYMT-01, D-09), the Payment entity + state machine enum + repository with the single-pending partial unique index (D-05/D-13), the mock-first PaymentGateway interface + StubPaymentGateway (D-10), and the JWT resource-server SecurityConfig + RabbitMqConfig. No payment flow logic yet — that is Plan 05.

Purpose: Stand up the payment-service skeleton, the read-only catalog API, and the gateway seam so Plan 05 can implement the purchase flow against fixed contracts.
Output: A working `GET /api/v1/bundles` endpoint, a payments schema ready for the flow, and a stub gateway.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-wallet-payments/03-CONTEXT.md
@.planning/phases/03-wallet-payments/03-RESEARCH.md
@.planning/phases/03-wallet-payments/03-PATTERNS.md

<interfaces>
<!-- Contracts Plan 05 consumes. Extracted from 03-PATTERNS.md + 03-RESEARCH.md -->
PaymentStatus enum: PENDING, SUCCESS, EXPIRED, FAILED (D-06).
Payment fields: id UUID, userId UUID, bundleId UUID, amountTzs long (BIGINT raw TZS — D-11), smsCount int, status PaymentStatus, externalId String (= payment UUID string, Azampay idempotency key, UNIQUE), operatorReference String nullable, provider String, createdAt, updatedAt.
SmsBundle fields: id UUID, name String, smsCount int, priceTzs long (BIGINT raw TZS), isActive boolean, isPurchasable boolean, description String nullable.
PaymentGateway: StkPushResult initiateStkPush(StkPushRequest); TransactionStatusResult queryTransactionStatus(String externalId).
StkPushRequest record(UUID paymentId, String msisdn, long amountTzs, String provider).
StkPushResult: accepted(String externalId) factory; fields success, externalId, message.
TransactionStatusResult: success/failed/pending(externalId) factories; isSuccess(), isFailed(), isPending().
PaymentRepository: findByExternalId(String); findByUserIdOrderByCreatedAtDesc(userId, Pageable); existsByUserIdAndStatus(userId, PENDING); findByStatusInAndCreatedAtBefore(statuses, cutoff, Pageable).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bundle catalog — entity, seed migration, read API (PYMT-01)</name>
  <files>services/payment-service/src/main/java/com/smsreseller/payment/bundle/SmsBundle.java, services/payment-service/src/main/java/com/smsreseller/payment/bundle/BundleRepository.java, services/payment-service/src/main/java/com/smsreseller/payment/bundle/BundleController.java, services/payment-service/src/main/java/com/smsreseller/payment/bundle/BundleDto.java, services/payment-service/src/main/java/com/smsreseller/payment/config/SecurityConfig.java, services/payment-service/src/main/java/com/smsreseller/payment/config/RabbitMqConfig.java, services/payment-service/src/main/resources/db/migration/V1__create_sms_bundles.sql, services/payment-service/src/main/resources/db/migration/V2__seed_sms_bundles.sql, services/payment-service/src/test/java/com/smsreseller/payment/BundleCatalogIT.java</files>
  <read_first>
    - 03-PATTERNS.md "Flyway migrations" seed block (lines 684-694 — raw TZS BIGINT, is_purchasable), "REST Controller pattern" (lines 512-549), "SecurityConfig" (lines 569-601), "RabbitMqConfig" (lines 604-640)
    - 03-CONTEXT.md D-09 (catalog DB table seeded via Flyway), D-11 (raw TZS BIGINT — supersedes ×100 cents), D-12 (catalog inside payment-service)
    - services/identity-service/src/main/java/com/smsreseller/identity/config/SecurityConfig.java + RabbitMqConfig.java (analogs)
    - PROJECT.md §Pricing Model (LOCKED bundle definitions)
  </read_first>
  <behavior>
    - Test (BundleCatalogIT, PYMT-01): GET /api/v1/bundles returns 5 active bundles; Starter has smsCount=200 and priceTzs=3200 (raw TZS, NOT 320000); Taster has priceTzs=0 and isPurchasable=false; response is JSON with name/smsCount/priceTzs/isPurchasable.
  </behavior>
  <action>
    Write BundleCatalogIT FIRST (extends AbstractPaymentIntegrationTest, converts 03-01 placeholder) asserting the above; run RED. Then implement:
    V1__create_sms_bundles.sql: sms_bundles(id UUID PK default gen_random_uuid, name VARCHAR, sms_count INT, price_tzs BIGINT, is_active BOOLEAN default true, is_purchasable BOOLEAN default true, description VARCHAR nullable).
    V2__seed_sms_bundles.sql per D-11 raw TZS (NOT ×100): Taster 50/0/false, Starter 200/3200/true, Growth 1000/14500/true, Pro 5000/65000/true, Scale 20000/240000/true; ON CONFLICT DO NOTHING. (Override the ×100 "cents" comment in PATTERNS — use raw shillings per D-11.)
    SmsBundle @Entity (jakarta.*, Lombok, priceTzs mapped to long — never BigDecimal/Double per Pitfall 7). BundleRepository.findByIsActiveTrue(). BundleDto record(id, name, smsCount, priceTzs, isPurchasable, description) with from(SmsBundle) factory. BundleController GET /api/v1/bundles returns active bundles as DTOs (read-only; authenticated via JWT).
    SecurityConfig: JWT resource-server only (no PasswordEncoder/DaoAuthProvider), CSRF disabled, STATELESS, permitAll on /actuator/health/** and /error AND /api/v1/payments/callback (public Azampay webhook — for Plan 05), anyRequest authenticated, oauth2ResourceServer.jwt. RabbitMqConfig: TopicExchange "payment.events" durable, ROUTING_KEY_PREFIX "payment.", Jackson2JsonMessageConverter + RabbitTemplate (does NOT redeclare identity.events). Use jakarta.* only.
  </action>
  <verify>
    <automated>./gradlew :services:payment-service:test --tests "BundleCatalogIT" --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - BundleCatalogIT GREEN
    - V2__seed_sms_bundles.sql contains `3200` (Starter raw TZS) and does NOT contain `320000`
    - V2 seeds Taster with is_purchasable false and price 0
    - SecurityConfig permits `/api/v1/payments/callback` and authenticates all else
    - priceTzs mapped to `long` (no BigDecimal/Double): `grep -rE 'BigDecimal|Double' services/payment-service/src/main/java/com/smsreseller/payment/bundle/` returns nothing
    - No `javax.` import
  </acceptance_criteria>
  <done>The 5-bundle catalog is seeded with raw-TZS prices and served read-only via JWT-protected GET /api/v1/bundles; security and AMQP config exist.</done>
</task>

<task type="auto">
  <name>Task 2: Payment entity + state machine + repository + PaymentGateway interface & stub</name>
  <files>services/payment-service/src/main/java/com/smsreseller/payment/payment/Payment.java, services/payment-service/src/main/java/com/smsreseller/payment/payment/PaymentStatus.java, services/payment-service/src/main/java/com/smsreseller/payment/payment/PaymentRepository.java, services/payment-service/src/main/java/com/smsreseller/payment/gateway/PaymentGateway.java, services/payment-service/src/main/java/com/smsreseller/payment/gateway/StubPaymentGateway.java, services/payment-service/src/main/java/com/smsreseller/payment/gateway/StkPushRequest.java, services/payment-service/src/main/java/com/smsreseller/payment/gateway/StkPushResult.java, services/payment-service/src/main/java/com/smsreseller/payment/gateway/TransactionStatusResult.java, services/payment-service/src/main/resources/db/migration/V3__create_payments.sql</files>
  <read_first>
    - 03-PATTERNS.md "PaymentGateway interface + Stub + Azampay" (lines 197-291), "JPA Entity pattern" (Payment differences, lines 459-464), "Flyway migrations" single-pending index (lines 676-682)
    - 03-RESEARCH.md Pattern 3 (gateway), Payment State Machine enum (lines 677-685), Azampay API Shape (externalId = our payment UUID), Open Question 4 → D-13 (DB partial unique index)
    - 03-CONTEXT.md D-05/D-13 (single pending payment via partial unique index), D-06 (lifecycle), D-10 (mock-first), D-11 (raw TZS)
  </read_first>
  <action>
    V3__create_payments.sql: payments(id UUID PK, user_id UUID, bundle_id UUID, amount_tzs BIGINT, sms_count INT, status VARCHAR(20), external_id VARCHAR(128) UNIQUE, operator_reference VARCHAR nullable, provider VARCHAR, created_at TIMESTAMPTZ default now, updated_at TIMESTAMPTZ). Add partial unique index `uq_payments_user_pending ON payments(user_id) WHERE status = 'PENDING'` (D-13). COMMENT ON COLUMN external_id 'Azampay idempotency key = payment UUID'.
    PaymentStatus enum PENDING/SUCCESS/EXPIRED/FAILED (D-06). Payment @Entity (jakarta.*, Lombok, @Enumerated STRING status, amountTzs long, @CreatedDate/@LastModifiedDate). PaymentRepository: findByExternalId, findByUserIdOrderByCreatedAtDesc(Pageable), existsByUserIdAndStatus(userId, PaymentStatus), findByStatusInAndCreatedAtBefore(List<PaymentStatus>, Instant, Pageable).
    PaymentGateway interface: initiateStkPush(StkPushRequest), queryTransactionStatus(String externalId). StkPushRequest record(UUID paymentId, String msisdn, long amountTzs, String provider). StkPushResult with accepted(externalId) factory + success/externalId/message fields. TransactionStatusResult with success/failed/pending(externalId) factories + isSuccess/isFailed/isPending.
    StubPaymentGateway @Profile("stub") @Service: magic externalId-suffix outcome control (...0001→FAILURE, ...0002→TIMEOUT/pending, default→SUCCESS), @Value app.payment.stub.default-outcome + delay-ms; initiateStkPush returns accepted immediately; queryTransactionStatus resolves outcome by suffix (mirrors StubNidaVerificationService magic-NIN pattern). Do NOT create AzampayPaymentGateway here — Plan 05/06 owns prod impl. Use jakarta.* only.
  </action>
  <verify>
    <automated>./gradlew :services:payment-service:compileJava :services:payment-service:compileTestJava --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - V3__create_payments.sql contains `uq_payments_user_pending` and `WHERE status = 'PENDING'` and `external_id VARCHAR(128) UNIQUE` (or named unique constraint)
    - PaymentStatus enum has exactly PENDING, SUCCESS, EXPIRED, FAILED
    - StubPaymentGateway annotated `@Profile("stub")` and implements PaymentGateway
    - PaymentRepository declares findByExternalId and existsByUserIdAndStatus
    - amountTzs is `long`; no BigDecimal/Double in payment package
    - `compileJava` + `compileTestJava` BUILD SUCCESSFUL; no `javax.` import
  </acceptance_criteria>
  <done>Payment schema with single-pending enforcement, the 4-state enum, repository query seams, and a profile-gated stub gateway exist for Plan 05 to build the flow on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→bundle/payment API | JWT-validated requests |
| double-submit purchase → payments table | Single-pending enforcement boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Concurrent purchase creating two PENDING payments | mitigate | Partial unique index uq_payments_user_pending (D-13); INSERT fails on second pending — verified in Plan 05 |
| T-03-06 | Elevation of Privilege | User reads bundles/payments without auth | mitigate | SecurityConfig authenticates all except actuator/error/callback; JWT resource-server via shared-security |
| T-03-07 | Tampering | TZS stored as float | mitigate | priceTzs/amountTzs are BIGINT→long (Pitfall 7); grep gate asserts no BigDecimal/Double |
</threat_model>

<verification>
- `./gradlew :services:payment-service:test --tests "BundleCatalogIT"` GREEN
- `./gradlew :services:payment-service:compileJava :services:payment-service:compileTestJava` SUCCESSFUL
- Single-pending partial unique index present in V3
</verification>

<success_criteria>
Seeded bundle catalog served via authenticated GET /api/v1/bundles; payments schema with single-pending index; PaymentGateway stub seam ready — all on payment-service with JWT + AMQP config.
</success_criteria>

<output>
Create `.planning/phases/03-wallet-payments/03-03-SUMMARY.md` when done
</output>
