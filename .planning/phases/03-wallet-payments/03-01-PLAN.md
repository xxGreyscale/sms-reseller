---
phase: 03-wallet-payments
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - services/wallet-service/build.gradle.kts
  - services/payment-service/build.gradle.kts
  - services/wallet-service/src/main/java/com/opendesk/wallet/WalletServiceApplication.java
  - services/payment-service/src/main/java/com/opendesk/payment/PaymentServiceApplication.java
  - services/wallet-service/src/main/resources/application.yml
  - services/payment-service/src/main/resources/application.yml
  - services/wallet-service/src/test/resources/application-test.yml
  - services/payment-service/src/test/resources/application-test.yml
  - services/wallet-service/src/test/java/com/opendesk/wallet/AbstractWalletIntegrationTest.java
  - services/payment-service/src/test/java/com/opendesk/payment/AbstractPaymentIntegrationTest.java
  - services/wallet-service/src/test/resources/test-keys/jwt-private.pem
  - services/wallet-service/src/test/resources/test-keys/jwt-public.pem
  - services/payment-service/src/test/resources/test-keys/jwt-private.pem
  - services/payment-service/src/test/resources/test-keys/jwt-public.pem
  - services/wallet-service/src/test/java/com/opendesk/wallet/BalanceIT.java
  - services/wallet-service/src/test/java/com/opendesk/wallet/TransactionHistoryIT.java
  - services/wallet-service/src/test/java/com/opendesk/wallet/CreditReservationIT.java
  - services/wallet-service/src/test/java/com/opendesk/wallet/LowCreditAlertIT.java
  - services/wallet-service/src/test/java/com/opendesk/wallet/ExpiryWarningIT.java
  - services/wallet-service/src/test/java/com/opendesk/wallet/CreditLotExpiryTest.java
  - services/wallet-service/src/test/java/com/opendesk/wallet/UserVerifiedConsumerIT.java
  - services/payment-service/src/test/java/com/opendesk/payment/BundleCatalogIT.java
  - services/payment-service/src/test/java/com/opendesk/payment/PaymentInitiationIT.java
  - services/payment-service/src/test/java/com/opendesk/payment/PaymentTimeoutIT.java
  - services/payment-service/src/test/java/com/opendesk/payment/CallbackProcessingIT.java
  - services/payment-service/src/test/java/com/opendesk/payment/PaymentHistoryIT.java
  - services/payment-service/src/test/java/com/opendesk/payment/RefundIT.java
  - services/payment-service/src/test/java/com/opendesk/payment/ReconciliationIT.java
autonomous: true
requirements: [WLET-01, WLET-02, WLET-03, WLET-04, WLET-05, WLET-06, WLET-07, PYMT-01, PYMT-02, PYMT-03, PYMT-04, PYMT-05, PYMT-06, PYMT-07, PYMT-08]

must_haves:
  truths:
    - "wallet-service and payment-service compile with the full Phase 3 dependency set"
    - "Both services have a Testcontainers integration base (PG16 + Redis 7 + RabbitMQ) mirroring identity-service"
    - "Every Phase 3 requirement has a named, runnable placeholder IT so the validation map is non-empty"
  artifacts:
    - path: "services/wallet-service/src/test/java/com/opendesk/wallet/AbstractWalletIntegrationTest.java"
      provides: "PG16 @ServiceConnection + Redis 7 + RabbitMQ container base, stub+test profiles"
      contains: "postgres:16"
    - path: "services/payment-service/src/test/java/com/opendesk/payment/AbstractPaymentIntegrationTest.java"
      provides: "PG16 @ServiceConnection + Redis 7 + RabbitMQ container base, stub+test profiles"
      contains: "postgres:16"
  key_links:
    - from: "services/wallet-service/build.gradle.kts"
      to: "libs:shared-security"
      via: "implementation(project(...))"
      pattern: "shared-security"
---

<objective>
Wave 0 foundation for both Phase 3 modules. Add the full dependency set to wallet-service and payment-service, create Spring Boot application entrypoints and config, build Testcontainers integration bases (mirroring identity-service `AbstractIntegrationTest`), and scaffold one named placeholder IT per Phase 3 requirement so every later plan has a concrete test target to rewrite (the Phase 2 02-01 convention).

Purpose: Establish a non-empty validation map and a compiling, testable two-module foundation before any ledger or payment logic exists.
Output: Two buildable modules with green test infra and 14 placeholder ITs reporting "skipped" via `Assumptions.abort`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wallet-payments/03-CONTEXT.md
@.planning/phases/03-wallet-payments/03-RESEARCH.md
@.planning/phases/03-wallet-payments/03-PATTERNS.md
@.planning/phases/03-wallet-payments/03-VALIDATION.md
@.planning/phases/02-identity-auth/02-01-SUMMARY.md

<interfaces>
<!-- Mirror these exactly from identity-service. Extracted from 02-01-SUMMARY.md + 03-PATTERNS.md -->
identity-service build deps to replicate (build.gradle.kts): spring-boot-starter-web, data-jpa, security, oauth2-resource-server, validation, data-redis, amqp, spring-retry, resilience4j-spring-boot3, mapstruct(+processor), flyway-core, flyway-postgresql, postgresql-driver runtimeOnly; test: spring-boot-testcontainers, testcontainers-postgresql, testcontainers-rabbitmq, testcontainers-junit-jupiter. All via `libs.*` version catalog entries that already exist (added in 02-01).

AbstractIntegrationTest base shape (from 03-PATTERNS.md lines 700-748): @SpringBootTest RANDOM_PORT, @ActiveProfiles({"stub","test"}), static PostgreSQLContainer("postgres:16") @ServiceConnection, GenericContainer("redis:7") + RabbitMQContainer("rabbitmq:3-management") wired via @DynamicPropertySource. wallet DB name "wallet_test", payment DB name "payment_test".

Placeholder IT convention (from 02-01-SUMMARY.md lines 110-125): plain JUnit test, NO @SpringBootTest, does NOT extend the Abstract base, single @Test method that calls org.junit.jupiter.api.Assumptions.abort("placeholder — implemented in 03-NN"). Reports as skipped, not failed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build deps + application entrypoints + config for both services</name>
  <files>services/wallet-service/build.gradle.kts, services/payment-service/build.gradle.kts, services/wallet-service/src/main/java/com/opendesk/wallet/WalletServiceApplication.java, services/payment-service/src/main/java/com/opendesk/payment/PaymentServiceApplication.java, services/wallet-service/src/main/resources/application.yml, services/payment-service/src/main/resources/application.yml</files>
  <read_first>
    - services/wallet-service/build.gradle.kts and services/payment-service/build.gradle.kts (current skeletons — only have web, data-jpa, flyway, shared libs)
    - services/identity-service/build.gradle.kts (the full dep set to mirror — analog)
    - 02-01-SUMMARY.md (application.yml keys: virtual threads, flyway, actuator probes; application-test.yml short TTL + create-drop)
    - 03-RESEARCH.md "Installation (additions beyond Phase 2)" + Standard Stack table
  </read_first>
  <action>
    Add to BOTH wallet-service and payment-service build.gradle.kts the same dependency set identity-service uses, via existing `libs.*` catalog refs: spring-boot-starter-security, oauth2-resource-server, validation, data-redis, amqp, spring-retry, resilience4j-spring-boot3, mapstruct + mapstruct-processor (annotationProcessor, after lombok), and runtimeOnly postgresql-driver. flyway-core/flyway-postgresql are already present. Add test deps: spring-boot-testcontainers, testcontainers-postgresql, testcontainers-rabbitmq, testcontainers-junit-jupiter. Do NOT add spring-boot-starter-mail (Phase 5 owns notification delivery). payment-service additionally needs no extra catalog entries beyond identity's set.
    Create WalletServiceApplication and PaymentServiceApplication with @SpringBootApplication, @EnableScheduling, @EnableJpaAuditing (mirror IdentityServiceApplication). Packages com.opendesk.wallet and com.opendesk.payment.
    Create application.yml for each: virtual threads enabled (spring.threads.virtual.enabled=true), Flyway enabled, datasource placeholder, actuator health probes on main port, shared-security resource-server public-key-location key. wallet config key app.wallet.low-credit-threshold=20 (D-08). payment config keys app.payment.stub.default-outcome=SUCCESS, app.payment.stub.delay-ms=500, app.reconciliation.fixed-delay-ms=120000, app.payment.timeout-seconds=120 (D-06 2-min). Use jakarta.* only; no javax.*.
  </action>
  <verify>
    <automated>./gradlew :services:wallet-service:compileJava :services:payment-service:compileJava --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :services:wallet-service:compileJava :services:payment-service:compileJava` BUILD SUCCESSFUL
    - Both build.gradle.kts contain `libs.spring.boot.starter.oauth2.resource.server` and `libs.testcontainers.rabbitmq`
    - WalletServiceApplication.java contains `@SpringBootApplication`, `@EnableScheduling`, `@EnableJpaAuditing`
    - payment-service application.yml contains `app.payment.timeout-seconds` and `app.reconciliation.fixed-delay-ms`
    - No `javax.` import in any created file
  </acceptance_criteria>
  <done>Both modules compile with the full Phase 3 dep set; entrypoints and config exist with the documented keys.</done>
</task>

<task type="auto">
  <name>Task 2: AbstractIntegrationTest bases + RSA test keys + application-test.yml</name>
  <files>services/wallet-service/src/test/java/com/opendesk/wallet/AbstractWalletIntegrationTest.java, services/payment-service/src/test/java/com/opendesk/payment/AbstractPaymentIntegrationTest.java, services/wallet-service/src/test/resources/application-test.yml, services/payment-service/src/test/resources/application-test.yml, services/wallet-service/src/test/resources/test-keys/jwt-private.pem, services/wallet-service/src/test/resources/test-keys/jwt-public.pem, services/payment-service/src/test/resources/test-keys/jwt-private.pem, services/payment-service/src/test/resources/test-keys/jwt-public.pem</files>
  <read_first>
    - services/identity-service/src/test/java/com/opendesk/identity/AbstractIntegrationTest.java (exact analog to copy)
    - 03-PATTERNS.md lines 698-748 (AbstractWalletIntegrationTest target shape)
    - services/identity-service/src/test/resources/test-keys/jwt-private.pem + jwt-public.pem (RSA keypair to copy verbatim so resource-server JWT decode works in ITs)
    - 02-01-SUMMARY.md decision: RSA keys copied per-module because modules have no test-source dependency
  </read_first>
  <action>
    Create AbstractWalletIntegrationTest (package com.opendesk.wallet) and AbstractPaymentIntegrationTest (package com.opendesk.payment) as copies of identity AbstractIntegrationTest: @SpringBootTest(RANDOM_PORT), @ActiveProfiles({"stub","test"}), static PostgreSQLContainer("postgres:16") with @ServiceConnection (db name wallet_test / payment_test), GenericContainer("redis:7") exposing 6379, RabbitMQContainer("rabbitmq:3-management"), all started in a static block, with @DynamicPropertySource wiring redis host/port and rabbitmq host/port/username/password.
    Copy the identity RSA test keypair PEM files verbatim into each module's src/test/resources/test-keys/ (jwt-private.pem, jwt-public.pem) so the shared-security NimbusJwtDecoder resolves a public key in ITs.
    Create application-test.yml for each: spring.jpa.hibernate.ddl-auto=create-drop OR flyway-driven (mirror identity), profiles stub+test, public-key-location classpath:test-keys/jwt-public.pem, short stub delays. Use jakarta.* only.
  </action>
  <verify>
    <automated>./gradlew :services:wallet-service:compileTestJava :services:payment-service:compileTestJava --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - Both Abstract bases contain literal `postgres:16` and `rabbitmq:3-management`
    - Both bases annotated `@ActiveProfiles({"stub", "test"})`
    - jwt-public.pem exists under each module's src/test/resources/test-keys/
    - `compileTestJava` BUILD SUCCESSFUL for both modules
    - No `javax.` import
  </acceptance_criteria>
  <done>Both services have a working Testcontainers IT base with PG16 + Redis 7 + RabbitMQ and a resolvable JWT public key.</done>
</task>

<task type="auto">
  <name>Task 3: Placeholder ITs — one named target per Phase 3 requirement</name>
  <files>services/wallet-service/src/test/java/com/opendesk/wallet/BalanceIT.java, services/wallet-service/src/test/java/com/opendesk/wallet/TransactionHistoryIT.java, services/wallet-service/src/test/java/com/opendesk/wallet/CreditReservationIT.java, services/wallet-service/src/test/java/com/opendesk/wallet/LowCreditAlertIT.java, services/wallet-service/src/test/java/com/opendesk/wallet/ExpiryWarningIT.java, services/wallet-service/src/test/java/com/opendesk/wallet/CreditLotExpiryTest.java, services/wallet-service/src/test/java/com/opendesk/wallet/UserVerifiedConsumerIT.java, services/payment-service/src/test/java/com/opendesk/payment/BundleCatalogIT.java, services/payment-service/src/test/java/com/opendesk/payment/PaymentInitiationIT.java, services/payment-service/src/test/java/com/opendesk/payment/PaymentTimeoutIT.java, services/payment-service/src/test/java/com/opendesk/payment/CallbackProcessingIT.java, services/payment-service/src/test/java/com/opendesk/payment/PaymentHistoryIT.java, services/payment-service/src/test/java/com/opendesk/payment/RefundIT.java, services/payment-service/src/test/java/com/opendesk/payment/ReconciliationIT.java</files>
  <read_first>
    - 02-01-SUMMARY.md lines 108-125 (placeholder IT convention: plain JUnit, no @SpringBootTest, Assumptions.abort)
    - 03-RESEARCH.md "Phase Requirements → Test Map" (file name → requirement → behavior mapping; authoritative source for which test covers which REQ)
    - 03-VALIDATION.md Per-Task Verification Map (to be populated from these file names)
  </read_first>
  <action>
    Create 14 placeholder test classes as plain JUnit 5 tests (NO @SpringBootTest, do NOT extend the Abstract base). Each has a single @Test method named per the behavior in the RESEARCH Test Map, whose body calls `org.junit.jupiter.api.Assumptions.abort("placeholder — implemented in 03-NN")`. Map each to its requirement(s) in a class-level comment:
      wallet: BalanceIT(WLET-01), TransactionHistoryIT(WLET-02), CreditReservationIT(WLET-03), LowCreditAlertIT(WLET-04), ExpiryWarningIT(WLET-05), CreditLotExpiryTest(WLET-06+WLET-07), UserVerifiedConsumerIT(cross — UserVerified→50 bonus credits).
      payment: BundleCatalogIT(PYMT-01), PaymentInitiationIT(PYMT-02), PaymentTimeoutIT(PYMT-03+PYMT-07), CallbackProcessingIT(PYMT-04+PYMT-06), PaymentHistoryIT(PYMT-05), RefundIT(PYMT-08), ReconciliationIT(cross — EXPIRED late-success).
    These give every later plan a named test target to convert RED→GREEN. Then populate the 03-VALIDATION.md Per-Task Verification Map: one row per requirement with the file name, the `./gradlew ... --tests "ClassName"` command, and Status ⬜ pending; set nyquist_compliant frontmatter rationale note (leave false until Wave 0 verified green-skipped). Use jakarta.* only.
  </action>
  <verify>
    <automated>./gradlew :services:wallet-service:test :services:payment-service:test --no-daemon 2>&1 | grep -v '^#' | grep -Eic 'BUILD SUCCESSFUL|skipped|SUCCESS'</automated>
  </verify>
  <acceptance_criteria>
    - 14 test classes exist with the exact names listed
    - Each placeholder calls `Assumptions.abort` and contains no `@SpringBootTest`
    - `./gradlew :services:wallet-service:test :services:payment-service:test` BUILD SUCCESSFUL with all 14 reporting skipped (not failed)
    - 03-VALIDATION.md Per-Task Verification Map has a row for every one of WLET-01..07 and PYMT-01..08 (15 IDs) with a non-empty Automated Command
    - No `javax.` import
  </acceptance_criteria>
  <done>14 named placeholder ITs run as skipped; the validation map is non-empty and covers all 15 requirement IDs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→wallet/payment API | Untrusted JWT-bearing requests cross here (validated by shared-security) |
| build → dependency registries | npm/maven supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | RSA test keypair in src/test/resources | accept | Test-only keys, excluded from prod artifacts; prod keys via K8s Secret (INFR-05) — same disposition as T-02-01 |
| T-03-SC | Tampering | maven dependency installs | mitigate | All deps are Spring BOM / Maven Central, already vetted in Phase 2 (03-RESEARCH Package Legitimacy Audit — no [SUS]/[SLOP]); no new third-party registries |
</threat_model>

<verification>
- `./gradlew :services:wallet-service:test :services:payment-service:test` BUILD SUCCESSFUL
- All 14 placeholder ITs report skipped, none failed
- 03-VALIDATION.md verification map non-empty for all 15 requirement IDs
</verification>

<success_criteria>
Both modules compile and test-compile with the full Phase 3 dependency set; Testcontainers bases exist mirroring identity-service; every Phase 3 requirement has a named runnable placeholder IT.
</success_criteria>

<output>
Create `.planning/phases/03-wallet-payments/03-01-SUMMARY.md` when done
</output>
