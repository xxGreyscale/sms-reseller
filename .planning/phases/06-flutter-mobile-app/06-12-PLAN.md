---
phase: 06-flutter-mobile-app
plan: 12
type: execute
wave: 6
depends_on: ["06-05", "06-06", "06-07", "06-08", "06-09", "06-10", "06-11"]
files_modified:
  - apps/customer-app/integration_test/app_test.dart
  - apps/customer-app/android/app/build.gradle
  - apps/customer-app/android/key.properties.template
  - apps/customer-app/ios/Runner/Info.plist
  - apps/customer-app/store/metadata/android/en-US/full_description.txt
  - apps/customer-app/store/metadata/android/sw/full_description.txt
  - apps/customer-app/store/metadata/ios/en-US/description.txt
  - apps/customer-app/store/metadata/ios/sw/description.txt
  - apps/customer-app/store/PRIVACY_POLICY.md
  - apps/customer-app/store/SUBMISSION_CHECKLIST.md
  - .github/workflows/customer-app.yml
autonomous: false
user_setup:
  - service: google-play
    why: "Android AAB submission (D-02/D-03)"
    env_vars:
      - name: ANDROID_KEYSTORE_BASE64
        source: "Locally generated upload keystore (key.jks), base64-encoded, stored as a GitHub secret"
      - name: ANDROID_KEY_PROPERTIES
        source: "storePassword/keyPassword/keyAlias for the keystore"
    dashboard_config:
      - task: "Create app + complete store listing (icon 512x512, feature graphic 1024x500, ≥2 screenshots, descriptions EN+SW, privacy policy URL, IARC content rating); upload AAB and submit"
        location: "Google Play Console"
  - service: apple-app-store
    why: "iOS IPA submission (D-02/D-03)"
    env_vars:
      - name: APPLE_DISTRIBUTION_CERT
        source: "Apple Developer Program distribution certificate + provisioning profile"
    dashboard_config:
      - task: "Create app record, privacy nutrition labels (phone, email, NIN, payment data), screenshots per device size, description EN+SW, age rating; upload IPA via TestFlight then submit for review"
        location: "App Store Connect"
requirements: [MOBL-09]

must_haves:
  truths:
    - "A signed Android AAB builds via `flutter build appbundle --release`"
    - "Store metadata (icon, screenshots placeholders, EN+SW descriptions, privacy policy) is present in the repo"
    - "A CI workflow builds the app and flags macOS-runner availability for the iOS IPA build"
    - "Full Flutter test suite + end-to-end integration test pass before submission"
  artifacts:
    - path: "apps/customer-app/store/SUBMISSION_CHECKLIST.md"
      provides: "store submission steps + external prerequisites (D-02/D-03)"
      contains: "App Store Connect"
    - path: ".github/workflows/customer-app.yml"
      provides: "CI build for the customer app + iOS macOS-runner note"
      contains: "flutter"
  key_links:
    - from: ".github/workflows/customer-app.yml"
      to: "flutter build appbundle"
      via: "CI release build job"
      pattern: "build appbundle"
---

<objective>
Prepare and submit the customer app to Google Play and the Apple App Store (MOBL-09, D-02/D-03):
signing config, store metadata (icon, screenshots, EN+SW descriptions, privacy policy), an end-to-end
integration test covering the register→PENDING→VERIFIED→dashboard→purchase→send spine, a CI workflow
(with a macOS-runner note for the iOS IPA — Open Question #5), and a manual submission checkpoint.
Per D-02, the phase is "done" when signed builds are produced and submitted; external review approval
is tracked separately (D-03 prerequisite, Phase-0-style).

Purpose: MOBL-09 + success criterion 5 (live on both stores with metadata).
Output: signing/build config + metadata + privacy policy + CI + e2e test + submission checklist.
Deletes MOBL-09 placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-flutter-mobile-app/06-RESEARCH.md
@.planning/phases/06-flutter-mobile-app/06-UI-SPEC.md
@.planning/phases/06-flutter-mobile-app/06-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end integration test (the auth→purchase→send spine)</name>
  <files>apps/customer-app/integration_test/app_test.dart</files>
  <read_first>
    - RESEARCH.md § Test Framework (integration_test sdk; full suite command), § Validation Architecture (MOBL test map), Pitfall 6 (sdk not pub.dev)
    - UI-SPEC § Screen Inventory + Interaction Contracts (the route flow)
    - All Wave 1–5 screens + providers (the e2e exercises them through mock Dio overrides)
  </read_first>
  <action>
    Expand integration_test/app_test.dart into a happy-path end-to-end flow using ProviderScope overrides with mock Dio:
    onboarding → register (→PENDING) → poll /auth/me returns VERIFIED → dashboard shows balance → open bundle catalog →
    initiate purchase → poll /api/v1/payments/{id} returns CONFIRMED → balance refreshes → add a contact → compose a
    campaign with contactIds → send succeeds → campaign detail shows. Use the integration_test SDK import. Assert key
    l10n strings appear (en, and a sw spot-check). This is the cross-cutting regression that proves all waves compose.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test integration_test/app_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The e2e test passes and exercises register→PENDING→VERIFIED→dashboard→purchase(CONFIRMED)→add contact→send→detail
    - Imports package:integration_test/integration_test.dart (sdk)
  </acceptance_criteria>
  <done>End-to-end spine green.</done>
</task>

<task type="auto">
  <name>Task 2: Signing config + store metadata + privacy policy + CI workflow</name>
  <files>apps/customer-app/android/app/build.gradle, apps/customer-app/android/key.properties.template, apps/customer-app/ios/Runner/Info.plist, apps/customer-app/store/metadata/android/en-US/full_description.txt, apps/customer-app/store/metadata/android/sw/full_description.txt, apps/customer-app/store/metadata/ios/en-US/description.txt, apps/customer-app/store/metadata/ios/sw/description.txt, apps/customer-app/store/PRIVACY_POLICY.md, apps/customer-app/store/SUBMISSION_CHECKLIST.md, .github/workflows/customer-app.yml</files>
  <read_first>
    - RESEARCH.md § Store Submission Requirements (Android: appbundle, key.jks/key.properties, targetSdk 34, metadata sizes, privacy URL, IARC; iOS: build ipa on macOS, distribution cert, min iOS 12, metadata, privacy nutrition labels, TestFlight; Shared: app name, bundle id, Swahili listing), § Open Questions #5 (macOS runner), Environment Availability
    - CONTEXT D-02 (signed AAB+IPA + metadata + submission; approval external), D-03 (accounts/certs prerequisite), D-04 (Swahili store listing)
    - CLAUDE.md (Tanzania context, GitHub Actions CI patterns)
  </read_first>
  <action>
    Configure android/app/build.gradle release signing reading from key.properties (committed only as key.properties.template;
    real key.properties + key.jks are gitignored — D-03/Open Question #5), targetSdkVersion 34. Set iOS minimum deployment 12.0
    in Info.plist/project and the bundle id consistent with Android applicationId. Write store/PRIVACY_POLICY.md (declares
    collection of phone, email, NIN, payment data — required for NIDA/payment). Write EN+SW store descriptions for both stores
    (D-04). Write store/SUBMISSION_CHECKLIST.md enumerating every required asset (icon 512/1024, feature graphic, screenshots
    per form factor, IARC rating, privacy nutrition labels, TestFlight step) and the external prerequisites (Apple Developer
    Program + Google Play Console accounts, signing certs — D-03). Add .github/workflows/customer-app.yml: a Linux job running
    `flutter test` + `flutter build appbundle --release`, and an iOS job `runs-on: macos-latest` running `flutter build ipa`
    guarded/flagged per Open Question #5 (note in the workflow if the macOS runner / Apple account is not yet provisioned).
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test && flutter build appbundle --release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Full `flutter test` green and `flutter build appbundle --release` produces an .aab
    - EN+SW store descriptions and PRIVACY_POLICY.md exist
    - CI workflow contains a `flutter build appbundle` job and a macOS iOS job (or a documented flag if unavailable)
    - key.properties/key.jks are gitignored (only the template is committed)
  </acceptance_criteria>
  <done>Signed AAB builds; metadata + privacy + CI in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Submit to Google Play + Apple App Store (manual checkpoint)</name>
  <action>Submit the signed builds to both stores per the steps below; external review approval is tracked separately (D-02).</action>
  <what-built>Signed Android AAB (and iOS IPA where a macOS runner + Apple Developer account exist), full store metadata (icon, screenshots, EN+SW descriptions, privacy policy), and a submission checklist. All automated tests + the e2e spine are green and the AAB builds.</what-built>
  <how-to-verify>
    1. Confirm `flutter build appbundle --release` produced `build/app/outputs/bundle/release/app-release.aab`.
    2. Provision (if not already — D-03) the Google Play Console + Apple Developer Program accounts and signing keys/certs.
    3. In Google Play Console: create the app, upload the AAB, complete the listing (icon 512x512, feature graphic 1024x500, ≥2 screenshots, EN+SW descriptions, privacy policy URL, IARC content rating), and submit for review.
    4. On macOS: `flutter build ipa --release`; in App Store Connect create the app record, add privacy nutrition labels (phone, email, NIN, payment data), screenshots per device size, EN+SW description, age rating; upload via TestFlight; submit for review.
    5. Confirm submission receipt emails from both stores.
    NOTE (D-02): "live/approved" is gated by external review and is tracked separately — this phase is complete once both builds are SUBMITTED.
  </how-to-verify>
  <resume-signal>Type "submitted" once both stores have accepted the submission (receipt emails received), or describe any blocker (e.g., account/cert not yet provisioned per D-03).</resume-signal>
</task>

</tasks>

<verification>
- `cd apps/customer-app && flutter test && flutter test integration_test/` → full suite + e2e green
- `flutter build appbundle --release` → AAB produced
- Manual checkpoint: both stores submitted (or D-03 prerequisite blocker recorded)
</verification>

<success_criteria>
Signed AAB (and IPA where macOS/Apple account available) build successfully; complete EN+SW store
metadata + privacy policy present; CI builds the app; app submitted to Google Play + App Store
(approval tracked externally per D-02/D-03). MOBL-09 covered.
</success_criteria>

<output>
Create `.planning/phases/06-flutter-mobile-app/06-12-SUMMARY.md` when done.
</output>
