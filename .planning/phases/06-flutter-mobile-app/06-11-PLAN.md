---
phase: 06-flutter-mobile-app
plan: 11
type: tdd
wave: 4
depends_on: ["06-01", "06-07"]
files_modified:
  - apps/customer-app/lib/features/notifications/notification_feed_screen.dart
  - apps/customer-app/lib/features/notifications/notification_provider.dart
  - apps/customer-app/lib/features/notifications/notification_api.dart
  - apps/customer-app/lib/shared/widgets/notification_badge.dart
  - apps/customer-app/test/features/notifications/notification_feed_test.dart
autonomous: true
requirements: [MOBL-04]

must_haves:
  truths:
    - "The feed polls GET /api/v1/notifications every 30 seconds"
    - "The unread badge count is derived client-side as count(read == false)"
    - "Each notification type maps to its UI-SPEC icon; unread rows use the highlighted tileColor"
    - "The poller timer is cancelled on dispose (no leak)"
  artifacts:
    - path: "apps/customer-app/lib/features/notifications/notification_provider.dart"
      provides: "30s polling feed provider with client-derived unread count"
      contains: "Timer.periodic"
    - path: "apps/customer-app/lib/shared/widgets/notification_badge.dart"
      provides: "Material 3 Badge over the bell icon"
      contains: "Badge"
  key_links:
    - from: "notification_provider.dart"
      to: "GET /api/v1/notifications"
      via: "30s poll; unread = items.where(!read).length"
      pattern: "notifications"
---

<objective>
Build the in-app notification feed and unread badge (D-01 polling, supports MOBL-04 dashboard bell):
a 30-second polling provider over GET /api/v1/notifications, a client-derived unread badge, and the
feed screen with type→icon mapping and unread highlighting. No FCM (D-01); mark-read is client-side
derived (D-14 PATCH optional and not required by the app per UI-SPEC).

Purpose: completes the dashboard bell/badge referenced in 06-07 and the Notifications nav destination.
Output: feed screen + provider + api + NotificationBadge + 1 test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-flutter-mobile-app/06-RESEARCH.md
@.planning/phases/06-flutter-mobile-app/06-UI-SPEC.md
@.planning/phases/06-flutter-mobile-app/06-PATTERNS.md
@.planning/phases/05-notifications-admin-analytics/05-06-SUMMARY.md

<interfaces>
GET /api/v1/notifications?page=0&size=20 → Page<{id, type, message, read, createdAt}>
  type ∈ {NIDA_VERIFIED, PAYMENT_CONFIRMED, LOW_CREDIT, CREDIT_EXPIRY, CAMPAIGN_COMPLETED, SENDER_ID_DECISION}
06-07: dashboard app bar mounts NotificationBadge reading this provider; shared widgets.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: 30s polling feed + client-derived unread badge + type→icon feed screen — RED→GREEN</name>
  <files>apps/customer-app/lib/features/notifications/notification_api.dart, apps/customer-app/lib/features/notifications/notification_provider.dart, apps/customer-app/lib/shared/widgets/notification_badge.dart, apps/customer-app/lib/features/notifications/notification_feed_screen.dart, apps/customer-app/test/features/notifications/notification_feed_test.dart</files>
  <read_first>
    - UI-SPEC § Notification Feed (AppBar; ListView ListTile icon+message+bodySmall timestamp; unread tileColor primaryContainer; read white; on open mark visible read client-side; notificationsEmptyHeading; full type→icon map with colors), § Interaction Contracts → Notification Feed Polling (30s; unread = items.where(!read).length; badge clears on screen open), § Component Inventory (NotificationBadge)
    - RESEARCH.md § Architecture Diagram (NotificationFeedProvider), Anti-Patterns (Timer w/o onDispose), § Widget Test with mock Dio
    - CONTEXT D-01 (polling not FCM), D-14 (mark-read optional; badge client-derived)
    - 06-07 dashboard bell mount point
  </read_first>
  <behavior>
    - Test 1: provider polls GET /api/v1/notifications every 30s (advance test clock and assert a second fetch)
    - Test 2: unread badge count equals count(read == false) from the fetched items
    - Test 3: feed renders each notification with its type-mapped icon; unread rows use the highlighted tileColor; empty → notificationsEmptyHeading
    - Test 4: poller timer cancelled on dispose (no fetch after disposal)
  </behavior>
  <action>
    Write notification_feed_test.dart FIRST (mock Dio paged feed + controllable clock). Confirm RED. Build notification_api.dart
    (GET feed). notification_provider.dart: a @riverpod notifier with Timer.periodic(30s) fetching the feed, exposing items +
    unreadCount (items.where((n)=>!n.read).length), cancel in ref.onDispose. notification_badge.dart: Material 3 Badge over the
    bell icon showing unreadCount (hidden when 0). notification_feed_screen.dart per UI-SPEC: ListView of ListTiles with the
    type→icon map + colors, unread tileColor primaryContainer, on open mark visible items read locally, empty state. Confirm
    GREEN. (No MOBL placeholder to remove here — feed supports MOBL-04's bell; MOBL-04 placeholder is removed by 06-07.)
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test test/features/notifications/notification_feed_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All 4 behaviors pass
    - Poll interval is 30s (grep: Duration(seconds: 30)); timer cancelled in ref.onDispose
    - Unread count derived client-side (grep: where(...!...read))
    - All 6 notification types have an icon mapping
  </acceptance_criteria>
  <done>Polling feed + unread badge + typed feed screen green.</done>
</task>

</tasks>

<verification>
`cd apps/customer-app && flutter test test/features/notifications/` → green.
</verification>

<success_criteria>
30-second polling notification feed with client-derived unread badge, type→icon mapping, and unread
highlighting (D-01). EN+SW.
</success_criteria>

<output>
Create `.planning/phases/06-flutter-mobile-app/06-11-SUMMARY.md` when done.
</output>
