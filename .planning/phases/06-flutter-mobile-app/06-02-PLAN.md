---
phase: 06-flutter-mobile-app
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - services/payment-service/src/main/java/com/opendesk/payment/payment/PaymentController.java
  - services/payment-service/src/main/java/com/opendesk/payment/payment/PaymentService.java
  - services/payment-service/src/main/java/com/opendesk/payment/payment/PaymentRepository.java
  - services/payment-service/src/test/java/com/opendesk/payment/payment/PaymentByIdIT.java
autonomous: true
requirements: [MOBL-05]

must_haves:
  truths:
    - "GET /api/v1/payments/{id} returns the caller's payment with its status string"
    - "GET /api/v1/payments/{id} returns 404 for a payment owned by a different user (IDOR guard)"
  artifacts:
    - path: "services/payment-service/src/main/java/com/opendesk/payment/payment/PaymentController.java"
      provides: "single-payment status endpoint for STK countdown polling"
      contains: "@GetMapping(\"/{id}\")"
    - path: "services/payment-service/src/test/java/com/opendesk/payment/payment/PaymentByIdIT.java"
      provides: "Testcontainers IT proving owner-scoped 200 + cross-user 404"
      contains: "findByIdAndUser"
  key_links:
    - from: "PaymentController.getById"
      to: "PaymentService.findByIdAndUser"
      via: "(id, userId) compound lookup from JWT subject"
      pattern: "findByIdAndUser"
---

<objective>
Close backend gap D-11: add GET /api/v1/payments/{id} to payment-service so the Flutter 2-minute
STK countdown screen can poll a single payment's status (PENDING → CONFIRMED/EXPIRED) without
scanning the paginated list. Owner-scoped via the JWT subject (IDOR guard); 404 if not owned.

Purpose: 06-09 (STK purchase screen) polls this endpoint every 5 seconds during the countdown.
Output: One new GET handler, one service method, one repository method, one Testcontainers IT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-flutter-mobile-app/06-PATTERNS.md
@.planning/phases/03-wallet-payments/03-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: GET /api/v1/payments/{id} owner-scoped status endpoint — RED→GREEN</name>
  <files>services/payment-service/src/test/java/com/opendesk/payment/payment/PaymentByIdIT.java, services/payment-service/src/main/java/com/opendesk/payment/payment/PaymentRepository.java, services/payment-service/src/main/java/com/opendesk/payment/payment/PaymentService.java, services/payment-service/src/main/java/com/opendesk/payment/payment/PaymentController.java</files>
  <read_first>
    - 06-PATTERNS.md § D-11 (exact PaymentController GET list analog lines 63–71, IDOR line 52, the new @GetMapping("/{id}") to copy, service findByIdAndUser, repository findByIdAndUserId, PaymentDto.status note)
    - 06-PATTERNS.md § Shared Patterns — Backend Additions (IDOR guard, 404 on missing-or-not-owned)
    - Existing PaymentController.java + PaymentService.java + PaymentRepository.java in payment-service (the files being extended)
    - CLAUDE.md (jakarta.* imports, IDOR userId from JWT subject, Testcontainers @ServiceConnection, no cross-service joins)
    - The existing payment-service IT base (AbstractIntegrationTest) for Testcontainers + RSA JWT fixture pattern
  </read_first>
  <behavior>
    - Test 1: authenticated user requests GET /api/v1/payments/{ownId} → 200 with body status field equal to the payment's status name ("PENDING"/"CONFIRMED"/"EXPIRED")
    - Test 2: authenticated user requests GET /api/v1/payments/{otherUsersId} → 404 (not 403/200 — never leak existence)
    - Test 3: GET /api/v1/payments/{randomUuid} → 404
  </behavior>
  <action>
    Write PaymentByIdIT.java FIRST extending the existing payment-service IT base (Testcontainers PG16 + RSA JWT fixture
    used by the existing PaymentController ITs). Persist a payment for userA, mint a JWT for userA and for userB, assert
    the three behaviors. Confirm RED. Then add to PaymentRepository: `Optional<Payment> findByIdAndUserId(UUID id, UUID userId);`
    (JPA derived query). Add to PaymentService: `@Transactional(readOnly = true) Optional<Payment> findByIdAndUser(UUID id, UUID userId)`
    delegating to the repo. Add to PaymentController the @GetMapping("/{id}") exactly per 06-PATTERNS § D-11: derive
    `UUID userId = UUID.fromString(auth.getToken().getSubject());`, call findByIdAndUser, map present→ResponseEntity.ok(PaymentDto.from(p, timeoutSeconds)),
    absent→ResponseEntity.notFound().build(). No PaymentDto change (status field already present). No SecurityConfig change
    (falls under existing authenticated paths). Confirm GREEN.
  </action>
  <verify>
    <automated>cd /Users/somar/Desktop/private/open-desk && ./gradlew :services:payment-service:test --tests "com.opendesk.payment.payment.PaymentByIdIT"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 behaviors pass under Testcontainers
    - PaymentController contains `@GetMapping("/{id}")` and derives userId from `auth.getToken().getSubject()` (IDOR)
    - Cross-user request returns 404 (grep test asserts 404, not 403)
    - PaymentRepository has `findByIdAndUserId`
  </acceptance_criteria>
  <done>Single-payment owner-scoped status endpoint live and IT-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→payment-service API | untrusted JWT-bearing request crosses here; path id is attacker-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | GET /api/v1/payments/{id} | mitigate | userId from JWT subject only; repository WHERE includes user_id; 404 (not 403) on not-owned so existence never leaks |
| T-06-02-02 | Elevation of Privilege | path-supplied id | mitigate | compound (id, userId) lookup — never lookup by id alone (06-PATTERNS IDOR guard) |
</threat_model>

<verification>
`./gradlew :services:payment-service:test` → all payment ITs including PaymentByIdIT green.
</verification>

<success_criteria>
GET /api/v1/payments/{id} returns owner-scoped status with 404 IDOR protection, proven by Testcontainers IT.
</success_criteria>

<output>
Create `.planning/phases/06-flutter-mobile-app/06-02-SUMMARY.md` when done.
</output>
