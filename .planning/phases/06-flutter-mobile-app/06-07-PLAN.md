---
phase: 06-flutter-mobile-app
plan: 07
type: tdd
wave: 3
depends_on: ["06-01", "06-06"]
files_modified:
  - apps/customer-app/lib/features/dashboard/dashboard_screen.dart
  - apps/customer-app/lib/features/dashboard/balance_provider.dart
  - apps/customer-app/lib/features/dashboard/recent_campaigns_provider.dart
  - apps/customer-app/lib/shared/widgets/balance_card.dart
  - apps/customer-app/lib/shared/widgets/stale_indicator.dart
  - apps/customer-app/lib/shared/widgets/app_navigation_bar.dart
  - apps/customer-app/lib/shared/widgets/loading_overlay.dart
  - apps/customer-app/lib/shared/widgets/error_banner.dart
  - apps/customer-app/lib/shared/format/tzs_format.dart
  - apps/customer-app/test/features/dashboard/dashboard_screen_test.dart
  - apps/customer-app/test/features/dashboard/balance_cache_test.dart
autonomous: true
requirements: [MOBL-04]

must_haves:
  truths:
    - "Dashboard shows the SMS balance, up to 5 recent campaigns, and a quick-send FAB"
    - "Balance is cache-read + online-write: cached value shows immediately with a StaleIndicator, live value replaces it"
    - "When offline with a cached balance, the cached value + StaleIndicator are shown (no hard error)"
    - "NavigationBar (Dashboard/Contacts/Campaigns/Notifications) appears only when Verified"
  artifacts:
    - path: "apps/customer-app/lib/features/dashboard/balance_provider.dart"
      provides: "Hive cache-read + online-write balance"
      contains: "Hive"
    - path: "apps/customer-app/lib/shared/widgets/balance_card.dart"
      provides: "headlineLarge credit count card"
      contains: "headlineLarge"
  key_links:
    - from: "balance_provider.dart"
      to: "GET /api/v1/wallet/balance"
      via: "fetch then write Hive balance box; fallback to cache on error"
      pattern: "wallet/balance"
---

<objective>
Build the dashboard (MOBL-04): balance card, recent campaigns (≤5), quick-send FAB, the Material 3
NavigationBar shown only when Verified, and the shared cross-cutting widgets (StaleIndicator,
LoadingOverlay, ErrorBanner) + TZS/credit formatting helper. Establishes the Hive cache-read +
online-write pattern (D-05) that contacts/campaigns reuse.

Purpose: the post-verification home; proves the read-cache policy and the shared widget kit.
Output: dashboard + 2 providers + 5 shared widgets + format helper + 2 test files. Deletes MOBL-04 placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-flutter-mobile-app/06-RESEARCH.md
@.planning/phases/06-flutter-mobile-app/06-UI-SPEC.md
@.planning/phases/06-flutter-mobile-app/06-PATTERNS.md
@.planning/phases/03-wallet-payments/03-05-SUMMARY.md

<interfaces>
GET /api/v1/wallet/balance → {availableCredits: int}
GET /api/v1/campaigns?page=0&size=5 → Page<CampaignResponse> (recent)
06-01: Hive boxes balance/campaigns; AppLocalizations; AuthState.verified; app_router (NavigationBar shell).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared widget kit + TZS format + NavigationBar shell</name>
  <files>apps/customer-app/lib/shared/widgets/stale_indicator.dart, apps/customer-app/lib/shared/widgets/loading_overlay.dart, apps/customer-app/lib/shared/widgets/error_banner.dart, apps/customer-app/lib/shared/widgets/app_navigation_bar.dart, apps/customer-app/lib/shared/format/tzs_format.dart</files>
  <read_first>
    - UI-SPEC § Component Inventory (StaleIndicator, LoadingOverlay, ErrorBanner specs), § Navigation (NavigationBar 4 destinations, Verified-only, hidden routes list), § TZS Currency Formatting (NumberFormat('#,###','sw_TZ') + "TZS " prefix; credits shown as "150 credits", no TZS for credits), § Color (status colors), § Typography
    - RESEARCH.md Pitfall 7 (TZS no decimals)
    - 06-01 app_router (where the NavigationBar shell mounts), AppLocalizations
  </read_first>
  <action>
    Build stale_indicator.dart (bodySmall row: cache icon + staleDataIndicator with {time}); loading_overlay.dart
    (semi-transparent scrim + CircularProgressIndicator blocking interaction during writes); error_banner.dart
    (MaterialBanner-style: problem text + retry CTA, dismissible) accepting message + onRetry; app_navigation_bar.dart
    (Material 3 NavigationBar with 4 destinations Dashboard/Contacts/Campaigns/Notifications, labels from AppLocalizations,
    rendered only for AuthState.verified and hidden on the routes listed in UI-SPEC) — wire it into the router shell from
    06-01. tzs_format.dart: tzs(int) → "TZS #,###" via NumberFormat('#,###','sw_TZ'); credits(int) → "{n} credits"
    (localized). No fenced code in implementation beyond standard Flutter.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter analyze lib/shared --no-fatal-infos 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - tzs_format produces "TZS 5,000" (no decimals) and "150 credits"
    - NavigationBar has exactly 4 destinations and is gated on AuthState.verified
    - ErrorBanner exposes a retry callback
  </acceptance_criteria>
  <done>Shared widget kit + TZS format + Verified-only NavigationBar in place.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Dashboard + balance cache-read/online-write + recent campaigns (MOBL-04) — RED→GREEN</name>
  <files>apps/customer-app/lib/features/dashboard/balance_provider.dart, apps/customer-app/lib/features/dashboard/recent_campaigns_provider.dart, apps/customer-app/lib/shared/widgets/balance_card.dart, apps/customer-app/lib/features/dashboard/dashboard_screen.dart, apps/customer-app/test/features/dashboard/dashboard_screen_test.dart, apps/customer-app/test/features/dashboard/balance_cache_test.dart</files>
  <read_first>
    - UI-SPEC § Dashboard (CustomScrollView + SliverAppBar with BalanceCard in flexibleSpace; app bar actions notification bell + NotificationBadge + language toggle; SliverList "Recent Campaigns" header + ≤5 CampaignListTile; "View all" link; campaignsEmptyHeading; FAB.extended dashboardQuickSendFab → /campaigns/new; StaleIndicator below balance), § Interaction Contracts (read cache-first; pull-to-refresh), § Component Inventory (BalanceCard)
    - RESEARCH.md Pattern 5 (Hive cache-read + online-write FULL code), § Widget Test with mock Dio
    - 06-01 Hive boxes (balance, campaigns); 06-07 Task 1 shared widgets + tzs_format
  </read_first>
  <behavior>
    - Test 1: dashboard shows balance from API (mock availableCredits=150 → "150 credits") and up to 5 recent CampaignListTiles (mock 3 campaigns)
    - Test 2: offline with pre-seeded Hive balance box → cached value shown + StaleIndicator (mock Dio throws connectionError)
    - Test 3: no campaigns → campaignsEmptyHeading shown
    - Test 4: quick-send FAB navigates to /campaigns/new
  </behavior>
  <action>
    Write balance_cache_test.dart + dashboard_screen_test.dart FIRST (mock Dio + pre-seeded Hive box per RESEARCH Pattern 5
    + Widget Test example). Confirm RED. Then balance_provider.dart implementing the exact cache-read/online-write pattern
    (read Hive 'balance' first, fetch GET /api/v1/wallet/balance, write-through, fallback to cache on error, rethrow only
    if no cache). recent_campaigns_provider.dart: GET /api/v1/campaigns?page=0&size=5 with Hive 'campaigns' cache fallback.
    balance_card.dart: headlineLarge credit count + labelMedium "SMS Credits" + bodySmall amber expiry-warning row when
    relevant. dashboard_screen.dart per UI-SPEC: CustomScrollView, SliverAppBar(BalanceCard + bell/badge/lang actions),
    SliverList recent campaigns or empty state, FAB.extended, StaleIndicator when cached, RefreshIndicator. Confirm GREEN;
    remove MOBL-04 placeholder.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test test/features/dashboard/</automated>
  </verify>
  <acceptance_criteria>
    - All 4 behaviors pass
    - balance_provider writes through to and falls back from the Hive 'balance' box (grep: Hive box 'balance')
    - Offline path shows cached balance + StaleIndicator (no thrown error when cache exists)
    - MOBL-04 placeholder removed
  </acceptance_criteria>
  <done>Dashboard with cached balance + recent campaigns + quick-send green; MOBL-04 covered.</done>
</task>

</tasks>

<verification>
`cd apps/customer-app && flutter test test/features/dashboard/` → green; `flutter analyze` clean.
</verification>

<success_criteria>
Dashboard shows balance (cache-read/online-write), ≤5 recent campaigns, quick-send FAB, and the
Verified-only NavigationBar; offline shows cached data with StaleIndicator (MOBL-04, D-05). EN+SW.
</success_criteria>

<output>
Create `.planning/phases/06-flutter-mobile-app/06-07-SUMMARY.md` when done.
</output>
