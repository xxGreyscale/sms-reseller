---
phase: 06-flutter-mobile-app
plan: 06
type: tdd
wave: 2
depends_on: ["06-01", "06-04"]
files_modified:
  - apps/customer-app/lib/features/auth/nida_pending_screen.dart
  - apps/customer-app/lib/features/auth/pending_poller_notifier.dart
  - apps/customer-app/test/features/auth/nida_pending_test.dart
autonomous: true
requirements: [MOBL-02]

must_haves:
  truths:
    - "PENDING user is walled to /pending and cannot reach campaign/contact/dashboard write routes"
    - "The pending screen auto-polls verification status every 10 seconds"
    - "On status VERIFIED the user transitions to /dashboard and sees verifiedSuccessMessage"
    - "Transient poll errors are swallowed silently (no error surfaced)"
  artifacts:
    - path: "apps/customer-app/lib/features/auth/nida_pending_screen.dart"
      provides: "walled PENDING screen with pulsing indicator + logout link"
      contains: "pendingScreenTitle"
    - path: "apps/customer-app/lib/features/auth/pending_poller_notifier.dart"
      provides: "10s Timer.periodic poller with ref.onDispose cleanup"
      contains: "Timer.periodic"
  key_links:
    - from: "pending_poller_notifier.dart"
      to: "GET /auth/me"
      via: "non-rotating status poll (D-13)"
      pattern: "/auth/me"
    - from: "AuthNotifier (VERIFIED)"
      to: "go_router redirect"
      via: "refreshListenable fires → /dashboard"
      pattern: "refreshListenable"
---

<objective>
Build the NIDA PENDING walled state (MOBL-02, D-09): a no-back, no-bottom-nav screen that auto-polls
verification status every 10 seconds via the cheap non-rotating GET /auth/me (added in 06-04 / D-13),
transitions to /dashboard on VERIFIED, and swallows transient poll errors. go_router's three guards
(06-01) enforce the wall.

Purpose: the register→PENDING→poll→VERIFIED flow is the spine that proves auth + walled state + polling.
Output: pending screen + poller notifier + widget test. Deletes the MOBL-02 placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-flutter-mobile-app/06-RESEARCH.md
@.planning/phases/06-flutter-mobile-app/06-UI-SPEC.md
@.planning/phases/06-flutter-mobile-app/06-PATTERNS.md
@.planning/phases/02-identity-auth/02-02-SUMMARY.md

<interfaces>
GET /auth/me → {userId, status}   (06-04 / D-13 — non-rotating; poll this, NOT /auth/refresh, per CONTEXT D-13)
AuthNotifier (06-01): refreshAndCheckStatus() updates AuthState from a status read; signOut().
AppRouter guards (06-01): AuthState.pending + route != /pending → redirect /pending.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: PENDING wall + 10s auto-poll → VERIFIED transition (MOBL-02) — RED→GREEN</name>
  <files>apps/customer-app/lib/features/auth/pending_poller_notifier.dart, apps/customer-app/lib/features/auth/nida_pending_screen.dart, apps/customer-app/test/features/auth/nida_pending_test.dart</files>
  <read_first>
    - UI-SPEC § NIDA Pending (no bottom nav, no back, pulsing 80dp primaryContainer circle, pendingScreenTitle/Body, pendingStatusLabel + 14dp CircularProgressIndicator, pendingLogoutLink error-color TextButton, verifiedSuccessMessage snackbar on success), § Interaction Contracts → NIDA Pending Auto-Poll (10s; on VERIFIED redirect; swallow transient errors)
    - RESEARCH.md Pattern 4 (PendingPollerNotifier Timer.periodic + ref.onDispose), Pitfall 3 (status must come from a server read, not stale JWT claim — here via GET /auth/me), Anti-Patterns (Timer without onDispose)
    - CONTEXT D-09 (walled), D-13 (poll /auth/me, non-rotating)
    - 06-01 AuthNotifier + app_router guards; 06-04 GET /auth/me contract
  </read_first>
  <behavior>
    - Test 1: pending screen renders pendingScreenTitle + pulsing indicator + logout link (strings from AppLocalizations, sw spot-check)
    - Test 2: poller fires every 10s; advance the test clock and assert a status poll occurred (mock /auth/me)
    - Test 3: when poll returns status VERIFIED, AuthState becomes verified (drives redirect) and verifiedSuccessMessage is shown
    - Test 4: a poll throwing an error does not surface any error UI; the next tick still polls
    - Test 5 (guard): a PENDING auth state attempting to navigate to /campaigns/new redirects to /pending (reuses 06-01 guard; assert here end-to-end)
  </behavior>
  <action>
    Write nida_pending_test.dart FIRST with mock Dio for GET /auth/me and a controllable clock (pump durations). Assert the
    five behaviors. Confirm RED. Then build pending_poller_notifier.dart: a @riverpod notifier starting Timer.periodic(10s)
    that calls AuthNotifier.refreshAndCheckStatus() (which reads GET /auth/me and updates AuthState); cancel via
    ref.onDispose. Wrap poll calls in try/catch to swallow transient errors. Build nida_pending_screen.dart per UI-SPEC:
    centered Column, AnimatedContainer pulsing circle (primaryContainer, 80dp), titleMedium pendingScreenTitle, bodyMedium
    pendingScreenBody (80% width, center), bodySmall row with 14dp CircularProgressIndicator + pendingStatusLabel, bottom
    pendingLogoutLink TextButton (error foreground) calling signOut. No AppBar back, no bottom nav. On VERIFIED show
    verifiedSuccessMessage snackbar (router redirect handles navigation). Confirm GREEN; remove MOBL-02 placeholder.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test test/features/auth/nida_pending_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All 5 behaviors pass
    - Poller polls GET /auth/me (grep: '/auth/me' in poller path, NOT '/auth/refresh')
    - Timer cancelled in ref.onDispose (grep: ref.onDispose)
    - PENDING guard redirects away from /campaigns/new
    - MOBL-02 placeholder removed
  </acceptance_criteria>
  <done>PENDING wall + 10s poll + VERIFIED transition green; MOBL-02 covered.</done>
</task>

</tasks>

<verification>
`cd apps/customer-app && flutter test test/features/auth/nida_pending_test.dart` → green.
</verification>

<success_criteria>
PENDING user is walled, auto-polls /auth/me every 10s, transitions to /dashboard on VERIFIED with
the success message, and never surfaces transient poll errors (MOBL-02, D-09). EN+SW.
</success_criteria>

<output>
Create `.planning/phases/06-flutter-mobile-app/06-06-SUMMARY.md` when done.
</output>
