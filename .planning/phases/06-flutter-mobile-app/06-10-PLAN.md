---
phase: 06-flutter-mobile-app
plan: 10
type: tdd
wave: 5
depends_on: ["06-03", "06-09"]
files_modified:
  - apps/customer-app/lib/features/campaigns/composer_screen.dart
  - apps/customer-app/lib/features/campaigns/history_screen.dart
  - apps/customer-app/lib/features/campaigns/detail_screen.dart
  - apps/customer-app/lib/features/campaigns/campaign_provider.dart
  - apps/customer-app/lib/features/campaigns/campaign_api.dart
  - apps/customer-app/lib/shared/widgets/sms_char_counter.dart
  - apps/customer-app/lib/shared/widgets/campaign_list_tile.dart
  - apps/customer-app/lib/shared/widgets/campaign_status_chip.dart
  - apps/customer-app/test/features/campaigns/sms_char_counter_test.dart
  - apps/customer-app/test/features/campaigns/composer_test.dart
  - apps/customer-app/test/features/campaigns/history_detail_test.dart
autonomous: true
requirements: [MOBL-07, MOBL-08]

must_haves:
  truths:
    - "Composer picks recipients from cached contacts and sends with contactIds[] (D-12)"
    - "Char counter shows GSM-7 (160/part) vs UCS-2 (70/part) with multi-part counts, in real time"
    - "Send is online-only; insufficient credits shows errorInsufficientCredits + Buy Credits CTA"
    - "Campaign history paginates; detail shows aggregate counts + per-message status"
  artifacts:
    - path: "apps/customer-app/lib/shared/widgets/sms_char_counter.dart"
      provides: "GSM-7/UCS-2 part counter (pure Dart, no API)"
      contains: "UCS-2"
    - path: "apps/customer-app/lib/features/campaigns/composer_screen.dart"
      provides: "composer sending contactIds[]"
      contains: "contactIds"
  key_links:
    - from: "composer_screen.dart"
      to: "POST /api/v1/campaigns (contactIds[]) + /send"
      via: "create with contactIds then send (D-12)"
      pattern: "contactIds"
    - from: "detail_screen.dart"
      to: "GET /api/v1/campaigns/{id} + /messages"
      via: "aggregate counts + per-message list"
      pattern: "campaigns/"
---

<objective>
Build the campaign surface: composer (immediate send, MOBL-07) using contactIds[] targeting (06-03 /
D-12), the GSM-7/UCS-2 character counter, campaign history (MOBL-08, paginated) and campaign detail
(aggregate counts + per-message status). No scheduling (D-08). Online-only send with insufficient-credits
and network error handling.

Purpose: MOBL-07 + MOBL-08 + success criterion 4 (compose immediate-send + view history with detail).
Output: 3 screens + provider + api + 3 widgets + 3 test files. Deletes MOBL-07 and MOBL-08 placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-flutter-mobile-app/06-RESEARCH.md
@.planning/phases/06-flutter-mobile-app/06-UI-SPEC.md
@.planning/phases/06-flutter-mobile-app/06-PATTERNS.md
@.planning/phases/04-contacts-messaging/04-05-SUMMARY.md

<interfaces>
POST /api/v1/campaigns {name, senderId, body, contactIds[]} → {id, status}   (contactIds path added by 06-03 / D-12)
POST /api/v1/campaigns/{id}/send → {campaignId, recipientCount, creditsReserved}   (409/422 on insufficient credits)
GET  /api/v1/campaigns?page&size → Page<CampaignResponse>
GET  /api/v1/campaigns/{id} → CampaignResponse (aggregate counts)
GET  /api/v1/campaigns/{id}/messages → Page<{recipient, status}>
06-09: cached contacts (recipient picker source). 06-07: shared widgets, status color system.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: SMS char counter (GSM-7/UCS-2) + status chip + list tile (pure widgets) — RED→GREEN</name>
  <files>apps/customer-app/lib/shared/widgets/sms_char_counter.dart, apps/customer-app/lib/shared/widgets/campaign_status_chip.dart, apps/customer-app/lib/shared/widgets/campaign_list_tile.dart, apps/customer-app/test/features/campaigns/sms_char_counter_test.dart</files>
  <read_first>
    - RESEARCH.md Pattern 8 (SmsCounter GSM-7 charset, 160/153 GSM-7, 70/67 UCS-2), Anti-Patterns
    - UI-SPEC § Interaction Contracts → Character Counter (display formats "120/160 · 1 SMS", "⚠ 45/70 · 1 SMS (UCS-2)", multi-part, error-color UCS-2 warning, no blocking), § Component Inventory (SmsCharCounter, CampaignStatusChip, CampaignListTile), § Color status table
    - 06-07 status colors / tzs_format
  </read_first>
  <behavior>
    - Test 1: pure GSM-7 text of 120 chars → "120/160 · 1 SMS"
    - Test 2: 170 GSM-7 chars → 2 SMS (153/part multi-part math)
    - Test 3: any non-GSM char (e.g. emoji) → UCS-2 mode "X/70 · 1 SMS (UCS-2)" with warning styling; 75 UCS-2 chars → 2 parts (67/part)
    - Test 4: CampaignStatusChip maps each status to the UI-SPEC color pair
  </behavior>
  <action>
    Write sms_char_counter_test.dart FIRST asserting the counting math + UCS-2 detection + chip colors. Confirm RED. Implement
    a pure-Dart SmsCounter (GSM-7 charset table from RESEARCH Pattern 8; thresholds 160/153 and 70/67) and the SmsCharCounter
    widget rendering the UI-SPEC display string with error-color warning when UCS-2 (non-blocking). campaign_status_chip.dart
    using the status color system. campaign_list_tile.dart (name + date + recipient count + status chip). Confirm GREEN.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test test/features/campaigns/sms_char_counter_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - 4 behaviors pass; counting matches GSM-7 160/153 and UCS-2 70/67 boundaries
    - UCS-2 warning is non-blocking and error-colored
  </acceptance_criteria>
  <done>Char counter + chips + tile green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Campaign composer (contactIds send, MOBL-07) — RED→GREEN</name>
  <files>apps/customer-app/lib/features/campaigns/campaign_api.dart, apps/customer-app/lib/features/campaigns/campaign_provider.dart, apps/customer-app/lib/features/campaigns/composer_screen.dart, apps/customer-app/test/features/campaigns/composer_test.dart</files>
  <read_first>
    - UI-SPEC § Campaign Composer (recipient "Select contacts ({n})" → bottom-sheet multi-select CheckboxListTile from cached contacts; message TextFormField maxLines null minLines 5; SmsCharCounter below; UCS-2 warning inline; composerSendButton disabled until ≥1 contact AND non-empty message; on send LoadingOverlay over POST /campaigns + /send; success→detail; errorInsufficientCredits→ErrorBanner Buy Credits→/bundles; network error→Try Again)
    - RESEARCH.md § Backend API Contract (Campaigns), Pitfall 5 (must send contactIds, not empty groupIds), § Widget Test with mock Dio
    - 06-03 D-12 contactIds contract; 06-09 cached contacts; 06-10 Task1 SmsCharCounter
  </read_first>
  <behavior>
    - Test 1: composer disables send until ≥1 contact selected AND message non-empty
    - Test 2: send POSTs /api/v1/campaigns with contactIds[] (the selected contacts) then /send; success navigates to campaign detail
    - Test 3: insufficient-credits response → errorInsufficientCredits ErrorBanner with a Buy Credits action to /bundles
    - Test 4: network error on send → Try Again banner; no navigation
  </behavior>
  <action>
    Write composer_test.dart FIRST (mock cached contacts + mock Dio create/send incl. insufficient-credits + connectionError).
    Confirm RED. Build campaign_api.dart (create with contactIds[], send, list, get, messages). campaign_provider.dart for
    send orchestration. composer_screen.dart per UI-SPEC: recipient picker bottom sheet from 06-09 cached contacts, message
    field + SmsCharCounter + UCS-2 inline warning, composerSendButton gated, LoadingOverlay during create+send, success→
    /campaigns/:id, errorInsufficientCredits→ErrorBanner with Buy Credits→/bundles, network error→Try Again. The create
    payload MUST use contactIds (Pitfall 5 — never empty groupIds). Confirm GREEN; remove MOBL-07 placeholder.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test test/features/campaigns/composer_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - 4 behaviors pass
    - Create payload includes contactIds[] (grep: contactIds in the create request), not empty groupIds
    - Insufficient-credits path offers Buy Credits → /bundles
    - MOBL-07 placeholder removed
  </acceptance_criteria>
  <done>Composer immediate-send via contactIds green; MOBL-07 covered.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Campaign history + detail (MOBL-08) — RED→GREEN</name>
  <files>apps/customer-app/lib/features/campaigns/history_screen.dart, apps/customer-app/lib/features/campaigns/detail_screen.dart, apps/customer-app/test/features/campaigns/history_detail_test.dart</files>
  <read_first>
    - UI-SPEC § Campaign History (CustomScrollView + SliverAppBar; paginated CampaignListTile; infinite scroll within 200dp of bottom; campaignsEmptyHeading; RefreshIndicator), § Campaign Detail (AppBar campaign name ellipsis; summary card 3 stat tiles total/delivered/failed; CampaignStatusChip; "Message Delivery Status" header; paginated per-message rows phone + chip; failed rows red-50 tint)
    - RESEARCH.md § Backend API Contract (Campaigns list/detail/messages), § Widget Test with mock Dio
    - 06-10 Task1 CampaignListTile/StatusChip; 06-07 RefreshIndicator pattern
  </read_first>
  <behavior>
    - Test 1: history renders a page of campaigns; scrolling near bottom loads the next page
    - Test 2: empty history → campaignsEmptyHeading
    - Test 3: detail shows aggregate total/delivered/failed stat tiles + overall status chip + per-message rows (failed row tinted)
  </behavior>
  <action>
    Write history_detail_test.dart FIRST (mock GET /campaigns paged + GET /campaigns/{id} + /messages). Confirm RED. Build
    history_screen.dart (SliverAppBar + paginated SliverList of CampaignListTile, infinite scroll, empty state, RefreshIndicator)
    and detail_screen.dart (3 stat tiles, status chip, per-message paginated list with failed-row red-50 tint). Confirm GREEN;
    remove MOBL-08 placeholder.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test test/features/campaigns/history_detail_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - 3 behaviors pass; pagination loads additional pages
    - Detail shows aggregate counts + per-message status; failed rows tinted
    - MOBL-08 placeholder removed
  </acceptance_criteria>
  <done>History + detail green; MOBL-08 covered.</done>
</task>

</tasks>

<verification>
`cd apps/customer-app && flutter test test/features/campaigns/` → green.
</verification>

<success_criteria>
Composer sends immediate campaigns via contactIds[] with live GSM-7/UCS-2 counting and credit/error
handling (MOBL-07); history + detail show paginated campaigns and per-message status (MOBL-08). EN+SW.
</success_criteria>

<output>
Create `.planning/phases/06-flutter-mobile-app/06-10-SUMMARY.md` when done.
</output>
