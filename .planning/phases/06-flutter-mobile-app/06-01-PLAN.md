---
phase: 06-flutter-mobile-app
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/customer-app/pubspec.yaml
  - apps/customer-app/l10n.yaml
  - apps/customer-app/analysis_options.yaml
  - apps/customer-app/build.yaml
  - apps/customer-app/lib/l10n/app_en.arb
  - apps/customer-app/lib/l10n/app_sw.arb
  - apps/customer-app/lib/main.dart
  - apps/customer-app/lib/core/theme/app_theme.dart
  - apps/customer-app/lib/core/dio/dio_client.dart
  - apps/customer-app/lib/core/dio/auth_interceptor.dart
  - apps/customer-app/lib/core/storage/secure_storage.dart
  - apps/customer-app/lib/core/hive/hive_boxes.dart
  - apps/customer-app/lib/core/auth/auth_state.dart
  - apps/customer-app/lib/core/auth/auth_notifier.dart
  - apps/customer-app/lib/core/router/app_router.dart
  - apps/customer-app/lib/core/router/go_router_refresh_stream.dart
  - apps/customer-app/lib/core/locale/locale_notifier.dart
  - apps/customer-app/test/core/dio/auth_interceptor_test.dart
  - apps/customer-app/test/core/router/redirect_guard_test.dart
  - apps/customer-app/test/placeholder_mobl_test.dart
  - apps/customer-app/integration_test/app_test.dart
autonomous: true
requirements: [MOBL-01, MOBL-02, MOBL-03, MOBL-04, MOBL-05, MOBL-06, MOBL-07, MOBL-08, MOBL-09]

must_haves:
  truths:
    - "`flutter test` runs and discovers tests in test/ and integration_test/"
    - "Dio AuthInterceptor attaches Bearer token and refreshes once on 401 via a separate token Dio"
    - "go_router redirect sends unauthenticated→/login, pending→/pending, verified-on-auth-route→/dashboard"
    - "Every user-facing string resolves from app_en.arb and app_sw.arb (EN+SW)"
    - "JWT access+refresh tokens persist in flutter_secure_storage; Hive holds the read-cache boxes only"
  artifacts:
    - path: "apps/customer-app/lib/core/dio/auth_interceptor.dart"
      provides: "QueuedInterceptor JWT attach + refresh-on-401"
      contains: "QueuedInterceptor"
    - path: "apps/customer-app/lib/core/router/app_router.dart"
      provides: "go_router with three redirect guards + refreshListenable"
      contains: "redirect"
    - path: "apps/customer-app/lib/l10n/app_sw.arb"
      provides: "Swahili translations (D-04)"
      contains: "@@locale"
    - path: "apps/customer-app/test/placeholder_mobl_test.dart"
      provides: "one failing placeholder per MOBL requirement (Nyquist non-empty map)"
      contains: "MOBL-01"
  key_links:
    - from: "lib/core/dio/auth_interceptor.dart"
      to: "flutter_secure_storage"
      via: "read/write access_token + refresh_token"
      pattern: "secureStorage"
    - from: "lib/core/router/app_router.dart"
      to: "authNotifierProvider"
      via: "GoRouterRefreshStream wrapping auth stream"
      pattern: "refreshListenable"
---

<objective>
Establish the Flutter customer app scaffold and all cross-cutting infrastructure every later
wave depends on: project + pubspec (locked stack versions), l10n harness (EN+SW ARB), Material 3
theme tokens, Hive read-cache boxes, flutter_secure_storage for tokens, the Dio QueuedInterceptor
auth pipeline, the Riverpod AuthNotifier + Freezed AuthState, go_router with three redirect guards,
the in-app locale toggle, and the test harness (flutter_test + integration_test + mock Dio) with
one failing placeholder per MOBL requirement so the validation map is non-empty.

Purpose: Nothing in Waves 2+ can be built until the interceptor, router, l10n, and auth state exist.
This is the contract every feature screen builds against.
Output: A compiling Flutter project at apps/customer-app with green interceptor + router unit tests
and a deliberately-red placeholder test enumerating MOBL-01..09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-flutter-mobile-app/06-CONTEXT.md
@.planning/phases/06-flutter-mobile-app/06-RESEARCH.md
@.planning/phases/06-flutter-mobile-app/06-UI-SPEC.md
@.planning/phases/06-flutter-mobile-app/06-PATTERNS.md

<interfaces>
<!-- Backend auth contract this scaffold codes against (Phase 2). -->
POST /auth/login  → {accessToken, refreshToken, status}   (status: "PENDING_VERIFICATION" | "VERIFIED")
POST /auth/refresh {refreshToken} → {accessToken, refreshToken, status}   (refresh token ROTATES every call)
POST /auth/register {fullName,email,phone,nin,password} → {userId, status:"PENDING_VERIFICATION", accessToken}
GET  /auth/me  → {userId, status}   (added by 06-04 / D-13 — non-rotating status read)
All other reads require Authorization: Bearer <accessToken>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flutter project + locked-stack pubspec + l10n + theme + Hive/secure-storage wiring</name>
  <files>apps/customer-app/pubspec.yaml, apps/customer-app/l10n.yaml, apps/customer-app/analysis_options.yaml, apps/customer-app/build.yaml, apps/customer-app/lib/l10n/app_en.arb, apps/customer-app/lib/l10n/app_sw.arb, apps/customer-app/lib/main.dart, apps/customer-app/lib/core/theme/app_theme.dart, apps/customer-app/lib/core/storage/secure_storage.dart, apps/customer-app/lib/core/hive/hive_boxes.dart, apps/customer-app/lib/core/locale/locale_notifier.dart</files>
  <read_first>
    - RESEARCH.md § Standard Stack (exact pubspec.yaml structure + versions), § Hive CE Initialization, Pattern 6 (l10n.yaml), Pitfall 4 (hive_ce NOT hive_flutter), Pitfall 6 (integration_test from sdk)
    - UI-SPEC.md § Design System, § Spacing Scale, § Typography, § Color (ColorScheme.fromSeed(Color(0xFF1565C0))), § l10n Architecture, § Copywriting Contract (ALL ARB keys EN+SW)
    - CONTEXT D-04 (bilingual day one), D-07 (Riverpod/Dio/Hive), D-10 (hive_ce_flutter, flutter_secure_storage for tokens)
  </read_first>
  <action>
    Run `flutter create --org com.smsreseller --project-name customer_app apps/customer-app` then replace pubspec.yaml
    with the locked versions from RESEARCH.md § Standard Stack (flutter_riverpod ^3.3.2, riverpod_annotation ^4.0.3,
    dio ^5.9.2, go_router ^17.3.0, hive_ce ^2.9.0, hive_ce_flutter ^2.3.4, shared_preferences ^2.5.5,
    flutter_secure_storage ^10.3.1, freezed_annotation ^3.0.0, json_annotation ^4.9.0, intl any, flutter_localizations sdk;
    dev: build_runner ^2.15.0, riverpod_generator (match annotation), riverpod_lint, custom_lint, freezed ^3.2.5,
    json_serializable ^6.x, mocktail ^1.x, integration_test sdk). Do NOT add hive or hive_flutter (Pitfall 4).
    Create l10n.yaml (arb-dir lib/l10n, template app_en.arb, output app_localizations.dart, nullable-getter false) and
    add `flutter: generate: true` to pubspec. Populate app_en.arb and app_sw.arb with EVERY ARB key from the UI-SPEC
    Copywriting Contract (all CTAs, empty states, error states, pending screen, STK screen, destructive dialogs) plus the
    onboarding slide copy — both locales complete (D-04). app_theme.dart: ThemeData(useMaterial3: true,
    colorScheme: ColorScheme.fromSeed(seedColor: Color(0xFF1565C0))) and a TextTheme mapping the 4 roles from
    UI-SPEC Typography (bodyMedium 16, labelMedium 14, titleMedium 20/600, headlineLarge 28/700). secure_storage.dart:
    a `secureStorageProvider` exposing FlutterSecureStorage for keys access_token + refresh_token. hive_boxes.dart:
    Hive.initFlutter() + open boxes `balance` (int), `contacts`, `campaigns`, `notifications` per RESEARCH Pattern 5/init.
    locale_notifier.dart: a Riverpod notifier persisting selected_locale to shared_preferences, default device locale with
    fallback to en. main.dart: WidgetsFlutterBinding + Hive init + ProviderScope wrapping MaterialApp.router using
    AppLocalizations.delegate, supportedLocales [en, sw], locale from localeNotifier, theme from app_theme,
    routerConfig from app_router (built in Task 3). Run `dart run build_runner build --delete-conflicting-outputs`
    and `flutter gen-l10n`.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter pub get && flutter gen-l10n && flutter analyze --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `flutter pub get` resolves with hive_ce_flutter present and NO hive/hive_flutter dependency
    - `flutter gen-l10n` generates app_localizations.dart with both en and sw; every UI-SPEC ARB key present in both files
    - app_theme uses ColorScheme.fromSeed(seedColor: Color(0xFF1565C0)) and useMaterial3: true
    - `flutter analyze` reports no errors (warnings/infos acceptable)
  </acceptance_criteria>
  <done>Project compiles; l10n generated EN+SW; theme + Hive + secure-storage providers exist.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Dio QueuedInterceptor auth pipeline (JWT attach + refresh-on-401) — RED→GREEN</name>
  <files>apps/customer-app/lib/core/dio/dio_client.dart, apps/customer-app/lib/core/dio/auth_interceptor.dart, apps/customer-app/test/core/dio/auth_interceptor_test.dart</files>
  <read_first>
    - RESEARCH.md Pattern 2 (QueuedInterceptor full code — separate _tokenDio instance), Pitfall 1 (rotating refresh consumed twice on concurrent 401s), Pitfall 2 (recursion if same Dio reused), § Widget Test with mock Dio
    - CONTEXT D-10 (QueuedInterceptor mandatory; tokens in flutter_secure_storage), D-07
    - UI-SPEC § Interaction Contracts (on refresh failure → clear tokens → sign out)
  </read_first>
  <behavior>
    - Test 1: a request with a stored access token gets `Authorization: Bearer <token>` attached
    - Test 2: on 401, interceptor calls POST /auth/refresh via a SEPARATE token Dio, writes new access+refresh to secure storage, retries original request, resolves with retried response
    - Test 3: two concurrent 401s trigger exactly ONE /auth/refresh call (QueuedInterceptor serialization); the second retries with the already-refreshed token
    - Test 4: refresh failure (refresh endpoint returns error / no refresh token) clears secure storage and rejects the original error
  </behavior>
  <action>
    Write auth_interceptor_test.dart FIRST using mocktail to mock FlutterSecureStorage and the token Dio (per RESEARCH
    Widget Test example). Assert the four behaviors above; run and confirm RED. Then implement AuthInterceptor extends
    QueuedInterceptor: onRequest attaches Bearer from secureStorage; onError handles only statusCode 401, reads
    refresh_token, posts to /auth/refresh on a dedicated `_tokenDio` (NOT the main Dio — Pitfall 2), persists
    accessToken+refreshToken, retries err.requestOptions with new Bearer via handler.resolve; on failure deleteAll +
    handler.reject. dio_client.dart exposes `dioProvider` (Riverpod) building the main Dio with baseUrl from
    `--dart-define=API_BASE_URL`, plus the AuthInterceptor. Run build_runner; confirm GREEN.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test test/core/dio/auth_interceptor_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All 4 behavior tests pass
    - AuthInterceptor extends QueuedInterceptor (grep: `class AuthInterceptor extends QueuedInterceptor`)
    - Refresh uses a separate Dio instance (no recursion); grep shows a `_tokenDio` field distinct from main Dio
    - Concurrent-401 test proves a single refresh call
  </acceptance_criteria>
  <done>Interceptor unit tests green; refresh-on-401 serialized and non-recursive.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: AuthNotifier + Freezed AuthState + go_router three-guard redirect — RED→GREEN</name>
  <files>apps/customer-app/lib/core/auth/auth_state.dart, apps/customer-app/lib/core/auth/auth_notifier.dart, apps/customer-app/lib/core/router/app_router.dart, apps/customer-app/lib/core/router/go_router_refresh_stream.dart, apps/customer-app/test/core/router/redirect_guard_test.dart, apps/customer-app/test/placeholder_mobl_test.dart, apps/customer-app/integration_test/app_test.dart</files>
  <read_first>
    - RESEARCH.md Pattern 1 (Freezed sealed AuthState + AsyncNotifier), Pattern 3 (go_router redirect guards full code), Pattern 4 (PENDING poll seam), Pitfall 2/3, § Validation Architecture (Wave 0 Gaps list)
    - UI-SPEC § Interaction Contracts → go_router Navigation Guards (the three ordered rules + GoRouterRefreshStream), § Screen Inventory (all routes), § Component Inventory (NavigationBar only when Verified)
    - CONTEXT D-06 (go_router guards), D-09 (PENDING walled), D-10 (Riverpod 3 @riverpod codegen)
  </read_first>
  <behavior>
    - Test 1: AuthState.unauthenticated + target route not in [/login,/register,/onboarding,/splash] → redirect '/login'
    - Test 2: AuthState.pending + any route != '/pending' → redirect '/pending'
    - Test 3: AuthState.verified + route in [/login,/register,/onboarding] → redirect '/dashboard'
    - Test 4: AuthState.verified + '/dashboard' → no redirect (null)
  </behavior>
  <action>
    Write redirect_guard_test.dart FIRST: build the GoRouter with overridden auth state and assert the four redirect
    outcomes (mount router, attempt navigation, assert matchedLocation). Confirm RED. Then implement: auth_state.dart as
    a Freezed sealed AuthState {unauthenticated, pending(accessToken), verified(accessToken, refreshToken)}.
    auth_notifier.dart as a @riverpod AsyncNotifier reading tokens from secureStorage on build(); expose
    `refreshAndCheckStatus()` (calls POST /auth/refresh, updates state from `status`) and `signOut()`. go_router_refresh_stream.dart:
    a ChangeNotifier bridging ref.watch(authNotifierProvider) changes. app_router.dart: GoRouter with the full route table
    from UI-SPEC Screen Inventory (placeholder Scaffold bodies for screens built in later waves), the three ordered
    redirect rules from UI-SPEC, and refreshListenable: GoRouterRefreshStream(...). Create placeholder_mobl_test.dart with
    one `test('MOBL-0X placeholder', () { fail('not implemented'); })` per MOBL-01..09 (Nyquist non-empty validation map;
    each later plan deletes its row as it lands). Create integration_test/app_test.dart with a single skeleton
    `testWidgets('app boots to splash')` using `integration_test` sdk import (Pitfall 6). Run build_runner; confirm the
    guard tests GREEN and the MOBL placeholders RED-by-design.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test test/core/router/redirect_guard_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All 4 redirect_guard_test behaviors pass
    - app_router.dart contains a GoRouter with `refreshListenable` set (grep: `refreshListenable`)
    - placeholder_mobl_test.dart contains exactly 9 placeholder tests naming MOBL-01..MOBL-09
    - integration_test/app_test.dart imports `package:integration_test/integration_test.dart` (sdk variant)
  </acceptance_criteria>
  <done>Router guards green; auth state + refresh seam exist; MOBL placeholder map non-empty.</done>
</task>

</tasks>

<verification>
- `cd apps/customer-app && flutter analyze` → no errors
- `flutter test test/core/` → interceptor + router suites green
- `flutter test test/placeholder_mobl_test.dart` → 9 red placeholders (expected until later waves)
</verification>

<success_criteria>
Compiling Flutter project; EN+SW l10n generated; QueuedInterceptor refresh-on-401 and go_router
three-guard redirect proven by green unit tests; tokens in secure storage, cache in Hive;
validation map seeded with one failing test per MOBL requirement.
</success_criteria>

<output>
Create `.planning/phases/06-flutter-mobile-app/06-01-SUMMARY.md` when done.
</output>
