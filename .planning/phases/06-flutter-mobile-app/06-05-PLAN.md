---
phase: 06-flutter-mobile-app
plan: 05
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/customer-app/lib/features/onboarding/splash_screen.dart
  - apps/customer-app/lib/features/onboarding/onboarding_screen.dart
  - apps/customer-app/lib/features/auth/register_screen.dart
  - apps/customer-app/lib/features/auth/login_screen.dart
  - apps/customer-app/lib/features/auth/auth_api.dart
  - apps/customer-app/test/features/onboarding/onboarding_screen_test.dart
  - apps/customer-app/test/features/auth/register_screen_test.dart
  - apps/customer-app/test/features/auth/login_session_test.dart
autonomous: true
requirements: [MOBL-01, MOBL-03]

must_haves:
  truths:
    - "Splash auto-advances by auth state: verified→/dashboard, pending→/pending, unauth→/onboarding"
    - "Onboarding shows 3 slides and navigates to register/login"
    - "Register submits {fullName,email,phone,nin,password} and lands the user in PENDING state"
    - "Login stores access+refresh tokens in secure storage; session restores on relaunch"
    - "Wrong credentials show errorLoginFailed inline (no snackbar)"
  artifacts:
    - path: "apps/customer-app/lib/features/auth/login_screen.dart"
      provides: "login form wired to /auth/login + token persistence"
      contains: "loginSubmitButton"
    - path: "apps/customer-app/lib/features/auth/register_screen.dart"
      provides: "NIDA registration form"
      contains: "registerSubmitButton"
  key_links:
    - from: "login_screen.dart"
      to: "authNotifierProvider"
      via: "login() persists tokens + sets AuthState"
      pattern: "authNotifier"
---

<objective>
Build the unauthenticated journey: splash (auth-state routing), 3-slide onboarding, NIDA registration
(→ PENDING), and login with JWT session persistence that survives app restart. All copy from l10n
(EN+SW). This is the front of the spine that proves the auth stack established in 06-01.

Purpose: gates entry to every authenticated screen; login persistence is MOBL-03; splash+onboarding is MOBL-01.
Output: 4 screens + auth API client + 3 widget/unit test files. Deletes the MOBL-01 and MOBL-03
placeholder rows from 06-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-flutter-mobile-app/06-RESEARCH.md
@.planning/phases/06-flutter-mobile-app/06-UI-SPEC.md
@.planning/phases/06-flutter-mobile-app/06-PATTERNS.md
@.planning/phases/02-identity-auth/02-02-SUMMARY.md
@.planning/phases/02-identity-auth/02-04-SUMMARY.md

<interfaces>
POST /auth/register {fullName,email,phone,nin,password} → {userId, status:"PENDING_VERIFICATION", accessToken}
POST /auth/login {email,password,deviceId} → {accessToken, refreshToken, status}
AuthNotifier (06-01): build() reads tokens from secureStorage; expose login(...) / register(...) / signOut().
AppRouter (06-01): routes /splash /onboarding /register /login /pending /dashboard with three redirect guards.
AppLocalizations (06-01): all ARB keys from UI-SPEC Copywriting Contract.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Splash + Onboarding (MOBL-01) — RED→GREEN</name>
  <files>apps/customer-app/lib/features/onboarding/splash_screen.dart, apps/customer-app/lib/features/onboarding/onboarding_screen.dart, apps/customer-app/test/features/onboarding/onboarding_screen_test.dart</files>
  <read_first>
    - UI-SPEC § Splash Screen (full-screen primary bg, 120dp logo, headlineLarge white name, 2s auto-advance by auth state), § Onboarding (3-slide PageView, skip→/login, next, last slide onboardingGetStarted, slide copy EN+SW), § Color, § Typography
    - RESEARCH.md Pattern 3 (router), Pattern 6 (l10n)
    - 06-01 app_router.dart + AuthNotifier (auth-state read)
  </read_first>
  <behavior>
    - Test 1: onboarding renders 3 slides; swiping to the last shows the onboardingGetStarted CTA
    - Test 2: tapping Skip navigates toward /login
    - Test 3: all rendered strings resolve from AppLocalizations (no hardcoded literals) — assert key text appears under locale 'en' and (spot-check) 'sw'
  </behavior>
  <action>
    Write onboarding_screen_test.dart FIRST (pump with ProviderScope + AppLocalizations; assert slides, CTA, navigation,
    sw-locale string). Confirm RED. Then build splash_screen.dart: Scaffold(backgroundColor: colorScheme.primary), centered
    120dp logo + headlineLarge app name, a 2-second timer then route by ref auth state (verified→/dashboard,
    pending→/pending, unauthenticated→/onboarding) — cancel timer in dispose. Build onboarding_screen.dart: PageView with
    the 3 UI-SPEC slides (illustration placeholder + titleMedium + bodyMedium), dot indicators, Skip TextButton top-right
    →/login, Next FilledButton (last slide label onboardingGetStarted → /register or /login per UI-SPEC). All strings via
    AppLocalizations. Confirm GREEN; remove the MOBL-01 placeholder from placeholder_mobl_test.dart.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test test/features/onboarding/</automated>
  </verify>
  <acceptance_criteria>
    - 3 behavior tests pass
    - Splash uses colorScheme.primary background and auto-advances by auth state
    - No hardcoded UI strings in either screen (grep finds AppLocalizations usage, no literal Text("...") for copy)
    - MOBL-01 placeholder removed
  </acceptance_criteria>
  <done>Splash + onboarding green; MOBL-01 covered.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Register (NIDA) + Login with session persistence (MOBL-03) — RED→GREEN</name>
  <files>apps/customer-app/lib/features/auth/auth_api.dart, apps/customer-app/lib/features/auth/register_screen.dart, apps/customer-app/lib/features/auth/login_screen.dart, apps/customer-app/test/features/auth/register_screen_test.dart, apps/customer-app/test/features/auth/login_session_test.dart</files>
  <read_first>
    - UI-SPEC § Register (field order: Full Name, Phone 07xx hint, Email, NIN uppercase max20, Password, Confirm; OutlineInputBorder; inline errors; registerSubmitButton FilledButton; errorNinTaken inline), § Login (email+password, loginSubmitButton, Forgot password link, errorLoginFailed inline NO snackbar), § Interaction Contracts (write loading: button spinner + LoadingOverlay)
    - RESEARCH.md § Backend API Contract (Auth), § Widget Test with mock Dio, Pitfall 1
    - 06-PATTERNS.md § Pattern Assignments — Flutter (NOVEL → RESEARCH + UI-SPEC)
    - 02-04-SUMMARY.md (login response: accessToken+refreshToken+status), 06-01 AuthNotifier.login/register + secureStorage
  </read_first>
  <behavior>
    - Test 1: register submits POST /auth/register with the 6 fields; on PENDING_VERIFICATION response the user is routed to /pending
    - Test 2: login submits POST /auth/login; on success accessToken+refreshToken written to (mock) secure storage and AuthState becomes verified or pending per status
    - Test 3: session restore — with tokens pre-seeded in mock secure storage, AuthNotifier.build() yields a non-unauthenticated state (relaunch keeps user logged in)
    - Test 4: login with wrong credentials shows errorLoginFailed inline and NO snackbar
  </behavior>
  <action>
    Write register_screen_test.dart and login_session_test.dart FIRST with mock Dio (mocktail) + mock FlutterSecureStorage
    per RESEARCH Widget Test example; assert the four behaviors. Confirm RED. Then build auth_api.dart (Riverpod provider
    wrapping the Dio calls register/login). register_screen.dart: SingleChildScrollView, the 6 TextFormFields in UI-SPEC
    order with OutlineInputBorder, NIN uppercase coercion maxLength 20, password obscure + eye toggle, FilledButton
    registerSubmitButton with LoadingOverlay during POST, errorNinTaken mapping, "Log in" link. login_screen.dart: logo,
    Email+Password fields, loginSubmitButton FilledButton with loading state, Forgot password TextButton, errorLoginFailed
    inline below password (no snackbar), Register link. Both call AuthNotifier which persists tokens to secureStorage and
    drives go_router redirect. Confirm GREEN; remove the MOBL-03 placeholder.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test test/features/auth/</automated>
  </verify>
  <acceptance_criteria>
    - All 4 behaviors pass
    - Tokens persisted via secureStorage (not Hive); grep shows secureStorage writes in login flow
    - errorLoginFailed shown inline, no SnackBar in the failure path
    - Register routes to /pending on PENDING_VERIFICATION
    - MOBL-03 placeholder removed
  </acceptance_criteria>
  <done>Register + login + session restore green; MOBL-03 covered.</done>
</task>

</tasks>

<verification>
`cd apps/customer-app && flutter test test/features/onboarding/ test/features/auth/` → green.
</verification>

<success_criteria>
Splash routes by auth state; onboarding (MOBL-01) complete; registration lands PENDING; login
persists JWT and restores session across restart (MOBL-03), with inline credential errors. EN+SW.
</success_criteria>

<output>
Create `.planning/phases/06-flutter-mobile-app/06-05-SUMMARY.md` when done.
</output>
