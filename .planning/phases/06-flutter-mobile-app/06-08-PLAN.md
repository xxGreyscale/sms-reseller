---
phase: 06-flutter-mobile-app
plan: 08
type: tdd
wave: 4
depends_on: ["06-02", "06-07"]
files_modified:
  - apps/customer-app/lib/features/payments/bundle_catalog_screen.dart
  - apps/customer-app/lib/features/payments/stk_purchase_screen.dart
  - apps/customer-app/lib/features/payments/payment_provider.dart
  - apps/customer-app/lib/features/payments/payment_api.dart
  - apps/customer-app/lib/shared/widgets/bundle_card.dart
  - apps/customer-app/lib/shared/widgets/countdown_widget.dart
  - apps/customer-app/test/features/payments/bundle_catalog_test.dart
  - apps/customer-app/test/features/payments/stk_countdown_test.dart
autonomous: true
requirements: [MOBL-05]

must_haves:
  truths:
    - "Bundle catalog lists active bundles in ascending price order with TZS prices"
    - "Purchase initiates POST /api/v1/payments and seeds a countdown from the response timeoutSeconds (120)"
    - "Countdown polls GET /api/v1/payments/{id} every 5s and transitions to SUCCESS on CONFIRMED / EXPIRED on EXPIRED-or-zero"
    - "On SUCCESS the wallet balance is refreshed; on EXPIRED a Try Again CTA returns to the catalog"
  artifacts:
    - path: "apps/customer-app/lib/shared/widgets/countdown_widget.dart"
      provides: "2-min countdown ring seeded from timeoutSeconds with ref.onDispose timer cleanup"
      contains: "timeoutSeconds"
    - path: "apps/customer-app/lib/features/payments/payment_provider.dart"
      provides: "STK initiation + status polling"
      contains: "payments/"
  key_links:
    - from: "payment_provider.dart"
      to: "GET /api/v1/payments/{id}"
      via: "5s status poll during countdown (D-11)"
      pattern: "payments/"
    - from: "STK SUCCESS"
      to: "balanceProvider"
      via: "invalidate/refresh balance after CONFIRMED"
      pattern: "balance"
---

<objective>
Build bundle purchase via Azampay STK push with the 2-minute countdown UI (MOBL-05): bundle catalog,
provider/msisdn bottom sheet, STK purchase screen with a countdown ring seeded from the server's
timeoutSeconds, 5-second status polling against GET /api/v1/payments/{id} (06-02 / D-11), and the
SUCCESS / EXPIRED terminal states with balance refresh and Try-Again.

Purpose: MOBL-05 + success criterion 3 (countdown displays correctly, balance updates after confirmation).
Output: 2 screens + provider + api + 2 widgets + 2 test files. Deletes MOBL-05 placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-flutter-mobile-app/06-RESEARCH.md
@.planning/phases/06-flutter-mobile-app/06-UI-SPEC.md
@.planning/phases/06-flutter-mobile-app/06-PATTERNS.md
@.planning/phases/03-wallet-payments/03-05-SUMMARY.md

<interfaces>
GET  /api/v1/bundles → [{id, name, smsCount, priceTzs, active}]
POST /api/v1/payments {bundleId, msisdn, provider} → {id, status:"PENDING", timeoutSeconds:120}
GET  /api/v1/payments/{id} → {id, status, ...}   (06-02 / D-11; status "PENDING"|"CONFIRMED"|"EXPIRED")
06-07: balanceProvider (refresh after CONFIRMED); tzs_format; shared widgets.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Bundle catalog + purchase bottom sheet (MOBL-05a) — RED→GREEN</name>
  <files>apps/customer-app/lib/features/payments/payment_api.dart, apps/customer-app/lib/shared/widgets/bundle_card.dart, apps/customer-app/lib/features/payments/bundle_catalog_screen.dart, apps/customer-app/test/features/payments/bundle_catalog_test.dart</files>
  <read_first>
    - UI-SPEC § Bundle Catalog (AppBar title, ListView of BundleCard ascending price, recommended bundle primaryContainer highlight, BundleCard: titleMedium name + bodyMedium SMS count + headlineLarge TZS price + bundleBuyButton; tap → bottom sheet msisdn + provider SegmentedButton M-Pesa/Tigo/Airtel/Halo/AzamPesa → confirm → /bundles/purchase), § Component Inventory (BundleCard), § TZS Formatting, bundlesEmptyHeading
    - RESEARCH.md § Backend API Contract (Payments), § Widget Test with mock Dio
    - 06-07 tzs_format + shared widgets
  </read_first>
  <behavior>
    - Test 1: catalog renders all active bundles sorted ascending by priceTzs with TZS-formatted prices
    - Test 2: no active bundles → bundlesEmptyHeading
    - Test 3: tapping Buy opens the bottom sheet with msisdn field + provider SegmentedButton; confirming navigates to /bundles/purchase with the selected bundle/msisdn/provider
  </behavior>
  <action>
    Write bundle_catalog_test.dart FIRST (mock GET /bundles). Confirm RED. Build payment_api.dart (Riverpod provider for
    GET /bundles, POST /payments, GET /payments/{id}). bundle_card.dart per UI-SPEC. bundle_catalog_screen.dart: AppBar,
    ListView of BundleCards ascending by priceTzs, recommended highlight, empty state, Buy → showModalBottomSheet with
    msisdn TextFormField + provider SegmentedButton (5 Azampay providers) → Confirm navigates to /bundles/purchase passing
    args. Confirm GREEN.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test test/features/payments/bundle_catalog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - 3 behaviors pass
    - Bundles sorted ascending by priceTzs; prices TZS-formatted (no decimals)
    - Bottom sheet offers the 5 Azampay providers
  </acceptance_criteria>
  <done>Catalog + purchase entry green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: STK countdown + status polling + SUCCESS/EXPIRED (MOBL-05b) — RED→GREEN</name>
  <files>apps/customer-app/lib/features/payments/payment_provider.dart, apps/customer-app/lib/shared/widgets/countdown_widget.dart, apps/customer-app/lib/features/payments/stk_purchase_screen.dart, apps/customer-app/test/features/payments/stk_countdown_test.dart</files>
  <read_first>
    - UI-SPEC § STK Purchase (no bottom nav, back disabled during countdown; stkInstruction; CountdownWidget 80dp ring primary color seeded from timeoutSeconds + headlineLarge mm:ss; stkCountdownLabel; stkWaitingBody; EXPIRED state Icons.timer_off + stkExpiredTitle/Body + stkTryAgainButton; SUCCESS state Icons.check_circle + stkSuccessTitle/Body{smsCount} + Go to Dashboard), § Interaction Contracts → Payment Status Polling (seed from timeoutSeconds; poll GET /api/v1/payments/{id} every 5s; CONFIRMED→success+refresh balance; EXPIRED-or-0→expired, no auto-retry; Try Again→/bundles)
    - RESEARCH.md Pattern 7 (CountdownWidget full code), Anti-Patterns (Timer w/o onDispose; seed from server timeoutSeconds not hardcoded; do not auto-retry STK)
    - 06-02 GET /api/v1/payments/{id}; 06-07 balanceProvider
  </read_first>
  <behavior>
    - Test 1: on entry POST /api/v1/payments fires; countdown seeds from response timeoutSeconds (120) and decrements (advance test clock 5s → shows 1:55)
    - Test 2: status poll fires every 5s against GET /api/v1/payments/{id}; on CONFIRMED → SUCCESS state shown and balanceProvider refreshed/invalidated
    - Test 3: poll returns EXPIRED OR countdown reaches 0 → EXPIRED state with stkTryAgainButton; tapping it navigates to /bundles
    - Test 4: timer is cancelled on dispose (no poll after leaving the screen)
  </behavior>
  <action>
    Write stk_countdown_test.dart FIRST (mock POST /payments + GET /payments/{id}; controllable clock via tester.pump
    durations). Confirm RED. Build payment_provider.dart: initiate() POST /payments storing id + timeoutSeconds; pollStatus()
    GET /payments/{id}; markExpired(); on CONFIRMED invalidate balanceProvider. countdown_widget.dart per RESEARCH Pattern 7:
    Timer.periodic(1s) seeded from timeoutSeconds, ring + mm:ss, every 5s trigger pollStatus, at 0 markExpired, cancel in
    dispose/ref.onDispose. stk_purchase_screen.dart: instruction + CountdownWidget + waiting body; swap to EXPIRED or SUCCESS
    terminal layouts per UI-SPEC; back disabled while counting. Confirm GREEN; remove MOBL-05 placeholder.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test test/features/payments/stk_countdown_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All 4 behaviors pass
    - Countdown seeds from response timeoutSeconds (grep: timeoutSeconds), not a hardcoded 120 literal in the widget
    - Poll hits GET /api/v1/payments/{id} every 5s; CONFIRMED refreshes balance
    - No auto-retry on EXPIRED; timer cancelled on dispose
    - MOBL-05 placeholder removed
  </acceptance_criteria>
  <done>STK countdown + polling + terminal states green; MOBL-05 covered.</done>
</task>

</tasks>

<verification>
`cd apps/customer-app && flutter test test/features/payments/` → green.
</verification>

<success_criteria>
Bundle purchase with 2-minute server-seeded countdown, 5s status polling against /api/v1/payments/{id},
SUCCESS (balance refresh) and EXPIRED (Try Again) terminal states — no infinite spinner (MOBL-05). EN+SW.
</success_criteria>

<output>
Create `.planning/phases/06-flutter-mobile-app/06-08-SUMMARY.md` when done.
</output>
