---
phase: 06-flutter-mobile-app
plan: 09
type: tdd
wave: 4
depends_on: ["06-01", "06-07"]
files_modified:
  - apps/customer-app/lib/features/contacts/contact_list_screen.dart
  - apps/customer-app/lib/features/contacts/add_contact_screen.dart
  - apps/customer-app/lib/features/contacts/contacts_provider.dart
  - apps/customer-app/lib/features/contacts/contact_api.dart
  - apps/customer-app/lib/shared/widgets/contact_list_tile.dart
  - apps/customer-app/test/features/contacts/contact_list_test.dart
  - apps/customer-app/test/features/contacts/add_contact_test.dart
autonomous: true
requirements: [MOBL-06]

must_haves:
  truths:
    - "Contact list shows cached contacts (Hive) when offline, with a StaleIndicator"
    - "Adding a contact is online-only (POST /api/v1/contacts) and the new contact appears in list + cache"
    - "Deleting a contact requires confirmation and is destructive-styled"
    - "Add contact while offline shows errorNetworkWrite + Try Again (no silent queue, D-05)"
  artifacts:
    - path: "apps/customer-app/lib/features/contacts/contacts_provider.dart"
      provides: "Hive cache-read + online-write contacts"
      contains: "Hive"
    - path: "apps/customer-app/lib/features/contacts/add_contact_screen.dart"
      provides: "online-only add with LoadingOverlay"
      contains: "addContactSaveButton"
  key_links:
    - from: "contacts_provider.dart"
      to: "GET /api/v1/contacts"
      via: "fetch then write Hive contacts box; cache fallback"
      pattern: "contacts"
---

<objective>
Build the flat contact list with manual add (MOBL-06, D-08): searchable list with offline cache,
add-contact (online-only write with LoadingOverlay + error states), and delete with confirmation.
No groups, no CSV (deferred MOBL-V2). Reuses the Hive cache-read/online-write pattern from 06-07.

Purpose: MOBL-06; also feeds the campaign composer recipient picker (06-10) from cached contacts.
Output: 2 screens + provider + api + ContactListTile + 2 test files. Deletes MOBL-06 placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-flutter-mobile-app/06-RESEARCH.md
@.planning/phases/06-flutter-mobile-app/06-UI-SPEC.md
@.planning/phases/06-flutter-mobile-app/06-PATTERNS.md
@.planning/phases/04-contacts-messaging/04-02-SUMMARY.md

<interfaces>
GET    /api/v1/contacts → Page<{id, name, phoneE164}>
POST   /api/v1/contacts {name, phone} → {id, name, phoneE164}   (server normalizes to E.164)
DELETE /api/v1/contacts/{id} → 204
06-07: Hive contacts box; StaleIndicator, LoadingOverlay, ErrorBanner; RefreshIndicator pattern.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Contact list (cache-read) + delete confirmation (MOBL-06a) — RED→GREEN</name>
  <files>apps/customer-app/lib/features/contacts/contact_api.dart, apps/customer-app/lib/features/contacts/contacts_provider.dart, apps/customer-app/lib/shared/widgets/contact_list_tile.dart, apps/customer-app/lib/features/contacts/contact_list_screen.dart, apps/customer-app/test/features/contacts/contact_list_test.dart</files>
  <read_first>
    - UI-SPEC § Contact List (CustomScrollView + SliverAppBar pinned + SearchBar in bottom slot; SliverList ContactListTile; contactsEmptyHeading + person_add icon; FAB person_add → /contacts/add; trailing delete IconButton error-color → deleteContactTitle dialog; RefreshIndicator; StaleIndicator when offline), § Component Inventory (ContactListTile: avatar initial + name + phone + delete), § Destructive Actions (AlertDialog, error-foreground TextButton confirm)
    - RESEARCH.md Pattern 5 (Hive cache), § Widget Test with mock Dio
    - 06-07 shared widgets + cache pattern
  </read_first>
  <behavior>
    - Test 1: offline with pre-seeded Hive contacts box → cached contacts shown + StaleIndicator
    - Test 2: empty list → contactsEmptyHeading
    - Test 3: tapping delete shows the confirmation dialog; confirming calls DELETE /api/v1/contacts/{id} and removes the row
    - Test 4: search filters the visible contacts
  </behavior>
  <action>
    Write contact_list_test.dart FIRST (mock Dio + pre-seeded Hive). Confirm RED. Build contact_api.dart (list/create/delete).
    contacts_provider.dart with the Hive cache-read + online-write pattern on the 'contacts' box. contact_list_tile.dart per
    UI-SPEC. contact_list_screen.dart: SliverAppBar + SearchBar, SliverList of ContactListTile, empty state, FAB→/contacts/add,
    delete with deleteContactTitle AlertDialog (error-foreground confirm), RefreshIndicator, StaleIndicator. Confirm GREEN.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test test/features/contacts/contact_list_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - 4 behaviors pass
    - Offline shows cached contacts + StaleIndicator
    - Delete is gated by a confirmation dialog with error-styled confirm
  </acceptance_criteria>
  <done>Contact list + delete green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Add contact (online-only write) (MOBL-06b) — RED→GREEN</name>
  <files>apps/customer-app/lib/features/contacts/add_contact_screen.dart, apps/customer-app/test/features/contacts/add_contact_test.dart</files>
  <read_first>
    - UI-SPEC § Add Contact (AppBar; Full Name + Phone 07xx hint; addContactSaveButton FilledButton; online-only LoadingOverlay during POST; success pops + list refreshes; error → ErrorBanner errorNetworkWrite + Try Again), § Interaction Contracts → Offline Write Attempt (detect DioExceptionType.connectionError; do NOT queue — D-05)
    - 06-09 Task 1 contacts_provider + contact_api; 06-07 LoadingOverlay/ErrorBanner
  </read_first>
  <behavior>
    - Test 1: valid name+phone → POST /api/v1/contacts; on success pops back and the contacts provider is refreshed (new contact in list+cache)
    - Test 2: offline (Dio connectionError) → errorNetworkWrite ErrorBanner + Try Again; NO contact added to cache (no silent queue)
    - Test 3: LoadingOverlay shown during the in-flight POST
  </behavior>
  <action>
    Write add_contact_test.dart FIRST (mock Dio success + connectionError). Confirm RED. Build add_contact_screen.dart:
    Full Name + Phone TextFormFields (07xx hint), addContactSaveButton FilledButton, LoadingOverlay during POST, on success
    invalidate contactsProvider + pop, on connectionError show ErrorBanner errorNetworkWrite with retry. Do NOT write to Hive
    on failure. Confirm GREEN; remove MOBL-06 placeholder.
  </action>
  <verify>
    <automated>cd apps/customer-app && flutter test test/features/contacts/add_contact_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - 3 behaviors pass
    - Offline write shows error + retry and does NOT add to cache (grep: no Hive put in the failure branch)
    - MOBL-06 placeholder removed
  </acceptance_criteria>
  <done>Online-only add contact green; MOBL-06 covered.</done>
</task>

</tasks>

<verification>
`cd apps/customer-app && flutter test test/features/contacts/` → green.
</verification>

<success_criteria>
Flat contact list with offline cache, online-only add (with error/retry, no silent queue), and
confirmed delete (MOBL-06, D-05/D-08). EN+SW.
</success_criteria>

<output>
Create `.planning/phases/06-flutter-mobile-app/06-09-SUMMARY.md` when done.
</output>
