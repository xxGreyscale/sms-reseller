---
phase: 07-clean-architecture-local-dev-tooling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - compose.yaml
  - scripts/db-init/01-create-service-databases.sql
  - .env.example
autonomous: true
requirements: [DEVX-02, DEVX-03]
must_haves:
  truths:
    - "docker compose up -d brings Postgres 16, Redis 7, RabbitMQ 3 to healthy with one command"
    - "Per-service logical databases are created on first Postgres boot via an init script"
    - "A committed .env.example documents AZAMPAY_* and infra connection vars; the real .env stays gitignored"
  artifacts:
    - path: "compose.yaml"
      provides: "Local infra (PG16/Redis7/Rabbit3) with healthchecks + named volumes"
      contains: "healthcheck"
    - path: "scripts/db-init/01-create-service-databases.sql"
      provides: "8 per-service logical DBs"
      contains: "CREATE DATABASE"
    - path: ".env.example"
      provides: "AZAMPAY_* + infra connection var template"
      contains: "AZAMPAY_CLIENT_ID"
  key_links:
    - from: "compose.yaml"
      to: "scripts/db-init"
      via: "docker-entrypoint-initdb.d volume mount"
      pattern: "docker-entrypoint-initdb.d"
    - from: ".env.example"
      to: "compose.yaml"
      via: "POSTGRES_USER/RABBITMQ_USER env defaults shared"
      pattern: "POSTGRES_USER"
---

<objective>
Create the local-infra Docker Compose stack and the committed env template. No compose file exists in the repo today (Phase 1's intended infra was never delivered) — this plan CREATES it. Postgres 16 + Redis 7 + RabbitMQ 3 with healthchecks and named volumes, per-service logical DBs via an init script, and a `.env.example` whose defaults align with the compose stack and the already-wired `${AZAMPAY_*}` placeholders in payment-service/application.yml.

Purpose: DEVX-02 (infra one-command boot to healthy) + DEVX-03 (env template; real secret never committed). These are the inputs the start script (07-04) sources.
Output: compose.yaml, scripts/db-init/*.sql, .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-clean-architecture-local-dev-tooling/07-RESEARCH.md
@.planning/phases/07-clean-architecture-local-dev-tooling/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create compose.yaml (PG16 / Redis7 / Rabbit3) with healthchecks + named volumes</name>
  <files>compose.yaml</files>
  <read_first>
    - 07-RESEARCH.md §docker-compose template (DEVX-02) — image tags, healthcheck commands, port maps, named volumes; schema v2 (no `version:` key)
    - 07-RESEARCH.md Pitfall 4 (per-service logical DBs) and Environment Availability (Docker 28.3 / Compose v2.38 installed)
    - CLAUDE.md Data section (PostgreSQL 16, Redis 7, RabbitMQ 3) — image versions must match
  </read_first>
  <action>
    Create compose.yaml at repo root (schema v2 — no `version:` key) with three services: `postgres` (image postgres:16, env POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB from `${...:-smsreseller}` defaults, port 5432, named volume pgdata, mount ./scripts/db-init into /docker-entrypoint-initdb.d:ro, healthcheck `pg_isready -U`), `redis` (image redis:7, port 6379, named volume redisdata, healthcheck `redis-cli ping`), `rabbitmq` (image rabbitmq:3-management, env RABBITMQ_DEFAULT_USER/PASS from `${RABBITMQ_USER:-guest}`/`${RABBITMQ_PASSWORD:-guest}`, ports 5672 + 15672, named volume rabbitdata, healthcheck `rabbitmq-diagnostics -q ping`). Declare named volumes pgdata/redisdata/rabbitdata. Image tags MUST match Testcontainers/CLAUDE.md exactly.
  </action>
  <acceptance_criteria>
    - `docker compose -f compose.yaml config` exits 0 (valid schema)
    - `grep -c healthcheck compose.yaml` returns 3
    - `docker compose up -d` then `docker compose ps --format '{{.Health}}' | grep -vqE 'unhealthy|starting'` succeeds after healthchecks settle (all healthy)
    - `docker compose down` tears the stack down cleanly
  </acceptance_criteria>
  <done>Compose stack boots all three services to healthy and tears down.</done>
</task>

<task type="auto">
  <name>Task 2: Per-service database init script</name>
  <files>scripts/db-init/01-create-service-databases.sql</files>
  <read_first>
    - 07-RESEARCH.md Pitfall 4 and Open Question 2 (one logical DB per service; naming convention)
    - CLAUDE.md (one logical DB + one Flyway set per service; no cross-service schema sharing)
    - settings.gradle.kts (the 8 service module names → DB names: identity, catalog, wallet, payment, contact, messaging, notification, admin)
  </read_first>
  <action>
    Create scripts/db-init/01-create-service-databases.sql that issues `CREATE DATABASE` for each of the 8 services: identity, catalog, wallet, payment, contact, messaging, notification, admin (owned by the POSTGRES_USER). This runs once on first Postgres container init via the docker-entrypoint-initdb.d mount. Names MUST match the per-service SPRING_DATASOURCE_URL the start script (07-04) will construct (jdbc:postgresql://localhost:5432/<service>). Do not create schemas or tables — Flyway owns those per service.
  </action>
  <acceptance_criteria>
    - `grep -c 'CREATE DATABASE' scripts/db-init/01-create-service-databases.sql` returns 8
    - After `docker compose down -v && docker compose up -d`, `docker compose exec -T postgres psql -U smsreseller -tc "SELECT datname FROM pg_database"` lists all 8 service DB names
  </acceptance_criteria>
  <done>Fresh Postgres volume creates exactly the 8 per-service logical databases.</done>
</task>

<task type="auto">
  <name>Task 3: Commit .env.example (AZAMPAY_* + infra vars); confirm .env stays gitignored</name>
  <files>.env.example</files>
  <read_first>
    - 07-RESEARCH.md §.env.example pattern (DEVX-03) — exact variable list with safe sandbox defaults
    - services/payment-service/src/main/resources/application.yml lines 44-49 (AZAMPAY_BASE_URL/APP_NAME/CLIENT_ID/CLIENT_SECRET wired as `${...:placeholder}`)
    - .gitignore lines 16-18 (.env already ignored)
  </read_first>
  <action>
    Create .env.example with: infra vars consumed by both compose and services — POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB, RABBITMQ_USER/RABBITMQ_PASSWORD, SPRING_DATASOURCE_USERNAME/SPRING_DATASOURCE_PASSWORD, SPRING_DATA_REDIS_HOST/PORT, SPRING_RABBITMQ_HOST/PORT/USERNAME/PASSWORD; and Azampay sandbox placeholders AZAMPAY_BASE_URL/AZAMPAY_APP_NAME/AZAMPAY_CLIENT_ID/AZAMPAY_CLIENT_SECRET. Use simple unquoted tokens (no spaces) and placeholder/sandbox values only — NEVER a real Azampay credential. Per-service SERVER_PORT and SPRING_DATASOURCE_URL are derived in the start script loop (07-04), not enumerated here; add a header comment saying so.
  </action>
  <acceptance_criteria>
    - `test -f .env.example` succeeds
    - `grep -q AZAMPAY_CLIENT_ID .env.example && grep -q SPRING_DATASOURCE_USERNAME .env.example && grep -q POSTGRES_USER .env.example` succeeds
    - `git check-ignore .env` exits 0 (real .env is ignored); `.env.example` is NOT ignored (`git check-ignore .env.example` exits non-zero)
    - No real secret present: `grep -viE 'placeholder|sandbox|localhost|guest|smsreseller|5432|6379|5672' .env.example | grep -i 'secret=' | grep -vq placeholder` finds nothing
  </acceptance_criteria>
  <done>.env.example committed with safe defaults; .env confirmed gitignored.</done>
</task>

</tasks>

<verification>
- `docker compose config` valid; stack reaches healthy; 8 DBs created on fresh volume.
- `.env.example` documents AZAMPAY_* + infra vars with no real secret; `.env` gitignored.
</verification>

<success_criteria>
DEVX-02 infra one-command-healthy + per-service DBs, and DEVX-03 env template, both satisfied and independent of the refactor.
</success_criteria>

<output>
Create `.planning/phases/07-clean-architecture-local-dev-tooling/07-02-SUMMARY.md` when done.
</output>
