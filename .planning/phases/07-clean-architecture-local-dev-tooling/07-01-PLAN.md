---
phase: 07-clean-architecture-local-dev-tooling
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - services/payment-service/build.gradle.kts
  - services/payment-service/src/test/java/com/smsreseller/payment/PaymentArchitectureTest.java
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "An ArchUnit layered-architecture test exists in the payment-service test source set"
    - "The test is RED until the package re-layer (07-03) is complete — it defines the target layer boundaries"
  artifacts:
    - path: "services/payment-service/src/test/java/com/smsreseller/payment/PaymentArchitectureTest.java"
      provides: "Layered dependency rule + domain-purity rule (ARCH-01 enforcement)"
      contains: "layeredArchitecture"
    - path: "gradle/libs.versions.toml"
      provides: "archunit-junit5 1.4.1 coordinate"
      contains: "archunit"
  key_links:
    - from: "services/payment-service/build.gradle.kts"
      to: "libs.archunit.junit5"
      via: "testImplementation"
      pattern: "archunit"
---

<objective>
Add ArchUnit 1.4.1 to the build and author the RED-first layered-architecture test that encodes the target clean-architecture boundaries for payment-service. This is the Wave-0 enforcement scaffold for ARCH-01: the rule is written FIRST (RED), then 07-03 moves packages until it goes GREEN.

Purpose: A build-enforced dependency rule so the layer boundary cannot silently rot. Per 07-RESEARCH.md, ArchUnit runs in the existing JUnit 5 harness at zero runtime cost.
Output: archunit catalog entry + test dep + `PaymentArchitectureTest.java` (RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-clean-architecture-local-dev-tooling/07-RESEARCH.md
@.planning/phases/07-clean-architecture-local-dev-tooling/07-VALIDATION.md
@.planning/phases/07-clean-architecture-local-dev-tooling/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add archunit-junit5 to the version catalog and payment-service test deps</name>
  <read_first>
    - gradle/libs.versions.toml (existing [versions] and [libraries] sections — match formatting)
    - services/payment-service/build.gradle.kts (testImplementation block)
    - 07-RESEARCH.md §Standard Stack / Installation (archunit = "1.4.1"; module com.tngtech.archunit:archunit-junit5) and §Package Legitimacy Audit (Approved)
  </read_first>
  <action>
    In gradle/libs.versions.toml add `archunit = "1.4.1"` under [versions] and `archunit-junit5 = { module = "com.tngtech.archunit:archunit-junit5", version.ref = "archunit" }` under [libraries]. In services/payment-service/build.gradle.kts add `testImplementation(libs.archunit.junit5)` in the test dependencies block. Do not change any other dependency, plugin, or convention-plugin wiring (Lombok-before-MapStruct order is owned by the spring-boot-service convention plugin — do not disturb).
  </action>
  <acceptance_criteria>
    - `grep -q 'archunit' gradle/libs.versions.toml` succeeds
    - `grep -q 'archunit.junit5' services/payment-service/build.gradle.kts` succeeds
    - `./gradlew :services:payment-service:dependencies --configuration testRuntimeClasspath` lists `com.tngtech.archunit:archunit-junit5:1.4.1`
  </acceptance_criteria>
  <done>archunit coordinate resolvable on the payment-service test classpath.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the RED layered-architecture test</name>
  <files>services/payment-service/src/test/java/com/smsreseller/payment/PaymentArchitectureTest.java</files>
  <read_first>
    - 07-RESEARCH.md §ArchUnit rule (verified syntax pattern) and §Recommended package structure / Per-class mapping table
    - 07-RESEARCH.md Pitfall 1 (port DTOs must live in application/port, not infrastructure)
    - services/payment-service/src/main/java/com/smsreseller/payment/ (current package-by-feature tree — the layers do NOT exist yet, so this test is expected RED)
  </read_first>
  <behavior>
    - Rule `layers`: `@AnalyzeClasses(packages = "com.smsreseller.payment")` with `layeredArchitecture().consideringOnlyDependenciesInLayers()` defining Domain `..domain..`, Application `..application..`, Infrastructure `..infrastructure..`, Presentation `..presentation..`; Presentation may not be accessed by any layer; Application accessed only by Presentation+Infrastructure; Infrastructure accessed only by Presentation; Domain accessed by Application+Infrastructure+Presentation.
    - Rule `domainPurity`: `noClasses().that().resideInAPackage("..domain..").should().dependOnClassesThat().resideInAnyPackage("..application..","..infrastructure..","..presentation..")`. Deliberately does NOT forbid `jakarta.persistence.*` (JPA-entity-as-domain pragmatic decision per RESEARCH A1).
    - Expected state now: RED (no layer packages exist). Becomes GREEN after 07-03 moves classes.
  </behavior>
  <action>
    Create PaymentArchitectureTest.java in package com.smsreseller.payment using the ArchUnit `layeredArchitecture()` + `noClasses()` rules per 07-RESEARCH.md §ArchUnit rule. Use `..domain..`, `..application..`, `..infrastructure..`, `..presentation..` package globs. Do NOT add a rule forbidding Spring/Jakarta annotations in domain (out of scope for behavior-neutral MVP per RESEARCH note). This test will be RED until 07-03; that is correct and intended — do not weaken the rules to make it pass before the refactor.
  </action>
  <acceptance_criteria>
    - File exists at the stated path with `@AnalyzeClasses` and a `layeredArchitecture()` ArchRule and a `domainPurity` ArchRule
    - `./gradlew :services:payment-service:compileTestJava` exits 0 (compiles)
    - `./gradlew :services:payment-service:test --tests "*PaymentArchitectureTest"` runs and FAILS with empty-layer / unmatched-package violations (RED is the expected, documented state at this wave)
  </acceptance_criteria>
  <done>Compiling ArchUnit test present; RED until 07-03 performs the package move.</done>
</task>

</tasks>

<verification>
- `./gradlew :services:payment-service:compileTestJava` compiles with archunit on the classpath.
- The architecture test is discovered by the test runner (RED expected this wave).
</verification>

<success_criteria>
ArchUnit dependency wired; RED layered-architecture rule authored and committed; ready for 07-03 to drive it GREEN by moving packages.
</success_criteria>

<output>
Create `.planning/phases/07-clean-architecture-local-dev-tooling/07-01-SUMMARY.md` when done.
</output>
