---
phase: 07-clean-architecture-local-dev-tooling
plan: 03
type: tdd
wave: 2
depends_on: ["07-01"]
files_modified:
  - services/payment-service/src/main/java/com/smsreseller/payment/
  - services/payment-service/CLEAN-ARCHITECTURE.md
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "payment-service source is organized into domain / application / infrastructure / presentation layers"
    - "The ArchUnit layered rule (07-01) passes GREEN after the move"
    - "The existing 24-class payment-service test suite passes unchanged — behavior-neutral (no REST/DB/event/test changes)"
    - "PaymentGateway port and its StkPush/TransactionStatus DTOs live in application/port (not infrastructure)"
    - "CLEAN-ARCHITECTURE.md documents layers, the JPA-entity-as-domain compromise, the per-class mapping, and the rollout playbook"
  artifacts:
    - path: "services/payment-service/CLEAN-ARCHITECTURE.md"
      provides: "Layer boundary doc + rollout playbook for the other 7 services"
      contains: "Rollout"
    - path: "services/payment-service/src/main/java/com/smsreseller/payment/domain/payment/Payment.java"
      provides: "Payment entity relocated to domain layer"
      contains: "class Payment"
    - path: "services/payment-service/src/main/java/com/smsreseller/payment/application/port/PaymentGateway.java"
      provides: "Output port in application layer"
      contains: "interface PaymentGateway"
  key_links:
    - from: "services/payment-service/src/main/java/com/smsreseller/payment/application/port/PaymentGateway.java"
      to: "domain + application/port DTOs only"
      via: "no import of ..infrastructure.. / ..presentation.."
      pattern: "package com.smsreseller.payment.application.port"
    - from: "services/payment-service/src/test/java/com/smsreseller/payment/PaymentArchitectureTest.java"
      to: "the four layer packages"
      via: "ArchUnit GREEN after move"
      pattern: "layeredArchitecture"
---

<objective>
Re-layer payment-service from package-by-feature (payment/ bundle/ callback/ gateway/ outbox/ reconciliation/ timeout/ config/) into clean-architecture layers (domain / application / infrastructure / presentation), driving the RED ArchUnit test from 07-01 to GREEN while keeping the existing 24-class Testcontainers suite green throughout. This is a pure MOVE: no behavior, REST contract, DB schema/Flyway, or event/outbox payload change. JPA `@Entity` classes stay AS the domain model (documented pragmatic compromise). Then write CLEAN-ARCHITECTURE.md as the reference + rollout playbook.

Purpose: ARCH-01 — the canonical clean-architecture reference pattern. The existing test suite is the regression oracle and the GREEN gate; never edit a test to make a move pass.
Output: re-layered source tree, GREEN ArchUnit, green oracle, CLEAN-ARCHITECTURE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-clean-architecture-local-dev-tooling/07-RESEARCH.md
@.planning/phases/07-clean-architecture-local-dev-tooling/07-CONTEXT.md
@.planning/phases/07-clean-architecture-local-dev-tooling/07-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Authoritative per-class destination mapping is the table in 07-RESEARCH.md §Per-class mapping table (current → layer). Executor MUST follow it exactly. Summary of target packages under com.smsreseller.payment: -->
domain/payment   : Payment, PaymentStatus, BundleNotPurchasableException, PendingPaymentExistsException
domain/bundle    : SmsBundle
domain/outbox    : OutboxEntry, PaymentConfirmedEvent
application       : PaymentService, BundleAdminService, CallbackProcessor
application/port  : PaymentGateway, WebhookSignatureValidator, StkPushRequest, StkPushResult, TransactionStatusResult  (port DTOs move WITH the port — Pitfall 1)
infrastructure/persistence : PaymentRepository, BundleRepository, OutboxRepository
infrastructure/gateway     : AzampayPaymentGateway, StubPaymentGateway, AzampayTokenProvider, AzampayTransientException
infrastructure/callback    : StubSignatureValidator, AzampayCallbackPayload
infrastructure/messaging   : OutboxRelay
infrastructure/scheduling  : ReconciliationJob, PaymentTimeoutJob
infrastructure/config      : RabbitMqConfig, Resilience4jConfig, SecurityConfig
presentation               : PaymentController, PaymentDto, PurchaseRequest, BundleController, AdminBundleController, BundleDto, BundleSaveRequest, CallbackController
root (unchanged)           : PaymentServiceApplication (composition root)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move classes into the four layers + fix package declarations/imports (drive ArchUnit GREEN, keep oracle green)</name>
  <files>services/payment-service/src/main/java/com/smsreseller/payment/</files>
  <read_first>
    - 07-RESEARCH.md §Per-class mapping table (current → layer) — the authoritative destination for every one of the ~38 source files
    - 07-RESEARCH.md Pitfall 1 (StkPushRequest/StkPushResult/TransactionStatusResult MUST go to application/port with the PaymentGateway port, NOT infrastructure — single most likely ArchUnit failure)
    - 07-RESEARCH.md Pitfall 5 (`clean` build required — incremental masks stale generated sources)
    - services/payment-service/src/test/java/com/smsreseller/payment/PaymentArchitectureTest.java (the GREEN target from 07-01)
    - The current source files under services/payment-service/src/main/java/com/smsreseller/payment/ (read each before moving)
  </read_first>
  <behavior>
    - Behavior-neutral move: after the re-layer, every test in the existing 24-class suite passes UNCHANGED. No test file is edited to accommodate a move.
    - ArchUnit `PaymentArchitectureTest` flips from RED (07-01) to GREEN: domain depends on nothing outside domain; application depends only on domain (+ its own application/port DTOs); infrastructure/presentation depend inward.
    - `domainPurity` rule passes: no domain class imports application/infrastructure/presentation (jakarta.persistence.* in domain is allowed by design).
  </behavior>
  <action>
    Move each file to its destination package per the 07-RESEARCH per-class mapping table; update the `package` declaration in each moved file and every `import` that references a moved class across the whole module (including test imports that reference production classes — adjust imports ONLY, never test logic/assertions). Keep PaymentServiceApplication.java at the root as the composition root. Critically: move StkPushRequest, StkPushResult, TransactionStatusResult into `application/port` alongside PaymentGateway (per Pitfall 1) so the application port never imports infrastructure. Do NOT split JPA entities into separate persistence POJOs — Payment/SmsBundle/OutboxEntry stay as the domain model with their jakarta.persistence annotations intact (RESEARCH A1). Do NOT touch any file under src/main/resources (application.yml, Flyway V1–V4) or any DB schema. After moving, run a clean test build (not incremental).
  </action>
  <acceptance_criteria>
    - `./gradlew :services:payment-service:test --tests "*PaymentArchitectureTest"` passes GREEN (layered rule + domainPurity)
    - `./gradlew :services:payment-service:clean test` exits 0 — all 24 test classes green (the regression oracle, behavior-neutral proof)
    - `git diff --name-only -- 'services/payment-service/src/test/**/*.java' | xargs -r git diff -- | grep -E '^\+' | grep -vqE '^\+\+\+|import ' ` finds NO added non-import lines in test files (only import paths changed)
    - `git diff --quiet -- services/payment-service/src/main/resources` (no resources/schema/Flyway change)
    - No file remains in the old feature packages: `find services/payment-service/src/main/java/com/smsreseller/payment -maxdepth 1 -type d | grep -vE 'domain|application|infrastructure|presentation' | grep -vqE '/payment$'` confirms only layer dirs (plus root) exist
  </acceptance_criteria>
  <done>All classes relocated; ArchUnit GREEN; full oracle green; only import paths changed in tests; resources untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Write CLEAN-ARCHITECTURE.md (boundaries + compromise + mapping + rollout playbook)</name>
  <files>services/payment-service/CLEAN-ARCHITECTURE.md</files>
  <read_first>
    - 07-RESEARCH.md §Clean-Architecture Layering pragmatic decision (JPA-entity-as-domain), §Recommended package structure, §Per-class mapping table
    - 07-CONTEXT.md decisions (layer definitions, dependency rule, behavior-neutral mandate, rollout-playbook requirement)
    - The final re-layered tree produced by Task 1
  </read_first>
  <action>
    Write services/payment-service/CLEAN-ARCHITECTURE.md documenting: (1) the four layers and the inward dependency rule; (2) the JPA-entity-as-domain pragmatic compromise, explicitly labeled as a deliberate MVP decision with full domain/persistence separation listed as a future rollout step; (3) the per-class mapping (old feature package → new layer) as a reference table; (4) how the ArchUnit rule enforces the boundary; (5) a step-by-step rollout playbook for applying the same pattern to the remaining 7 services (identity, catalog, wallet, contact, messaging, notification, admin). Include a `## Rollout` section header.
  </action>
  <acceptance_criteria>
    - `test -f services/payment-service/CLEAN-ARCHITECTURE.md` succeeds
    - `grep -q '## Rollout' services/payment-service/CLEAN-ARCHITECTURE.md` succeeds
    - `grep -qiE 'jpa|persistence' services/payment-service/CLEAN-ARCHITECTURE.md` succeeds (documents the compromise)
    - Mentions all four layer names (domain, application, infrastructure, presentation)
  </acceptance_criteria>
  <done>Reference doc + rollout playbook committed.</done>
</task>

</tasks>

<verification>
- `./gradlew :services:payment-service:clean test` green (24 classes) AND `*PaymentArchitectureTest` GREEN.
- No change to REST contracts, Flyway/schema, or event/outbox payloads (resources diff empty; test logic unchanged).
- CLEAN-ARCHITECTURE.md present with rollout playbook.
</verification>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Source-only re-layer; no new runtime input, endpoint, or data path |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Behavior drift during package move (silent contract/schema/event change) | mitigate | Existing 24-class Testcontainers oracle must stay green (`clean test`); resources diff asserted empty; test files change imports only |
| T-07-02 | Tampering | ArchUnit rule weakened to force GREEN | mitigate | Rule authored in 07-01 before the move; rule body not edited in this plan; domainPurity asserted |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No npm/pip/cargo packages introduced this plan; archunit (Maven) approved in 07-RESEARCH §Package Legitimacy Audit |
</threat_model>

<success_criteria>
ARCH-01 satisfied: layered source, enforced inward dependency rule GREEN, behavior-neutral (oracle green), and a documented reference + rollout playbook.
</success_criteria>

<output>
Create `.planning/phases/07-clean-architecture-local-dev-tooling/07-03-SUMMARY.md` when done.
</output>
