---
phase: 07-clean-architecture-local-dev-tooling
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - services/identity-service/src/main/resources/application-dev.yml
  - services/catalog-service/src/main/resources/application.yml
  - services/wallet-service/src/main/resources/application-dev.yml
  - services/payment-service/src/main/resources/application-dev.yml
  - services/contact-service/src/main/resources/application-dev.yml
  - services/messaging-service/src/main/resources/application-dev.yml
  - services/notification-service/src/main/resources/application-dev.yml
  - services/admin-service/src/main/resources/application-dev.yml
  - services/*/src/main/resources/keys/jwt-public.pem
  - scripts/start.sh
  - scripts/stop.sh
autonomous: true
requirements: [DEVX-01]
must_haves:
  truths:
    - "An audit confirms every one of the 8 services has its bootRun prerequisites (server.port source, datasource, redis, rabbitmq, JWT key) resolvable under the dev profile"
    - "A single start script sources .env, boots compose infra, waits for healthy, then launches all 8 Spring Boot services + admin-web with per-service logs"
    - "Each service binds a distinct port and a per-service datasource URL; /actuator/health returns UP"
    - "A matching stop script kills the bootRun/next processes and runs docker compose down"
  artifacts:
    - path: "scripts/start.sh"
      provides: "One-command full-stack boot (infra-healthy → 8 services + admin-web)"
      contains: "docker compose up"
    - path: "scripts/stop.sh"
      provides: "Teardown (kill PIDs + compose down)"
      contains: "docker compose down"
  key_links:
    - from: "scripts/start.sh"
      to: ".env"
      via: "set -a; source .env; set +a (fail fast if missing)"
      pattern: "source .env"
    - from: "scripts/start.sh"
      to: "./gradlew :services:<svc>:bootRun"
      via: "8 separate background invocations with per-service SERVER_PORT + SPRING_DATASOURCE_URL"
      pattern: "bootRun"
    - from: "scripts/start.sh"
      to: "apps/admin-web (next dev)"
      via: "npm run dev (NOT gradle)"
      pattern: "npm run dev"
---

<objective>
Make the full local stack boot with one command. First AUDIT all 8 services for the bootRun prerequisites the research flagged (no committed server.port/datasource/redis/rabbitmq config; JWT public key only under test-keys; catalog-service has NO application.yml at all), then supply the minimum dev-profile config + dev JWT public key so each service boots healthy under the dev/stub profile. Then write start.sh (source .env → compose up → wait healthy → launch 8 bootRun + admin-web next dev, per-service logs) and a matching stop.sh.

Purpose: DEVX-01 — single-command boot + teardown. Depends on 07-02 (compose + .env.example) and 07-03 (re-layered payment-service must still boot).
Output: per-service dev config, dev JWT keys, start.sh, stop.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-clean-architecture-local-dev-tooling/07-RESEARCH.md
@.planning/phases/07-clean-architecture-local-dev-tooling/07-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 8 services + supply dev-profile config and a dev JWT public key</name>
  <files>services/catalog-service/src/main/resources/application.yml, services/identity-service/src/main/resources/application-dev.yml, services/wallet-service/src/main/resources/application-dev.yml, services/payment-service/src/main/resources/application-dev.yml, services/contact-service/src/main/resources/application-dev.yml, services/messaging-service/src/main/resources/application-dev.yml, services/notification-service/src/main/resources/application-dev.yml, services/admin-service/src/main/resources/application-dev.yml, services/*/src/main/resources/keys/jwt-public.pem</files>
  <read_first>
    - 07-RESEARCH.md §Critical missing-config findings (1: no committed datasource/port; 2: missing classpath:keys/jwt-public.pem) and Pitfalls 2 & 3, Assumptions A2/A3, Open Questions 1-2
    - 07-RESEARCH.md §Security Domain + Threat note (dev key must be PUBLIC only — never commit a private signing key to src/main/resources)
    - Each service's existing src/main/resources/application.yml (note catalog-service has none; identity references jwt-private.pem too)
    - A test-keys jwt-public.pem under any service's src/test/resources/test-keys/ (reuse this PUBLIC key as the dev key; resource servers only validate)
    - 07-RESEARCH.md §Suggested port map (identity 8081, catalog 8082, wallet 8083, payment 8084, contact 8085, messaging 8086, notification 8087, admin 8088; admin-web 3000)
  </read_first>
  <action>
    AUDIT: grep all 8 services for committed `server.port`, `spring.datasource`, `spring.data.redis`, `spring.rabbitmq`, and the `classpath:keys/jwt-public.pem` reference; record the per-service gap in the SUMMARY. FIX each gap so a dev-profile bootRun succeeds: (a) For services missing the JWT public key, place the existing test PUBLIC key at src/main/resources/keys/jwt-public.pem (PUBLIC key only — never a private key; identity-service additionally signs, so handle its private-key location under the dev profile using its existing test private key path, not a newly committed prod secret). (b) Add an application-dev.yml per service (and create catalog-service's missing base application.yml with spring.application.name + actuator health exposure) that relies on Spring relaxed env binding for datasource/redis/rabbitmq (values come from .env / start-script env) and does NOT hardcode secrets. Do NOT change any committed prod profile behavior, REST contract, Flyway migration, or the payment-service test outcomes. Keep image/stack versions per CLAUDE.md.
  </action>
  <acceptance_criteria>
    - For all 8 services, `test -f services/<svc>-service/src/main/resources/keys/jwt-public.pem` succeeds AND each committed key is a PUBLIC key (`grep -L 'PRIVATE KEY' services/*/src/main/resources/keys/jwt-public.pem` lists all 8 — none contain a private key)
    - `test -f services/catalog-service/src/main/resources/application.yml` succeeds (previously absent)
    - With compose infra up and `.env` sourced, each service starts via `SERVER_PORT=<port> SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/<svc> ./gradlew :services:<svc>-service:bootRun --args='--spring.profiles.active=dev'` and `curl -s localhost:<port>/actuator/health` returns `{"status":"UP"}` (spot-check payment + at least one other service; full set covered by start.sh smoke)
    - `./gradlew :services:payment-service:clean test` still green (no regression from any payment-service config edit)
    - No private key committed under any src/main/resources: `grep -rL . /dev/null; grep -rq 'PRIVATE KEY' services/*/src/main/resources/keys/ && exit 1 || true`
  </acceptance_criteria>
  <done>Every service has resolvable dev-profile bootRun prerequisites; only public keys committed.</done>
</task>

<task type="auto">
  <name>Task 2: Write start.sh and stop.sh</name>
  <files>scripts/start.sh, scripts/stop.sh</files>
  <read_first>
    - 07-RESEARCH.md §Start Script pattern (DEVX-01) — the 9-step sequence; §Gradle-parallel guidance (8 SEPARATE background bootRun invocations, NOT one combined call, NOT --parallel); §Anti-Patterns; §Don't Hand-Roll (compose healthcheck poll, not sleep)
    - 07-RESEARCH.md §Suggested port map and §.env.example pattern (per-service SPRING_DATASOURCE_URL + SERVER_PORT derived in the loop)
    - 07-CONTEXT.md decision 2a (8 services + admin-web; Flutter NOT included; fail fast if .env missing; matching teardown)
    - compose.yaml and .env.example from 07-02
  </read_first>
  <action>
    Create scripts/start.sh (bash, `set -euo pipefail`): cd to repo root; fail fast with `cp .env.example .env` guidance if `.env` absent; `set -a; source .env; set +a`; `docker compose up -d`; poll `docker compose ps` health (no `sleep`-based wait — loop on compose healthchecks) until postgres/redis/rabbitmq healthy; `mkdir -p .logs`; loop over the 8 services (identity catalog wallet payment contact messaging notification admin) launching each as a SEPARATE background `SERVER_PORT=<mapped> SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/<svc> ./gradlew ":services:<svc>-service:bootRun" --args='--spring.profiles.active=dev' > .logs/<svc>.log 2>&1 &`, recording each PID to .logs/pids; launch admin-web via `(cd apps/admin-web && npm run dev > ../../.logs/admin-web.log 2>&1 &)` recording its PID. Use the suggested port map. Create scripts/stop.sh: kill PIDs in .logs/pids (ignore-missing), then `docker compose down`. Make both executable. Flutter customer-app is NOT launched.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/start.sh && bash -n scripts/stop.sh` (syntax OK); both are `chmod +x`
    - `grep -q 'source .env' scripts/start.sh && grep -q 'docker compose up' scripts/start.sh && grep -q 'npm run dev' scripts/start.sh` succeeds
    - `grep -c 'bootRun' scripts/start.sh` shows the 8 services launched (loop or explicit) and NOT a single combined `gradlew bootRun` for all; `grep -vq 'sleep ' scripts/start.sh` (no sleep-based wait) — readiness uses `docker compose ps` polling
    - `grep -q 'docker compose down' scripts/stop.sh` succeeds
    - Running `./scripts/start.sh` boots compose to healthy and at least payment-service `curl -s localhost:8084/actuator/health` returns UP; `./scripts/stop.sh` then stops all processes and `docker compose ps` shows no running infra (manual-assisted smoke per 07-VALIDATION Manual-Only Verifications)
  </acceptance_criteria>
  <done>One-command boot to healthy + matching teardown, per-service logs, Flutter excluded.</done>
</task>

</tasks>

<verification>
- Audit recorded for all 8 services; dev config + public JWT keys present; catalog-service application.yml created.
- `./scripts/start.sh` → compose healthy + services UP (payment + spot-checks); `./scripts/stop.sh` → clean teardown.
- payment-service oracle still green after config edits.
</verification>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| .env → child processes | Local secrets sourced into Gradle/Spring/Next processes on the dev host |
| committed keys/ dir | JWT material added to src/main/resources |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | Accidental commit of a private signing key in src/main/resources/keys | mitigate | Commit PUBLIC keys only (resource servers validate); acceptance asserts no `PRIVATE KEY` under any src/main/resources/keys |
| T-07-04 | Information Disclosure | Real Azampay/infra secret leaking via .env | accept | .env gitignored (07-02); .env.example carries placeholders only; start.sh sources .env, never echoes it |
| T-07-05 | Tampering | Dev config edit silently changing prod profile behavior | mitigate | Changes scoped to application-dev.yml + dev-profile key resolution; payment-service oracle re-run green |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages; admin-web adds none (07-RESEARCH §Package Legitimacy Audit) |
</threat_model>

<success_criteria>
DEVX-01 satisfied: audited prerequisites resolved, one-command boot to all-healthy with per-service logs, and a matching stop path — under the dev/stub profile with no real credentials.
</success_criteria>

<output>
Create `.planning/phases/07-clean-architecture-local-dev-tooling/07-04-SUMMARY.md` when done.
</output>
