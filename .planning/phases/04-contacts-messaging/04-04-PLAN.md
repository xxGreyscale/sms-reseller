---
phase: 04-contacts-messaging
plan: 04
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - services/messaging-service/src/main/resources/db/migration/V1__create_campaigns.sql
  - services/messaging-service/src/main/resources/db/migration/V2__create_outbound_messages.sql
  - services/messaging-service/src/main/resources/db/migration/V3__create_sender_id_requests.sql
  - services/messaging-service/src/main/resources/db/migration/V4__create_outbox.sql
  - services/messaging-service/src/main/java/com/smsreseller/messaging/config/SecurityConfig.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/config/RabbitMqConfig.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/campaign/Campaign.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/campaign/CampaignStatus.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/campaign/CampaignRepository.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/message/OutboundMessage.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/message/MessageStatus.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/message/OutboundMessageRepository.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/senderid/SenderIdRequest.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/senderid/SenderIdStatus.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/sms/SmsProvider.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/sms/SmsResult.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/sms/StubSmsProvider.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/sms/SmsEncoder.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/sms/SmsEncoding.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/outbox/OutboxEntry.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/outbox/OutboxRepository.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/outbox/OutboxRelay.java
  - services/messaging-service/src/test/java/com/smsreseller/messaging/SmsEncoderTest.java
autonomous: true
requirements: [MESG-01, MESG-02]

must_haves:
  truths:
    - "A user can create a campaign targeting one or more groups; it persists in DRAFT state"
    - "The SMS encoder detects GSM-7 vs UCS-2 and computes correct part counts (160/153 GSM-7, 70/67 UCS-2)"
    - "The quorum send queue + DLX TTL-ladder + dead queue are declared and pass passive verification"
    - "StubSmsProvider returns ACCEPTED / HARD_FAIL / TRANSIENT_FAIL based on phone suffix and simulates delivery receipts"
  artifacts:
    - path: "services/messaging-service/src/main/java/com/smsreseller/messaging/config/RabbitMqConfig.java"
      provides: "messaging.events topic exchange + messaging.send quorum queue + DLX TTL ladder + messaging.dead"
      contains: "quorum"
    - path: "services/messaging-service/src/main/java/com/smsreseller/messaging/sms/SmsEncoder.java"
      provides: "GSM-7/UCS-2 detection + part counting"
      contains: "partCount"
    - path: "services/messaging-service/src/main/resources/db/migration/V2__create_outbound_messages.sql"
      provides: "outbound_messages with lot_id column"
      contains: "lot_id"
  key_links:
    - from: "services/messaging-service/src/main/java/com/smsreseller/messaging/sms/StubSmsProvider.java"
      to: "SmsProvider"
      via: "@Profile(\"stub\") @Service implements SmsProvider"
      pattern: "Profile.*stub"
---

<objective>
Lay the messaging-service foundation: campaign / outbound-message / sender-id-request entities
and migrations, the SmsProvider mock-first interface (+ Stub), the GSM-7/UCS-2 SmsEncoder, the
transactional outbox copy, the SecurityConfig (with the /internal/** ROLE_ADMIN matcher), and
the RabbitMqConfig declaring the quorum send queue + DLX TTL-ladder + dead queue. Delivers
MESG-01 (create campaign targeting groups, DRAFT state) and MESG-02 (SMS part/encoding logic).
This is the structural backbone the send pipeline (04-05), DLX retry (04-06), and scheduler /
sender-ID (04-08) plans all build on.

Purpose: All messaging behavior depends on these entities, the queue topology, and the stub.
Output: Compiling messaging-service with full schema, queue topology, stub provider, encoder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-contacts-messaging/04-RESEARCH.md
@.planning/phases/04-contacts-messaging/04-PATTERNS.md

<interfaces>
SmsProvider (consumed by 04-05/06):
  SmsResult send(String phoneE164, String body, String senderId);
SmsResult encodes outcome ACCEPTED | HARD_FAIL | TRANSIENT_FAIL + optional externalId.
CampaignStatus enum: DRAFT | SCHEDULED | QUEUED | SENDING | COMPLETED | CANCELLED (04-RESEARCH §State Machines).
MessageStatus enum: PENDING | SENT | DELIVERED | FAILED.
SenderIdStatus enum: REQUESTED | APPROVED | REJECTED.
OutboundMessage carries lot_id (UUID) — set by 04-05 at creation, consumed by wallet events.
RabbitMqConfig constants (consumed by 04-05/06): EXCHANGE="messaging.events", SEND_QUEUE="messaging.send",
  DLX="messaging.retry.dlx", DEAD_QUEUE="messaging.dead", RETRY queues 1m/5m/15m.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entities + migrations + SecurityConfig + outbox copy</name>
  <files>services/messaging-service/src/main/resources/db/migration/V1__create_campaigns.sql, services/messaging-service/src/main/resources/db/migration/V2__create_outbound_messages.sql, services/messaging-service/src/main/resources/db/migration/V3__create_sender_id_requests.sql, services/messaging-service/src/main/resources/db/migration/V4__create_outbox.sql, services/messaging-service/src/main/java/com/smsreseller/messaging/config/SecurityConfig.java, services/messaging-service/src/main/java/com/smsreseller/messaging/campaign/Campaign.java, services/messaging-service/src/main/java/com/smsreseller/messaging/campaign/CampaignStatus.java, services/messaging-service/src/main/java/com/smsreseller/messaging/campaign/CampaignRepository.java, services/messaging-service/src/main/java/com/smsreseller/messaging/message/OutboundMessage.java, services/messaging-service/src/main/java/com/smsreseller/messaging/message/MessageStatus.java, services/messaging-service/src/main/java/com/smsreseller/messaging/message/OutboundMessageRepository.java, services/messaging-service/src/main/java/com/smsreseller/messaging/senderid/SenderIdRequest.java, services/messaging-service/src/main/java/com/smsreseller/messaging/senderid/SenderIdStatus.java, services/messaging-service/src/main/java/com/smsreseller/messaging/outbox/OutboxEntry.java, services/messaging-service/src/main/java/com/smsreseller/messaging/outbox/OutboxRepository.java, services/messaging-service/src/main/java/com/smsreseller/messaging/outbox/OutboxRelay.java</files>
  <read_first>
    - 04-RESEARCH.md §"Database Schema" messaging schema V1..V4 blocks + §State Machines (enum values)
    - services/payment-service/.../payment/Payment.java + PaymentStatus.java (entity/enum analogs, 04-PATTERNS rows)
    - services/identity-service/.../outbox/OutboxEntry.java + OutboxRepository.java + OutboxRelay.java (copy verbatim, change package + exchange constant to messaging.events)
    - services/wallet-service/.../config/SecurityConfig.java (analog); 04-PATTERNS note to add .requestMatchers("/api/v1/internal/**").hasRole("ADMIN")
  </read_first>
  <action>
    Write V1__create_campaigns.sql (campaigns: id, user_id, name, body TEXT, sender_id VARCHAR(20), status VARCHAR(20) DEFAULT 'DRAFT', scheduled_at, dispatched_at, created_at, updated_at; idx_campaigns_user_id; partial idx_campaigns_status_scheduled WHERE status='SCHEDULED'). V2__create_outbound_messages.sql (id, campaign_id REFERENCES campaigns, user_id, phone_e164, lot_id UUID NOT NULL, status VARCHAR(20) DEFAULT 'PENDING', external_id, created_at, updated_at; indexes on campaign and (campaign_id,status)). V3__create_sender_id_requests.sql (id, user_id, sender_name VARCHAR(11), status VARCHAR(20) DEFAULT 'REQUESTED', reject_reason TEXT, decided_at, created_at, updated_at). V4__create_outbox.sql (copy identity outbox: event_id unique, event_type, payload TEXT, sent BOOLEAN, created_at, sent_at; partial unsent index). Also add a campaign_groups join (campaign_id, group_id) either as a migration addition or a column array — per MESG-01 a campaign targets group IDs; store target group IDs (the recipient snapshot itself lands in outbound_messages at dispatch in 04-05). Create entities with jakarta.*, auditing, @Enumerated(STRING); enums CampaignStatus/MessageStatus/SenderIdStatus per the interfaces block. CampaignRepository: findByIdAndUserId, findByUserId(Pageable), findByStatusAndScheduledAtBefore(status, instant, Pageable) (used by 04-08). OutboundMessageRepository: findByCampaignId, status aggregate count query (used by 04-06), findByIdAndUserId. Copy the outbox triple, changing exchange constant to messaging.events. SecurityConfig mirrors wallet plus .requestMatchers("/api/v1/internal/**").hasRole("ADMIN") above anyRequest().authenticated(). Enable @EnableJpaAuditing + @EnableScheduling.
  </action>
  <verify>
    <automated>./gradlew :services:messaging-service:compileJava && grep -c lot_id services/messaging-service/src/main/resources/db/migration/V2__create_outbound_messages.sql</automated>
  </verify>
  <acceptance_criteria>
    - messaging-service compiles.
    - `grep -c lot_id services/messaging-service/.../V2__create_outbound_messages.sql` >= 1.
    - SecurityConfig contains `/api/v1/internal/**` and `hasRole("ADMIN")`.
    - CampaignStatus enum contains DRAFT, SCHEDULED, QUEUED, SENDING, COMPLETED, CANCELLED.
  </acceptance_criteria>
  <done>Schema, entities, enums, outbox, and admin-guarded security config exist and compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SmsEncoder (GSM-7/UCS-2 part counting) + RabbitMqConfig quorum/DLX topology + SmsProvider stub</name>
  <files>services/messaging-service/src/main/java/com/smsreseller/messaging/sms/SmsEncoder.java, services/messaging-service/src/main/java/com/smsreseller/messaging/sms/SmsEncoding.java, services/messaging-service/src/main/java/com/smsreseller/messaging/sms/SmsProvider.java, services/messaging-service/src/main/java/com/smsreseller/messaging/sms/SmsResult.java, services/messaging-service/src/main/java/com/smsreseller/messaging/sms/StubSmsProvider.java, services/messaging-service/src/main/java/com/smsreseller/messaging/config/RabbitMqConfig.java, services/messaging-service/src/test/java/com/smsreseller/messaging/SmsEncoderTest.java</files>
  <read_first>
    - 04-RESEARCH.md §Pattern 6 (GSM-7 charset + extended set; 160/153, 70/67; extended chars count as 2) and §Pattern 1 (full QueueBuilder quorum/DLX/TTL-ladder config) and §Pattern 1 application.yml
    - 04-PATTERNS.md SmsProvider + StubSmsProvider rows (magic suffix 0001=HARD_FAIL, 0002=TRANSIENT_FAIL, else ACCEPTED; @Value default-outcome; @Scheduled DLR simulation via ConcurrentHashMap)
    - 04-RESEARCH.md Test Map MESG-02; Pitfall 2 (must call .quorum()) + Pitfall 6 (stub must exercise all 3 outcomes)
    - services/payment-service/.../gateway/PaymentGateway.java + StubPaymentGateway.java (interface + stub analog)
    - services/wallet-service/.../config/RabbitMqConfig.java (exchange/converter/template analog)
  </read_first>
  <behavior>
    - SmsEncoderTest.partCountAndEncodingDetection (MESG-02): 160 GSM-7 chars → 1 part; 161 → 2 parts; a Swahili/extended char outside GSM-7 → UCS2 detected, 70 chars → 1 part, 71 → 2 parts; a GSM-7 extended char (e.g. "€") counts as 2 toward the GSM-7 length.
  </behavior>
  <action>
    SmsEncoding enum {GSM7, UCS2}. SmsEncoder.detect(text) returns UCS2 if any char not in the GSM-7 basic+extended set, else GSM7; charCount(text, enc) sums 1 per GSM-7 basic char and 2 per extended char (UCS2 = text.length()); partCount(charCount, enc) = 1 if <= single (160/70) else ceil(charCount/multi) (153/67), per Pattern 6. SmsProvider interface: SmsResult send(phoneE164, body, senderId). SmsResult record/class encoding ACCEPTED|HARD_FAIL|TRANSIENT_FAIL + optional externalId, with static factories accepted(extId)/hardFail(reason)/transientFail(reason). StubSmsProvider @Profile("stub") @Service: resolve outcome from last 4 digits of phoneE164 (0001→HARD_FAIL, 0002→TRANSIENT_FAIL else default ACCEPTED) with @Value("${app.sms.stub.default-outcome:ACCEPTED}"); maintain ConcurrentHashMap<UUID,Instant> of accepted messages and a @Scheduled(fixedDelay=5000) sweep that, after app.sms.stub.dlr-delay-ms (default 10000, override short in test profile), invokes a delivery-receipt callback seam (a functional interface or the messaging service handle wired in 04-06; for now expose a settable handler so 04-06 can attach handleDeliveryReceipt). RabbitMqConfig: TopicExchange messaging.events; DirectExchange messaging.retry.dlx; quorum sendQueue (deliveryLimit(3), DLX=messaging.retry.dlx, routingKey=messaging.retry.1m); classic durable retry queues 1m/5m/15m with TTL (overridable via app.messaging.retry.ttl-* for tests) each dead-lettering to default exchange routingKey messaging.send; quorum deadQueue bound to DLX with routingKey messaging.dead; Jackson2JsonMessageConverter + RabbitTemplate beans. Per OQ#4 keep retry queues classic (TTL universally supported) and only send/dead as quorum.
  </action>
  <verify>
    <automated>./gradlew :services:messaging-service:test --tests "SmsEncoderTest" && grep -c "quorum" services/messaging-service/src/main/java/com/smsreseller/messaging/config/RabbitMqConfig.java</automated>
  </verify>
  <acceptance_criteria>
    - SmsEncoderTest.partCountAndEncodingDetection passes (all asserted boundaries).
    - RabbitMqConfig contains `.quorum()` on the send and dead queues and `.deliveryLimit(3)` and `.ttl(` on retry queues.
    - StubSmsProvider is annotated `@Profile("stub")` and returns HARD_FAIL for a number ending 0001 and TRANSIENT_FAIL for 0002 (assert via a small direct unit assertion or in SmsEncoderTest companion).
  </acceptance_criteria>
  <done>Encoder math correct; full quorum/DLX/TTL topology declared; stub provider supports all three outcomes + DLR simulation seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → messaging-service REST | JWT-bearing; /internal/** must be ADMIN-only |
| messaging-service ↔ RabbitMQ | quorum durability + DLX prevents loss/poison loops |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Spoofing | /api/v1/internal/sender-ids/** | mitigate | SecurityConfig requires hasRole("ADMIN") on /api/v1/internal/** (sender-ID approval bypass, 04-RESEARCH.md). |
| T-04-06 | Denial of Service | poison AMQP message | mitigate | quorum send queue + deliveryLimit(3) + default-requeue-rejected:false (set in 04-01 yml) route poison to DLX, not infinite loop (Pitfall 1). |
| T-04-07 | Tampering | quorum vs classic queue mismatch | mitigate | .quorum() explicit on send/dead; retry queues classic-with-TTL by design (OQ#4); Pitfall 2 covered. |
</threat_model>

<verification>
- `./gradlew :services:messaging-service:test --tests "SmsEncoderTest"` green; module compiles; queue topology declared.
</verification>

<success_criteria>
MESG-01 schema/entities for campaign creation in DRAFT and MESG-02 encoder satisfied; full
messaging foundation (entities, queue topology, stub provider, outbox, admin-guarded security)
in place for downstream pipeline plans.
</success_criteria>

<output>
Create `.planning/phases/04-contacts-messaging/04-04-SUMMARY.md` when done.
</output>
