---
phase: 04-contacts-messaging
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - services/contact-service/src/main/resources/db/migration/V1__create_contacts.sql
  - services/contact-service/src/main/resources/db/migration/V2__create_contact_groups.sql
  - services/contact-service/src/main/resources/db/migration/V3__create_suppressed_numbers.sql
  - services/contact-service/src/main/java/com/opendesk/contact/config/SecurityConfig.java
  - services/contact-service/src/main/java/com/opendesk/contact/contact/Contact.java
  - services/contact-service/src/main/java/com/opendesk/contact/contact/ContactRepository.java
  - services/contact-service/src/main/java/com/opendesk/contact/contact/ContactService.java
  - services/contact-service/src/main/java/com/opendesk/contact/contact/ContactController.java
  - services/contact-service/src/main/java/com/opendesk/contact/contact/ContactDto.java
  - services/contact-service/src/main/java/com/opendesk/contact/group/ContactGroup.java
  - services/contact-service/src/main/java/com/opendesk/contact/group/GroupMembership.java
  - services/contact-service/src/main/java/com/opendesk/contact/group/ContactGroupRepository.java
  - services/contact-service/src/main/java/com/opendesk/contact/group/GroupService.java
  - services/contact-service/src/main/java/com/opendesk/contact/group/GroupController.java
  - services/contact-service/src/main/java/com/opendesk/contact/suppression/SuppressedNumber.java
  - services/contact-service/src/main/java/com/opendesk/contact/suppression/SuppressionRepository.java
  - services/contact-service/src/main/java/com/opendesk/contact/suppression/SuppressionService.java
  - services/contact-service/src/main/java/com/opendesk/contact/suppression/SuppressionController.java
  - services/contact-service/src/test/java/com/opendesk/contact/ContactCrudIT.java
  - services/contact-service/src/test/java/com/opendesk/contact/ContactGroupIT.java
  - services/contact-service/src/test/java/com/opendesk/contact/SuppressionIT.java
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-08]

must_haves:
  truths:
    - "A user can add, edit, and delete an individual contact (name + E.164 phone) scoped to their JWT subject"
    - "A user can create a named group and add/remove contacts; membership persists; deleting a contact removes it from its groups"
    - "A user can add a number to a per-user suppression list; suppression is queryable for campaign exclusion"
    - "Cross-user access returns 404 (IDOR guard), never another user's data"
  artifacts:
    - path: "services/contact-service/src/main/java/com/opendesk/contact/contact/Contact.java"
      provides: "Contact JPA entity"
      contains: "uq_contact_user_phone"
    - path: "services/contact-service/src/main/java/com/opendesk/contact/suppression/SuppressionRepository.java"
      provides: "existsByUserIdAndPhoneE164 query used by campaign expansion"
      contains: "existsByUserIdAndPhoneE164"
    - path: "services/contact-service/src/main/resources/db/migration/V1__create_contacts.sql"
      provides: "contacts table with unique (user_id, phone_e164)"
      contains: "uq_contact_user_phone"
  key_links:
    - from: "services/contact-service/src/main/java/com/opendesk/contact/contact/ContactController.java"
      to: "auth.getToken().getSubject()"
      via: "IDOR guard on every endpoint"
      pattern: "getSubject"
---

<objective>
Build contact-service core: Contact / ContactGroup / SuppressedNumber entities, Flyway
migrations, repositories, services, REST controllers with IDOR guards, and a resource-server
SecurityConfig. Delivers CONT-01/02/03 (contact CRUD), CONT-04 (named groups), and CONT-08
(suppression list) — the data foundation that CSV import (04-03) and campaign recipient
expansion (04-05) build on.

Purpose: Contacts and suppression are prerequisites for every campaign send.
Output: Working contact/group/suppression REST API with per-user data isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-contacts-messaging/04-RESEARCH.md
@.planning/phases/04-contacts-messaging/04-PATTERNS.md

<interfaces>
SuppressionRepository MUST expose (consumed by 04-05 campaign expansion via internal REST):
  boolean existsByUserIdAndPhoneE164(UUID userId, String phoneE164);
ContactRepository MUST expose:
  Optional<Contact> findByIdAndUserId(UUID id, UUID userId);   // IDOR
  Page<Contact> findByUserId(UUID userId, Pageable p);
  // insertIfAbsent (ON CONFLICT DO NOTHING) is added in 04-03 for CSV; basic save here.
Group recipient lookup (consumed by 04-05): a method returning distinct phone_e164 for a set
  of group IDs scoped to user — define ContactGroupRepository.findContactPhonesByGroupIdsAndUserId.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contact entity + migrations + CRUD API with IDOR guard (CONT-01/02/03)</name>
  <files>services/contact-service/src/main/resources/db/migration/V1__create_contacts.sql, services/contact-service/src/main/java/com/opendesk/contact/config/SecurityConfig.java, services/contact-service/src/main/java/com/opendesk/contact/contact/Contact.java, services/contact-service/src/main/java/com/opendesk/contact/contact/ContactRepository.java, services/contact-service/src/main/java/com/opendesk/contact/contact/ContactService.java, services/contact-service/src/main/java/com/opendesk/contact/contact/ContactController.java, services/contact-service/src/main/java/com/opendesk/contact/contact/ContactDto.java, services/contact-service/src/test/java/com/opendesk/contact/ContactCrudIT.java</files>
  <read_first>
    - services/payment-service/.../payment/Payment.java + PaymentController.java + PaymentRepository.java (entity/controller/repo analogs, 04-PATTERNS.md Contact rows)
    - services/wallet-service/.../config/SecurityConfig.java (resource-server analog)
    - 04-RESEARCH.md §"Database Schema" V1__create_contacts.sql block
    - 04-PATTERNS.md §"IDOR Guard" (auth.getToken().getSubject())
    - 04-RESEARCH.md Test Map rows CONT-01/02/03
  </read_first>
  <behavior>
    - addContactPersistsAndIsRetrievable (CONT-01): POST /api/v1/contacts {name, phone} → 200; GET list returns it scoped to JWT subject.
    - editContactUpdatesFields (CONT-02): PUT/PATCH /api/v1/contacts/{id} updates name/phone; change persisted.
    - deleteContactRemovesFromGroups (CONT-03): DELETE /api/v1/contacts/{id} removes row (group cleanup asserted in Task 2 once groups exist; here assert row gone + 404 on re-fetch).
    - IDOR: fetching another user's contact id returns 404 not 403.
  </behavior>
  <action>
    Write V1__create_contacts.sql exactly per 04-RESEARCH.md: contacts(id UUID PK default gen_random_uuid, user_id UUID NOT NULL, name VARCHAR(255), phone_e164 VARCHAR(20) NOT NULL, created_at/updated_at TIMESTAMPTZ, CONSTRAINT uq_contact_user_phone UNIQUE(user_id, phone_e164)), index idx_contacts_user_id. Contact entity uses jakarta.*, @EntityListeners(AuditingEntityListener.class), @CreatedDate/@LastModifiedDate, userId updatable=false, id assigned via UUID.randomUUID(). Enable @EnableJpaAuditing (config). ContactRepository: findByIdAndUserId, findByUserId(Pageable), existsByUserIdAndPhoneE164. ContactService: create/update/delete/list, all taking userId; throw a NotFound (mapped to 404) when findByIdAndUserId is empty. ContactController: every method derives userId from auth.getToken().getSubject(); @Valid on ContactDto (name optional, phone @NotBlank). SecurityConfig: copy wallet pattern (STATELESS, CSRF off, permit actuator/error, anyRequest authenticated, oauth2 resource-server jwt). Convert ContactCrudIT placeholders to real assertions extending AbstractContactIntegrationTest, minting a test JWT the way Phase 3 wallet ITs do (reuse the same RSA test-keys mechanism noted in STATE.md).
  </action>
  <verify>
    <automated>./gradlew :services:contact-service:test --tests "ContactCrudIT"</automated>
  </verify>
  <acceptance_criteria>
    - ContactCrudIT addContactPersistsAndIsRetrievable / editContactUpdatesFields / deleteContactRemovesFromGroups all pass.
    - `grep -c uq_contact_user_phone services/contact-service/src/main/resources/db/migration/V1__create_contacts.sql` >= 1.
    - Fetching a contact owned by another userId returns HTTP 404.
    - ContactController contains `getSubject` on every handler (IDOR).
  </acceptance_criteria>
  <done>Contact CRUD works, per-user isolated, migration + entity match the research schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Named groups with membership + suppression list (CONT-04, CONT-08)</name>
  <files>services/contact-service/src/main/resources/db/migration/V2__create_contact_groups.sql, services/contact-service/src/main/resources/db/migration/V3__create_suppressed_numbers.sql, services/contact-service/src/main/java/com/opendesk/contact/group/ContactGroup.java, services/contact-service/src/main/java/com/opendesk/contact/group/GroupMembership.java, services/contact-service/src/main/java/com/opendesk/contact/group/ContactGroupRepository.java, services/contact-service/src/main/java/com/opendesk/contact/group/GroupService.java, services/contact-service/src/main/java/com/opendesk/contact/group/GroupController.java, services/contact-service/src/main/java/com/opendesk/contact/suppression/SuppressedNumber.java, services/contact-service/src/main/java/com/opendesk/contact/suppression/SuppressionRepository.java, services/contact-service/src/main/java/com/opendesk/contact/suppression/SuppressionService.java, services/contact-service/src/main/java/com/opendesk/contact/suppression/SuppressionController.java, services/contact-service/src/test/java/com/opendesk/contact/ContactGroupIT.java, services/contact-service/src/test/java/com/opendesk/contact/SuppressionIT.java</files>
  <read_first>
    - 04-RESEARCH.md §"Database Schema" V2__create_contact_groups.sql + V3__create_suppressed_numbers.sql
    - services/payment-service/.../payment/PaymentService.java + PaymentController.java (role-match analogs)
    - 04-PATTERNS.md group/suppression rows; 04-RESEARCH.md Test Map CONT-04/CONT-08
    - D-08 (suppression scoped per-user globally)
  </read_first>
  <behavior>
    - groupMembershipIsPersisted (CONT-04): create group; add 2 contacts; membership persists; remove 1; reflects.
    - deleteContact cascade: deleting a contact removes its contact_group_members rows (ON DELETE CASCADE).
    - suppressedNumberIsExcluded (CONT-08): POST suppress +255712345678; SuppressionRepository.existsByUserIdAndPhoneE164 returns true for that user, false for another user (per-user global, D-08).
  </behavior>
  <action>
    Write V2 (contact_groups with uq_group_user_name UNIQUE(user_id,name); contact_group_members join table PK(group_id,contact_id) with ON DELETE CASCADE to both contact_groups and contacts) and V3 (suppressed_numbers with uq_suppressed_user_phone UNIQUE(user_id,phone_e164)) exactly per research. ContactGroup entity + GroupMembership (composite-key @Embeddable or @IdClass) or model membership via a @ManyToMany — prefer an explicit join entity for testability. ContactGroupRepository: findByIdAndUserId, findContactPhonesByGroupIdsAndUserId(Collection<UUID> groupIds, UUID userId) returning distinct phone_e164 (consumed by 04-05). GroupService: createGroup, addMember, removeMember, all userId-scoped with IDOR 404. SuppressedNumber entity + SuppressionRepository.existsByUserIdAndPhoneE164 + findByUserId. SuppressionService.suppress(userId, e164) idempotent (ON CONFLICT DO NOTHING or check-exists). Controllers derive userId from JWT subject. Convert ContactGroupIT + SuppressionIT placeholders to real assertions.
  </action>
  <verify>
    <automated>./gradlew :services:contact-service:test --tests "ContactGroupIT" --tests "SuppressionIT"</automated>
  </verify>
  <acceptance_criteria>
    - ContactGroupIT.groupMembershipIsPersisted and SuppressionIT.suppressedNumberIsExcluded pass.
    - Deleting a contact removes its rows from contact_group_members (assert via group membership query post-delete).
    - SuppressionRepository.existsByUserIdAndPhoneE164 returns false for a different userId with the same number (D-08 per-user global).
    - `grep -c uq_suppressed_user_phone services/contact-service/src/main/resources/db/migration/V3__create_suppressed_numbers.sql` >= 1.
  </acceptance_criteria>
  <done>Groups + membership + suppression persist with per-user scoping; cascade delete works; suppression lookup ready for campaign expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → contact-service REST | untrusted JWT-bearing requests; path/body ids must never override JWT subject |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of privilege | Contact/Group/Suppression controllers | mitigate | userId derived only from auth.getToken().getSubject(); all repo queries filter by userId; cross-user fetch returns 404 (404-not-403, 04-RESEARCH.md V4). |
| T-04-02 | Tampering | request DTOs | mitigate | @Valid on ContactDto; phone stored as VARCHAR string; normalization deferred to 04-03 PhoneNormalizer. |
</threat_model>

<verification>
- `./gradlew :services:contact-service:test --tests "ContactCrudIT" --tests "ContactGroupIT" --tests "SuppressionIT"` green.
</verification>

<success_criteria>
CONT-01/02/03/04/08 satisfied: contact CRUD, named groups with membership + cascade delete,
and per-user suppression list, all IDOR-guarded, with passing ITs.
</success_criteria>

<output>
Create `.planning/phases/04-contacts-messaging/04-02-SUMMARY.md` when done.
</output>
