---
phase: 04-contacts-messaging
plan: 06
type: tdd
wave: 3
depends_on: ["04-05"]
files_modified:
  - services/messaging-service/src/main/java/com/smsreseller/messaging/message/SendMessageConsumer.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/message/DeadLetterConsumer.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/message/MessageEventPublisher.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/message/DeliveryReceiptService.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/sms/StubSmsProvider.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/campaign/CampaignService.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/campaign/CampaignController.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/campaign/CampaignStatusView.java
  - services/messaging-service/src/test/java/com/smsreseller/messaging/DlxRetryIT.java
  - services/messaging-service/src/test/java/com/smsreseller/messaging/DeliveryTrackingIT.java
  - services/messaging-service/src/test/java/com/smsreseller/messaging/CampaignStatusIT.java
autonomous: true
requirements: [MESG-06, MESG-07, MESG-10]

must_haves:
  truths:
    - "A transient-failing message is retried via the DLX TTL-ladder (1m→5m→15m) and counted by x-delivery-limit"
    - "A message that exhausts retries OR hard-fails reaches the dead queue, becomes FAILED, and emits MessageRefundDue (wallet REFUND)"
    - "Stub-simulated delivery receipts transition SENT messages to DELIVERED (or FAILED)"
    - "Per-message status is queryable; campaign aggregate shows sent/delivered/failed counts"
  artifacts:
    - path: "services/messaging-service/src/main/java/com/smsreseller/messaging/message/DeadLetterConsumer.java"
      provides: "@RabbitListener on messaging.dead → FAILED + MessageRefundDue"
      contains: "messaging.dead"
    - path: "services/messaging-service/src/main/java/com/smsreseller/messaging/message/DeliveryReceiptService.java"
      provides: "handleDeliveryReceipt(messageId, status) SENT→DELIVERED/FAILED"
      contains: "handleDeliveryReceipt"
  key_links:
    - from: "services/messaging-service/src/main/java/com/smsreseller/messaging/message/DeadLetterConsumer.java"
      to: "MessageEventPublisher (MessageRefundDue)"
      via: "permanent failure → refund-due outbox event"
      pattern: "RefundDue"
    - from: "services/messaging-service/src/main/java/com/smsreseller/messaging/sms/StubSmsProvider.java"
      to: "DeliveryReceiptService.handleDeliveryReceipt"
      via: "@Scheduled DLR simulation callback"
      pattern: "handleDeliveryReceipt"
---

<objective>
Complete the retry, refund, and delivery-tracking half of the pipeline: advance failed messages
through the DLX TTL-ladder, dead-letter permanently-undeliverable messages to FAILED and emit
MessageRefundDue (wallet credit-back), ingest stub-simulated delivery receipts to transition
SENT→DELIVERED/FAILED, and expose per-message status and campaign aggregate counts. Delivers
MESG-10 (DLQ retry + refund), MESG-07 (per-message status), MESG-06 (aggregate counts).

Purpose: Closes the credit invariant (no free sends, no stranded charges) and gives users
accurate delivery visibility.
Output: Working DLX retry → refund flow + delivery receipt ingestion + status/aggregate APIs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/04-contacts-messaging/04-RESEARCH.md
@.planning/phases/04-contacts-messaging/04-PATTERNS.md

<interfaces>
From 04-04: RabbitMqConfig (retry queues 1m/5m/15m + messaging.dead), MessageStatus enum,
  StubSmsProvider DLR-simulation seam, OutboundMessageRepository status-aggregate query.
From 04-05: SendMessageConsumer (transient/hard-fail nack already wired), MessageEventPublisher
  (add refundDue(...)), SendMessagePayload(attemptCount).
From 04-07 (wallet): consumes MessageRefundDue → creditBack (already built).
DeliveryReceiptService.handleDeliveryReceipt(UUID messageId, DlrStatus status).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DLX ladder advancement + DeadLetterConsumer + MessageRefundDue (MESG-10)</name>
  <files>services/messaging-service/src/main/java/com/smsreseller/messaging/message/SendMessageConsumer.java, services/messaging-service/src/main/java/com/smsreseller/messaging/message/DeadLetterConsumer.java, services/messaging-service/src/main/java/com/smsreseller/messaging/message/MessageEventPublisher.java, services/messaging-service/src/test/java/com/smsreseller/messaging/DlxRetryIT.java</files>
  <read_first>
    - 04-RESEARCH.md §Pattern 1 "Routing key strategy" Approach A (read x-delivery-count header, republish to correct retry queue, ack original) + "The retry counter problem" sequence + Pitfall 3
    - 04-PATTERNS.md §"DeadLetterConsumer" (@RabbitListener(queues="messaging.dead") → FAILED + MessageRefundDue) + §"Key Observations" note 4 (DLX advancement is explicit)
    - 04-RESEARCH.md Test Map MESG-10; D-06 (~1m/5m/15m, 3 retries) D-07 (permanent = max retries OR hard-fail)
    - 04-VALIDATION.md note: use shortened TTL ladder (test profile from 04-01) to bound feedback latency
  </read_first>
  <behavior>
    - DlxRetryIT.permanentFailureEmitsRefundDueEvent (MESG-10): a recipient ending 0002 (TRANSIENT_FAIL) is retried across the ladder up to the delivery limit; once exhausted it lands in messaging.dead, OutboundMessage becomes FAILED, and a MessageRefundDue outbox row is written (eventId unique, lotId carried). A recipient ending 0001 (HARD_FAIL) goes to FAILED + MessageRefundDue immediately (no full ladder). Uses the shortened TTL test profile so the test completes within the feedback budget.
  </behavior>
  <action>
    Implement Approach A ladder advancement in SendMessageConsumer: on TRANSIENT_FAIL read the x-delivery-count (or attemptCount on payload) header; if attempts remain, republish the payload (attemptCount+1) to the next retry queue in the ladder (1m→5m→15m) via RabbitTemplate to messaging.retry.dlx with the appropriate routing key, then ack the original; if attempts are exhausted, route to messaging.dead. On HARD_FAIL, route directly to messaging.dead (mark intent permanent). Keep default-requeue-rejected:false semantics. DeadLetterConsumer: @RabbitListener(queues = RabbitMqConfig.DEAD_QUEUE), @Transactional — load the OutboundMessage, set FAILED, and call MessageEventPublisher.refundDue(messageId, userId, lotId, 1) (writes MessageRefundDue outbox → relay → wallet creditBack). Add MessageEventPublisher.refundDue. Convert DlxRetryIT to real assertions (Awaitility with shortened TTLs).
  </action>
  <verify>
    <automated>./gradlew :services:messaging-service:test --tests "DlxRetryIT"</automated>
  </verify>
  <acceptance_criteria>
    - permanentFailureEmitsRefundDueEvent passes: FAILED status + a MessageRefundDue outbox row with the correct lotId.
    - DeadLetterConsumer listens on messaging.dead and writes a refund event (grep: `messaging.dead` + `refundDue`).
    - Hard-fail (suffix 0001) reaches FAILED+refund without traversing all three retry queues; transient-fail (0002) traverses the ladder before dead.
  </acceptance_criteria>
  <done>Failed messages retry via DLX ladder; permanent failures become FAILED and trigger wallet refund.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delivery receipt ingestion + per-message status + campaign aggregate (MESG-07/06)</name>
  <files>services/messaging-service/src/main/java/com/smsreseller/messaging/message/DeliveryReceiptService.java, services/messaging-service/src/main/java/com/smsreseller/messaging/sms/StubSmsProvider.java, services/messaging-service/src/main/java/com/smsreseller/messaging/campaign/CampaignService.java, services/messaging-service/src/main/java/com/smsreseller/messaging/campaign/CampaignController.java, services/messaging-service/src/main/java/com/smsreseller/messaging/campaign/CampaignStatusView.java, services/messaging-service/src/test/java/com/smsreseller/messaging/DeliveryTrackingIT.java, services/messaging-service/src/test/java/com/smsreseller/messaging/CampaignStatusIT.java</files>
  <read_first>
    - 04-RESEARCH.md OQ#5 (StubSmsProvider @Scheduled DLR simulation; real webhook POST /api/v1/messaging/dlr later delegating to same handler) + §State Machines OutboundMessage (PENDING→SENT→DELIVERED; SENT→FAILED)
    - 04-PATTERNS.md StubSmsProvider DLR rows; OutboundMessageRepository aggregate query
    - 04-RESEARCH.md Test Map MESG-06/07
  </read_first>
  <behavior>
    - DeliveryTrackingIT.perMessageStatusTransitions (MESG-07): a sent message receives a stub DLR after the (shortened) delay → status DELIVERED; GET per-message endpoint reflects it; a DLR-failure transitions SENT→FAILED.
    - CampaignStatusIT.aggregateStatusCountsAreCorrect (MESG-06): after a campaign with mixed outcomes, GET campaign returns sent/delivered/failed counts matching the per-message rows; when all messages reach terminal state campaign becomes COMPLETED.
  </behavior>
  <action>
    DeliveryReceiptService.handleDeliveryReceipt(messageId, status): @Transactional, load OutboundMessage, transition SENT→DELIVERED or SENT→FAILED per the DLR status, update updated_at; recompute campaign terminal state (if all messages terminal → campaign COMPLETED, optionally emit CampaignCompleted outbox event per 04-RESEARCH event contract for Phase 5). Wire StubSmsProvider's @Scheduled DLR sweep (seam from 04-04) to call DeliveryReceiptService.handleDeliveryReceipt with default DELIVERED (configurable). Add a stub webhook endpoint POST /api/v1/messaging/dlr that delegates to the same handler (real DLR later). CampaignController: GET /api/v1/campaigns (history, MESG-06 aggregate counts), GET /api/v1/campaigns/{id} (detail + counts), GET /api/v1/campaigns/{id}/messages (per-message status, MESG-07) — all IDOR-guarded. CampaignStatusView DTO carries sent/delivered/failed/total. OutboundMessageRepository aggregate count query groups by status for a campaign. Convert DeliveryTrackingIT + CampaignStatusIT to real assertions (Awaitility for the DLR delay).
  </action>
  <verify>
    <automated>./gradlew :services:messaging-service:test --tests "DeliveryTrackingIT" --tests "CampaignStatusIT"</automated>
  </verify>
  <acceptance_criteria>
    - perMessageStatusTransitions: a SENT message reaches DELIVERED via the stub DLR; a failure DLR reaches FAILED.
    - aggregateStatusCountsAreCorrect: campaign aggregate sent/delivered/failed counts equal the per-message row counts; campaign COMPLETED once all terminal.
    - DeliveryReceiptService.handleDeliveryReceipt is invoked by both the stub @Scheduled sweep and POST /api/v1/messaging/dlr (grep both call sites).
  </acceptance_criteria>
  <done>Delivery receipts update per-message status; campaign aggregate counts and completion are accurate and queryable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SmsProvider/DLR → messaging | delivery status updates; real webhook later (untrusted) |
| messaging.dead → refund event | permanent failure must refund exactly once |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Denial of Service | retry ladder infinite loop | mitigate | x-delivery-limit(3) + explicit attemptCount advancement + default-requeue-rejected:false → bounded retries then dead (D-06, Pitfall 3). |
| T-04-15 | Tampering | double refund on permanent failure | mitigate | MessageRefundDue carries unique eventId; wallet processed_events dedups (04-07); DeadLetterConsumer is the single emit site. |
| T-04-16 | Spoofing | future real DLR webhook | accept | MVP uses stub; real webhook (POST /api/v1/messaging/dlr) will mirror Azampay callback signature verification when provider is contracted (deferred, 04-VALIDATION manual row). |
</threat_model>

<verification>
- `./gradlew :services:messaging-service:test --tests "DlxRetryIT" --tests "DeliveryTrackingIT" --tests "CampaignStatusIT"` green.
</verification>

<success_criteria>
MESG-06/07/10 satisfied: DLX retry ladder + refund-on-permanent-failure closes the credit
invariant; delivery receipts and per-message + aggregate status give accurate counts.
</success_criteria>

<output>
Create `.planning/phases/04-contacts-messaging/04-06-SUMMARY.md` when done.
</output>
