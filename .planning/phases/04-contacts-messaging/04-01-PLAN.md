---
phase: 04-contacts-messaging
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - services/contact-service/build.gradle.kts
  - services/messaging-service/build.gradle.kts
  - services/contact-service/src/main/resources/application.yml
  - services/messaging-service/src/main/resources/application.yml
  - services/contact-service/src/test/java/com/smsreseller/contact/AbstractContactIntegrationTest.java
  - services/messaging-service/src/test/java/com/smsreseller/messaging/AbstractMessagingIntegrationTest.java
  - services/contact-service/src/test/java/com/smsreseller/contact/ContactCrudIT.java
  - services/contact-service/src/test/java/com/smsreseller/contact/ContactGroupIT.java
  - services/contact-service/src/test/java/com/smsreseller/contact/CsvImportIT.java
  - services/contact-service/src/test/java/com/smsreseller/contact/PhoneNormalizerTest.java
  - services/contact-service/src/test/java/com/smsreseller/contact/SuppressionIT.java
  - services/messaging-service/src/test/java/com/smsreseller/messaging/CampaignIT.java
  - services/messaging-service/src/test/java/com/smsreseller/messaging/SmsEncoderTest.java
  - services/messaging-service/src/test/java/com/smsreseller/messaging/ScheduledCampaignIT.java
  - services/messaging-service/src/test/java/com/smsreseller/messaging/CampaignStatusIT.java
  - services/messaging-service/src/test/java/com/smsreseller/messaging/DeliveryTrackingIT.java
  - services/messaging-service/src/test/java/com/smsreseller/messaging/SendPipelineIT.java
  - services/messaging-service/src/test/java/com/smsreseller/messaging/DlxRetryIT.java
  - services/messaging-service/src/test/java/com/smsreseller/messaging/SenderIdIT.java
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, CONT-06, CONT-07, CONT-08, CONT-09, MESG-01, MESG-02, MESG-03, MESG-04, MESG-05, MESG-06, MESG-07, MESG-08, MESG-09, MESG-10, SNDR-02, SNDR-03, SNDR-04]

must_haves:
  truths:
    - "Both contact-service and messaging-service compile and start with Testcontainers in tests"
    - "Every Phase 4 requirement ID has at least one placeholder integration/unit test that currently fails (Assumptions.abort) so the validation map is non-empty"
    - "commons-csv 1.14.1 and libphonenumber 9.0.32 resolve in contact-service; spring-amqp + resilience4j resolve in messaging-service"
  artifacts:
    - path: "services/contact-service/src/test/java/com/smsreseller/contact/AbstractContactIntegrationTest.java"
      provides: "PG16 @ServiceConnection Testcontainers base for contact-service"
      contains: "PostgreSQLContainer"
    - path: "services/messaging-service/src/test/java/com/smsreseller/messaging/AbstractMessagingIntegrationTest.java"
      provides: "PG16 + RabbitMQ Testcontainers base for messaging-service"
      contains: "RabbitMQContainer"
  key_links:
    - from: "services/contact-service/build.gradle.kts"
      to: "gradle/libs.versions.toml"
      via: "version catalog aliases libs.commons.csv / libs.libphonenumber"
      pattern: "commons.csv|libphonenumber"
---

<objective>
Stand up the Wave 0 test foundation for the two new Phase 4 services. Add the two new
external libraries (commons-csv, libphonenumber) to the Gradle version catalog and wire the
per-service build files; create the Testcontainers integration-test base classes (mirroring
Phase 3's `AbstractWalletIntegrationTest`); and create one placeholder failing test per
requirement so the Nyquist validation map is populated before any production code is written.

Purpose: Establish the RED baseline for every Phase 4 requirement and unblock all parallel
Wave 1 implementation plans.
Output: Buildable modules, two IT base classes, and 13 placeholder test files (one or more
per requirement ID).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contacts-messaging/04-RESEARCH.md
@.planning/phases/04-contacts-messaging/04-PATTERNS.md
@.planning/phases/04-contacts-messaging/04-VALIDATION.md

<interfaces>
Existing version-catalog aliases (gradle/libs.versions.toml) already present and reusable:
- libs.spring.boot.starter.amqp
- libs.spring.boot.testcontainers
- libs.testcontainers.postgresql, libs.testcontainers.rabbitmq, libs.testcontainers.junit.jupiter
- libs.resilience4j.spring.boot3
New aliases to ADD: commons-csv = "1.14.1", libphonenumber = "9.0.32"
  commons-csv module = org.apache.commons:commons-csv
  libphonenumber module = com.googlecode.libphonenumber:libphonenumber

Phase 3 IT base to mirror (read it):
services/wallet-service/src/test/java/com/smsreseller/wallet/AbstractWalletIntegrationTest.java
  — @SpringBootTest(RANDOM_PORT), @ActiveProfiles({"stub","test"}),
    static PostgreSQLContainer("postgres:16") @ServiceConnection,
    static RabbitMQContainer("rabbitmq:3-management") + @DynamicPropertySource.

Existing module skeletons (already in settings.gradle.kts via "services:contact-service",
"services:messaging-service"):
  services/contact-service/build.gradle.kts  (plugin id "spring-boot-service")
  services/messaging-service/build.gradle.kts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version catalog + per-service build deps + base application.yml</name>
  <files>gradle/libs.versions.toml, services/contact-service/build.gradle.kts, services/messaging-service/build.gradle.kts, services/contact-service/src/main/resources/application.yml, services/messaging-service/src/main/resources/application.yml</files>
  <read_first>
    - gradle/libs.versions.toml (modifying — confirm existing alias style)
    - services/contact-service/build.gradle.kts and services/messaging-service/build.gradle.kts (modifying)
    - services/payment-service/build.gradle.kts (analog for amqp + resilience4j + testcontainers deps; 04-PATTERNS.md build.gradle row)
    - services/payment-service/src/main/resources/application.yml (analog for datasource/flyway/resource-server config)
    - 04-RESEARCH.md §"Standard Stack" + §Pattern 1 application.yml block (default-requeue-rejected:false)
  </read_first>
  <action>
    Add two version-catalog library aliases: commons-csv (module org.apache.commons:commons-csv, version 1.14.1) and libphonenumber (module com.googlecode.libphonenumber:libphonenumber, version 9.0.32). To contact-service build.gradle.kts add: libs.commons.csv, libs.libphonenumber, and the Testcontainers PG16 + junit-jupiter + spring-boot-testcontainers test deps. To messaging-service build.gradle.kts add: libs.spring.boot.starter.amqp, libs.resilience4j.spring.boot3, plus Testcontainers PG16 + rabbitmq + junit-jupiter + spring-boot-testcontainers test deps, and shared-security + shared-observability + web + data-jpa + flyway-core + flyway-postgresql (mirror contact-service's existing block). Create base application.yml for both services mirroring payment-service: datasource placeholders, flyway enabled, spring.threads.virtual.enabled:true, resource-server jwt public-key-location pointing at shared-security convention. For messaging-service application.yml additionally set spring.rabbitmq.listener.simple.default-requeue-rejected:false and acknowledge-mode:manual per D-06 and 04-RESEARCH.md Pitfall 1. Provide a test-profile override file (application-test.yml) for messaging-service that shortens the DLX TTL ladder (e.g. app.messaging.retry.ttl-1=2000/ttl-2=4000/ttl-3=6000) so DLX timing tests stay under the 180s feedback budget per 04-VALIDATION.md.
  </action>
  <verify>
    <automated>./gradlew :services:contact-service:dependencies :services:messaging-service:dependencies | grep -E "commons-csv|libphonenumber|amqp|resilience4j" </automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :services:contact-service:compileJava :services:messaging-service:compileJava` succeeds.
    - `grep -v '^#' gradle/libs.versions.toml | grep -c 'libphonenumber'` returns >= 1.
    - messaging-service application.yml contains `default-requeue-rejected: false` (assert: `grep -v '^#' services/messaging-service/src/main/resources/application.yml | grep -c 'default-requeue-rejected'` >= 1).
    - contact-service build resolves commons-csv:1.14.1 and libphonenumber:9.0.32 (dependencies output).
  </acceptance_criteria>
  <done>Both modules compile; new libraries resolve; messaging AMQP listener config present.</done>
</task>

<task type="auto">
  <name>Task 2: Testcontainers IT base classes for both services</name>
  <files>services/contact-service/src/test/java/com/smsreseller/contact/AbstractContactIntegrationTest.java, services/messaging-service/src/test/java/com/smsreseller/messaging/AbstractMessagingIntegrationTest.java</files>
  <read_first>
    - services/wallet-service/src/test/java/com/smsreseller/wallet/AbstractWalletIntegrationTest.java (exact analog, 04-PATTERNS.md base-class row)
    - 04-RESEARCH.md §"Validation Architecture" Wave 0 Gaps list
    - 04-PATTERNS.md note: contact-service base = Postgres only (no AMQP); messaging-service base = Postgres + RabbitMQ; omit Redis for both unless rate-limiting added
  </read_first>
  <action>
    Create AbstractContactIntegrationTest: @SpringBootTest(RANDOM_PORT), @ActiveProfiles({"stub","test"}), a static PostgreSQLContainer("postgres:16") annotated @ServiceConnection with database name contact_test. No RabbitMQ. Create AbstractMessagingIntegrationTest: same shape plus a static RabbitMQContainer("rabbitmq:3-management") wired via @DynamicPropertySource (host/port/username/password) exactly as the wallet base does, database name messaging_test. Both use ddl-auto=create-drop in the test profile (Flyway disabled in tests, matching the Phase 3 decision recorded in 03-02-SUMMARY) so entity annotations drive the test schema.
  </action>
  <verify>
    <automated>./gradlew :services:contact-service:compileTestJava :services:messaging-service:compileTestJava</automated>
  </verify>
  <acceptance_criteria>
    - AbstractContactIntegrationTest contains `PostgreSQLContainer` and NOT `RabbitMQContainer`.
    - AbstractMessagingIntegrationTest contains both `PostgreSQLContainer` and `RabbitMQContainer` and a `@DynamicPropertySource` method registering `spring.rabbitmq.host`.
    - Both test source sets compile.
  </acceptance_criteria>
  <done>Both IT base classes compile and start their containers when extended.</done>
</task>

<task type="auto">
  <name>Task 3: Placeholder failing test per requirement (RED baseline + validation map)</name>
  <files>services/contact-service/src/test/java/com/smsreseller/contact/ContactCrudIT.java, services/contact-service/src/test/java/com/smsreseller/contact/ContactGroupIT.java, services/contact-service/src/test/java/com/smsreseller/contact/CsvImportIT.java, services/contact-service/src/test/java/com/smsreseller/contact/PhoneNormalizerTest.java, services/contact-service/src/test/java/com/smsreseller/contact/SuppressionIT.java, services/messaging-service/src/test/java/com/smsreseller/messaging/CampaignIT.java, services/messaging-service/src/test/java/com/smsreseller/messaging/SmsEncoderTest.java, services/messaging-service/src/test/java/com/smsreseller/messaging/ScheduledCampaignIT.java, services/messaging-service/src/test/java/com/smsreseller/messaging/CampaignStatusIT.java, services/messaging-service/src/test/java/com/smsreseller/messaging/DeliveryTrackingIT.java, services/messaging-service/src/test/java/com/smsreseller/messaging/SendPipelineIT.java, services/messaging-service/src/test/java/com/smsreseller/messaging/DlxRetryIT.java, services/messaging-service/src/test/java/com/smsreseller/messaging/SenderIdIT.java, .planning/phases/04-contacts-messaging/04-VALIDATION.md</files>
  <read_first>
    - 04-RESEARCH.md §"Phase Requirements → Test Map" (exact test class + method names per requirement)
    - 03-01-SUMMARY pattern: placeholder ITs use `org.junit.jupiter.api.Assumptions.abort("Wave 0 placeholder — implement in <plan>")`
    - 04-VALIDATION.md Per-Task Verification Map (to fill)
  </read_first>
  <action>
    Create one test method per requirement using the exact class+method names from 04-RESEARCH.md's Test Map (e.g. ContactCrudIT.addContactPersistsAndIsRetrievable for CONT-01; SmsEncoderTest.partCountAndEncodingDetection for MESG-02; DlxRetryIT.permanentFailureEmitsRefundDueEvent for MESG-10; SenderIdIT.userCanSubmitRequest / adminApproveTransitionsToApproved / senderIdDecidedEventPublished for SNDR-02/03/04). ITs extend the relevant Abstract base; unit tests (PhoneNormalizerTest, SmsEncoderTest) are plain JUnit. Each method body is `Assumptions.abort("Wave 0 placeholder — implement in <implementing plan id>")` referencing the plan that will implement it (04-02/03/04/05/06/08). Then populate the 04-VALIDATION.md Per-Task Verification Map table: one row per requirement with Requirement ID, the implementing plan, wave, the exact `./gradlew ... --tests` command, File Exists=yes, Status=red. Set frontmatter wave_0_complete:true and nyquist_compliant:true.
  </action>
  <verify>
    <automated>./gradlew :services:contact-service:test :services:messaging-service:test --tests "*IT" --tests "*Test" 2>&1 | grep -E "aborted|SKIPPED|placeholder" | head</automated>
  </verify>
  <acceptance_criteria>
    - All 22 requirement IDs (CONT-01..09, MESG-01..10, SNDR-02/03/04) appear as a method in some placeholder file (assert each test-map command resolves to an existing method).
    - Test run reports the placeholder methods as aborted/skipped, none as failed-compilation.
    - 04-VALIDATION.md map table has >= 22 populated rows and frontmatter `nyquist_compliant: true`.
  </acceptance_criteria>
  <done>Every requirement has a named, currently-aborting test; validation map non-empty and marked nyquist_compliant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| build/CI → Maven Central | new third-party artifacts (commons-csv, libphonenumber) pulled at build |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | commons-csv, libphonenumber installs | mitigate | 04-RESEARCH.md Package Legitimacy Audit approved both (Apache + Google, 12+ yrs, Maven Central). Pin exact versions 1.14.1 / 9.0.32 in version catalog; no [ASSUMED]/[SUS]/[SLOP] packages → no blocking checkpoint required. |
</threat_model>

<verification>
- `./gradlew :services:contact-service:compileTestJava :services:messaging-service:compileTestJava` green.
- Placeholder test run shows aborted (not errored) tests for all 22 requirements.
</verification>

<success_criteria>
Both new modules build with their new deps; two Testcontainers bases exist; one aborting
placeholder test exists per Phase 4 requirement; validation map populated and nyquist_compliant.
</success_criteria>

<output>
Create `.planning/phases/04-contacts-messaging/04-01-SUMMARY.md` when done.
</output>
