---
phase: 04-contacts-messaging
plan: 03
type: tdd
wave: 2
depends_on: ["04-02"]
files_modified:
  - services/contact-service/src/main/java/com/opendesk/contact/csv/PhoneNormalizer.java
  - services/contact-service/src/main/java/com/opendesk/contact/csv/CsvImportService.java
  - services/contact-service/src/main/java/com/opendesk/contact/csv/ImportSummaryResponse.java
  - services/contact-service/src/main/java/com/opendesk/contact/csv/CsvImportController.java
  - services/contact-service/src/main/java/com/opendesk/contact/contact/ContactRepository.java
  - services/contact-service/src/test/java/com/opendesk/contact/PhoneNormalizerTest.java
  - services/contact-service/src/test/java/com/opendesk/contact/CsvImportIT.java
autonomous: true
requirements: [CONT-05, CONT-06, CONT-07, CONT-09]

must_haves:
  truths:
    - "A user can upload a CSV; valid rows are inserted as contacts with phone numbers normalized to E.164"
    - "07xx / 06xx Tanzanian numbers normalize to +255XXXXXXXXX; +255 numbers pass through; invalid numbers are rejected"
    - "Duplicate phone numbers (within the file or already in the account) are skipped, not inserted twice"
    - "The import returns a summary: counts of imported / duplicates skipped / invalid"
  artifacts:
    - path: "services/contact-service/src/main/java/com/opendesk/contact/csv/PhoneNormalizer.java"
      provides: "libphonenumber E.164 normalization for TZ"
      contains: "PhoneNumberUtil"
    - path: "services/contact-service/src/main/java/com/opendesk/contact/csv/ImportSummaryResponse.java"
      provides: "imported/duplicates/invalid counts DTO"
      contains: "invalid"
  key_links:
    - from: "services/contact-service/src/main/java/com/opendesk/contact/csv/CsvImportService.java"
      to: "services/contact-service/src/main/java/com/opendesk/contact/csv/PhoneNormalizer.java"
      via: "normalizeToE164 per row"
      pattern: "normalizeToE164"
    - from: "services/contact-service/src/main/java/com/opendesk/contact/csv/CsvImportService.java"
      to: "ContactRepository.insertIfAbsent"
      via: "ON CONFLICT DO NOTHING dedup on (user_id, phone_e164)"
      pattern: "insertIfAbsent|ON CONFLICT"
---

<objective>
Add CSV contact import to contact-service: stream the uploaded file with commons-csv, normalize
each Tanzanian phone number to E.164 via libphonenumber, deduplicate against the existing
account (and within the file), and return an import summary. Delivers CONT-05 (CSV import),
CONT-06 (dedup), CONT-07 (E.164 normalization), CONT-09 (import summary).

Purpose: Bulk contact onboarding is the primary way small orgs load their member lists.
Output: POST /api/v1/contacts/import endpoint returning ImportSummaryResponse.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/04-contacts-messaging/04-RESEARCH.md
@.planning/phases/04-contacts-messaging/04-PATTERNS.md

<interfaces>
From 04-02: ContactRepository, Contact entity, unique (user_id, phone_e164).
Add to ContactRepository:
  int insertIfAbsent(UUID userId, String name, String phoneE164);  // native INSERT ... ON CONFLICT (user_id, phone_e164) DO NOTHING; returns rows affected
PhoneNormalizer (new): String normalizeToE164(String raw) throws on invalid TZ number.
ImportSummaryResponse(int imported, int duplicates, int invalid).
</interfaces>
</context>

<feature>
  <name>CSV import with E.164 normalization, dedup, and summary</name>
  <files>PhoneNormalizer.java, CsvImportService.java, ImportSummaryResponse.java, CsvImportController.java, ContactRepository.java (add insertIfAbsent)</files>
  <read_first>
    - 04-RESEARCH.md §Pattern 4 (PhoneNumberUtil.parse(raw,"TZ") + isValidNumber + format E164) and §Pattern 5 (CSVFormat.DEFAULT.withFirstRecordAsHeader streaming)
    - 04-PATTERNS.md "No Analog Found" rows for PhoneNormalizer + CsvImportService (use RESEARCH patterns directly)
    - 04-RESEARCH.md Test Map CONT-05/06/07/09; Pitfall 5 (virtual threads make sync streaming acceptable at MVP)
    - services/contact-service/.../contact/ContactController.java (IDOR pattern for the import endpoint)
  </read_first>
  <behavior>
    - PhoneNormalizerTest.tanzanianMobileNormalizesToE164 (CONT-07): "0712345678" → "+255712345678"; "+255712345678" → "+255712345678"; "0612345678" → "+255612345678"; an invalid string (e.g. "12") throws.
    - CsvImportIT.csvUploadInsertsContacts (CONT-05): upload a 3-valid-row CSV (header name,phone) → 3 contacts persisted, normalized.
    - CsvImportIT.duplicatePhoneSkippedAndCounted (CONT-06): file with the same phone twice (and one already in DB) → inserted once, duplicate count reflects the skips.
    - CsvImportIT.importSummaryCountsAreCorrect (CONT-09): mixed file (valid + duplicate + invalid rows) → ImportSummaryResponse with exact imported/duplicates/invalid counts; invalid rows are NOT inserted and do NOT abort the import.
  </behavior>
  <implementation>
    RED: convert PhoneNormalizerTest and the three CsvImportIT methods from Assumptions.abort to real assertions; run to confirm failure (classes/endpoint absent).
    GREEN: PhoneNormalizer wraps PhoneNumberUtil.getInstance(); normalizeToE164 parses with region "TZ", throws if !isValidNumber, returns format E164. Add ContactRepository.insertIfAbsent as a native @Modifying query INSERT INTO contacts(id,user_id,name,phone_e164,created_at,updated_at) VALUES (gen_random_uuid()/param, ...) ON CONFLICT (user_id, phone_e164) DO NOTHING returning rows affected. CsvImportService.importCsv(userId, InputStream): stream with CSVFormat.DEFAULT.withFirstRecordAsHeader, per row read name+phone; try normalize then insertIfAbsent — increment imported when rows-affected==1, duplicates when ==0, invalid on normalize/parse exception (log row number + raw value, never abort). Track an in-memory Set of E.164 already seen in this file to count intra-file duplicates consistently. Return ImportSummaryResponse. CsvImportController: POST /api/v1/contacts/import, @RequestParam MultipartFile file, userId from JWT subject, returns ImportSummaryResponse. REFACTOR: extract row-parse into a small private method if needed.
    Treat all CSV cell values strictly as strings (CSV-injection threat T-04-CSV): do not evaluate formula-prefixed values.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user-uploaded CSV → contact-service | dirty, attacker-controlled file content |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-CSV | Tampering | CsvImportService | mitigate | All cell values handled as opaque strings; phone validated by libphonenumber; name not interpreted; no Excel-formula evaluation (04-RESEARCH.md V5 / CSV injection row). |
| T-04-03 | Denial of Service | large CSV on request thread | accept | Virtual threads enabled (Pitfall 5); MVP target is small orgs; async job deferred post-launch. |
| T-04-04 | Elevation of privilege | import endpoint | mitigate | userId from JWT subject only; contacts inserted under that user. |
</threat_model>

<verification>
- `./gradlew :services:contact-service:test --tests "PhoneNormalizerTest" --tests "CsvImportIT"` green.
</verification>

<success_criteria>
CONT-05/06/07/09 satisfied: CSV import normalizes to E.164, dedups within file and against the
account, reports a correct import summary, and never aborts on invalid rows.
</success_criteria>

<output>
Create `.planning/phases/04-contacts-messaging/04-03-SUMMARY.md` when done.
</output>
