---
phase: 05-notifications-admin-analytics
plan: 05
type: tdd
wave: 1
depends_on: [05-01]
files_modified:
  - services/payment-service/src/main/java/com/opendesk/payment/bundle/AdminBundleController.java
  - services/payment-service/src/main/java/com/opendesk/payment/bundle/BundleSaveRequest.java
  - services/payment-service/src/main/java/com/opendesk/payment/bundle/BundleAdminService.java
  - services/payment-service/src/main/java/com/opendesk/payment/config/SecurityConfig.java
autonomous: true
requirements: [ADMN-07]

must_haves:
  truths:
    - "A ROLE_ADMIN caller can list, create, update, and delete SMS bundles via /api/v1/admin/bundles"
    - "Bundle price and SMS count are rejected when <= 0 (validation)"
    - "A ROLE_USER token cannot mutate bundles (403); the existing public read catalog is unaffected"
  artifacts:
    - path: "services/payment-service/src/main/java/com/opendesk/payment/bundle/AdminBundleController.java"
      provides: "ADMN-07 bundle CRUD"
      contains: "/api/v1/admin/bundles"
    - path: "services/payment-service/src/main/java/com/opendesk/payment/bundle/BundleSaveRequest.java"
      provides: "validated create/update DTO"
      contains: "@Positive"
  key_links:
    - from: "AdminBundleController"
      to: "BundleRepository (existing)"
      via: "ADMIN-guarded CRUD on sms_bundles"
      pattern: "BundleRepository"

user_setup: []
---

<objective>
Add ADMIN-guarded CRUD over the existing SMS bundle catalog in payment-service (ADMN-07).

Purpose: D-08/D-14 — bundle management is owned by payment-service, which already owns the
sms_bundles table and the public read API. Phase 5 wraps it with ROLE_ADMIN create/update/delete +
validation, leaving the customer-facing read catalog (PYMT-01) untouched.

Output: AdminBundleController CRUD; validated BundleSaveRequest; admin path in SecurityConfig.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-notifications-admin-analytics/05-RESEARCH.md
@.planning/phases/05-notifications-admin-analytics/05-PATTERNS.md
@.planning/phases/03-wallet-payments/03-03-SUMMARY.md

<interfaces>
Existing (reuse, do not duplicate the read catalog):
  services/payment-service/.../bundle/BundleController.java — public GET catalog (PYMT-01)
  services/payment-service/.../bundle/BundleRepository.java — findAll, save, deleteById
  services/payment-service/.../bundle/SmsBundle.java — entity (name, smsCount, priceTzs BIGINT, active)
  services/payment-service/.../bundle/BundleDto.java — response DTO (BundleDto.from)
Controller analog (exact): 05-PATTERNS.md §AdminBundleController (lines 320–349)
SecurityConfig analog: 05-PATTERNS.md §SecurityConfig.
UI-SPEC §Bundle Catalog (fields: Name, SMS Count min=1, Price TZS min=1, Active; formatted price).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: AdminBundleController CRUD + validated DTO + SecurityConfig admin path</name>
  <files>services/payment-service/src/main/java/com/opendesk/payment/bundle/AdminBundleController.java, services/payment-service/src/main/java/com/opendesk/payment/bundle/BundleSaveRequest.java, services/payment-service/src/main/java/com/opendesk/payment/bundle/BundleAdminService.java, services/payment-service/src/main/java/com/opendesk/payment/config/SecurityConfig.java</files>
  <read_first>
    - 05-PATTERNS.md §AdminBundleController (lines 320–349 — list/create/update/delete on BundleRepository)
    - services/payment-service/.../bundle/BundleController.java + BundleRepository.java + SmsBundle.java + BundleDto.java
    - services/payment-service/.../config/SecurityConfig.java (existing chain; public catalog read must remain permitted)
    - 05-RESEARCH.md §Security Domain (bundle price <=0 → Tampering; @Positive/@Min(1))
    - UI-SPEC §Bundle Catalog (field constraints + Active toggle)
  </read_first>
  <behavior>
    - GET /api/v1/admin/bundles (ROLE_ADMIN) lists all bundles incl. inactive
    - POST creates a bundle; PUT /{id} updates name/smsCount/priceTzs/active; DELETE /{id} removes it
    - smsCount or priceTzs <= 0 → 400 (validation), no row written
    - ROLE_USER → 403 on any /api/v1/admin/bundles call; existing public GET catalog still works
  </behavior>
  <action>Create BundleSaveRequest (name @NotBlank; smsCount @Min(1); priceTzs @Positive long — TZS integer per CLAUDE.md; active boolean). Create BundleAdminService wrapping BundleRepository for create/update/delete (404 when updating/deleting a missing id, via the PATTERNS error-handling try/catch). Create AdminBundleController @RequestMapping("/api/v1/admin/bundles") with list/create/update/delete returning BundleDto. Extend payment-service SecurityConfig: `/api/v1/admin/**` (or `/api/v1/admin/bundles/**`) → hasRole("ADMIN") using the roles-claim jwtAuthenticationConverter; keep the public catalog GET path permitted exactly as before. Make AdminBundleCatalogIT GREEN. RED→GREEN→REFACTOR.</action>
  <verify>
    <automated>./gradlew :services:payment-service:test --tests "*AdminBundleCatalogIT*"</automated>
  </verify>
  <acceptance_criteria>
    - AdminBundleCatalogIT passes: admin CRUD round-trip works; price/smsCount <=0 → 400; user token → 403
    - BundleSaveRequest contains `@Positive` / `@Min(1)` on price and smsCount
    - SecurityConfig admin bundle path is hasRole("ADMIN"); public catalog GET still permitted
  </acceptance_criteria>
  <done>Operators can manage the bundle catalog; validation blocks non-positive pricing; customer read API unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client JWT → /api/v1/admin/bundles | Privileged catalog mutation affecting all customers' pricing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Elevation of Privilege | bundle mutation with user token | mitigate | hasRole("ADMIN") on /api/v1/admin/bundles |
| T-05-13 | Tampering | price/smsCount set to 0 or negative | mitigate | @Positive / @Min(1) on BundleSaveRequest |
</threat_model>

<verification>
- Admin CRUD round-trip; validation enforced; user blocked; public catalog intact.
</verification>

<success_criteria>
- `./gradlew :services:payment-service:test` green for AdminBundleCatalogIT
- /api/v1/admin/bundles ADMIN-gated; non-positive pricing rejected
</success_criteria>

<output>
Create `.planning/phases/05-notifications-admin-analytics/05-05-SUMMARY.md` when done.
</output>
