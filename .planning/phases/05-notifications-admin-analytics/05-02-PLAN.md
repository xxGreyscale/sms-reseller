---
phase: 05-notifications-admin-analytics
plan: 02
type: tdd
wave: 1
depends_on: [05-01]
files_modified:
  - services/messaging-service/src/main/resources/db/migration/V6__add_operator_to_outbound_messages.sql
  - services/messaging-service/src/main/java/com/smsreseller/messaging/message/OutboundMessage.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/message/CarrierResolver.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/message/DeliveryReceiptService.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/message/OutboundMessageRepository.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/analytics/CampaignAnalyticsController.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/analytics/CampaignAnalyticsService.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/analytics/CampaignStatsDto.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/analytics/OperatorRateDto.java
  - services/messaging-service/src/main/java/com/smsreseller/messaging/config/SecurityConfig.java
autonomous: true
requirements: [NOTF-05, ANLX-01, ANLX-03]

must_haves:
  truths:
    - "When a campaign's messages all reach terminal state, messaging-service emits a CampaignCompleted outbox event on messaging.events with delivered/failed counts"
    - "Each OutboundMessage stores the derived TZ operator (Vodacom/Tigo/Airtel/Halotel/unknown) from its E.164 number"
    - "Owner can GET campaign delivery stats (sent/delivered/failed) for their own campaign, scoped to JWT subject"
    - "Owner can GET operator-level delivery rates grouped by operator+status, scoped to JWT subject"
  artifacts:
    - path: "services/messaging-service/src/main/resources/db/migration/V6__add_operator_to_outbound_messages.sql"
      provides: "operator column + backfill"
      contains: "ALTER TABLE outbound_messages"
    - path: "services/messaging-service/src/main/java/com/smsreseller/messaging/analytics/CampaignAnalyticsController.java"
      provides: "ANLX-01/03 endpoints"
      contains: "/api/v1/analytics"
  key_links:
    - from: "DeliveryReceiptService.checkCampaignCompletion"
      to: "messaging.events / messaging.CampaignCompleted"
      via: "OutboxEntry save in same transaction"
      pattern: "CampaignCompleted"
    - from: "CampaignAnalyticsController"
      to: "OutboundMessageRepository aggregate queries"
      via: "JWT-subject-scoped service call"
      pattern: "findOperatorRatesByUser"

user_setup: []
---

<objective>
Close the two messaging-service gaps that Phase 5 depends on: D-12 (no CampaignCompleted event)
and D-13 (no operator column for ANLX-03), and expose the campaign + operator analytics read APIs.

Purpose: NOTF-05 cannot be wired until messaging-service emits CampaignCompleted. ANLX-03 cannot
be implemented without a stored operator value to GROUP BY. Both are blocking prerequisites for
the notification-service (05-06) and the customer analytics surface.

Output: V6 migration adding operator; CarrierResolver (libphonenumber); CampaignCompleted outbox
emit; ANLX-01/03 analytics endpoints scoped to JWT subject.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-notifications-admin-analytics/05-RESEARCH.md
@.planning/phases/05-notifications-admin-analytics/05-PATTERNS.md
@.planning/phases/04-contacts-messaging/04-06-SUMMARY.md

<interfaces>
Existing entity (add `operator` field):
  services/messaging-service/.../message/OutboundMessage.java
    - fields: id, campaignId, userId, phoneE164, lotId, status (MessageStatus), externalId, createdAt, updatedAt
Existing service to modify:
  services/messaging-service/.../message/DeliveryReceiptService.java#checkCampaignCompletion(UUID) — sets COMPLETED, NO outbox emit (lines 75–93)
Existing outbox machinery (reuse):
  services/messaging-service/.../outbox/OutboxEntry.java (builder: id, eventId, aggregateType, aggregateId, eventType, payload)
  services/messaging-service/.../outbox/OutboxRelay.java (@Scheduled → publishes to messaging.events / messaging.<eventType>)
libphonenumber already a messaging-service dependency (Phase 4, CONT-07).
CampaignCompleted payload (RESEARCH.md): { eventId, campaignId, userId, totalCount, deliveredCount, failedCount }
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: operator column + CarrierResolver, populated at dispatch and backfilled</name>
  <files>services/messaging-service/src/main/resources/db/migration/V6__add_operator_to_outbound_messages.sql, services/messaging-service/src/main/java/com/smsreseller/messaging/message/OutboundMessage.java, services/messaging-service/src/main/java/com/smsreseller/messaging/message/CarrierResolver.java</files>
  <read_first>
    - services/messaging-service/.../message/OutboundMessage.java (entity to extend)
    - 05-CONTEXT.md D-13 (carrier derivation from E.164, libphonenumber, store-vs-compute)
    - Phase 4 CSV normalization code using libphonenumber (CONT-07) for the PhoneNumberUtil usage pattern — grep messaging/contact for `PhoneNumberUtil`
    - services/messaging-service/.../V1__create_campaigns.sql (migration style analog)
  </read_first>
  <behavior>
    - CarrierResolver.resolve("+255712345678") returns a non-null TZ operator label (store-at-write decision per D-13)
    - CarrierResolver.resolve(malformed) returns "UNKNOWN" (never throws — DLR/analytics must not break on bad data)
    - OutboundMessage persists and reads back the operator value
  </behavior>
  <action>Add `operator VARCHAR(50)` (nullable) to outbound_messages via V6 Flyway migration; include a one-time backfill UPDATE deriving operator from phone_e164 using a CASE on the TZ MNO prefix ranges (Vodacom/M-Pesa, Tigo, Airtel, Halotel) so existing rows are not NULL for ANLX-03. Add the `operator` field to OutboundMessage entity (@Column name="operator"). Create CarrierResolver using libphonenumber's carrier mapping (PhoneNumberToCarrierMapper) with a TZ-prefix fallback table; return "UNKNOWN" on parse failure — store-at-write per D-13. Wire CarrierResolver into the send pipeline at OutboundMessage creation (set operator from phoneE164) — locate the creation site via grep for `OutboundMessage.builder()`; do not change reservation/credit logic. Follow RED→GREEN→REFACTOR: write the CarrierResolver unit test + an entity persistence assertion first.</action>
  <verify>
    <automated>./gradlew :services:messaging-service:test --tests "*CarrierResolver*" --tests "*OperatorRateAnalyticsIT*"</automated>
  </verify>
  <acceptance_criteria>
    - V6 migration file contains `ALTER TABLE outbound_messages` and a backfill `UPDATE` (grep -v '^--' | grep -c UPDATE >= 1)
    - CarrierResolver unit test passes (valid TZ number → operator label; malformed → "UNKNOWN")
    - OutboundMessage has an `operator` column mapping
  </acceptance_criteria>
  <done>operator stored at write + backfilled; CarrierResolver never throws.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: CampaignCompleted outbox emit (D-12 gap fix)</name>
  <files>services/messaging-service/src/main/java/com/smsreseller/messaging/message/DeliveryReceiptService.java</files>
  <read_first>
    - services/messaging-service/.../message/DeliveryReceiptService.java (checkCampaignCompletion, lines 75–93)
    - 05-PATTERNS.md §DeliveryReceiptService modification (lines 361–402 — exact emit pattern + payload)
    - services/messaging-service/.../outbox/OutboxEntry.java + OutboxRelay.java (outbox contract)
    - 05-RESEARCH.md Pitfall 4 (CampaignCompleted missing → NOTF-05 dead)
  </read_first>
  <behavior>
    - When the last message of a campaign reaches terminal state, exactly one CampaignCompleted outbox row is written with totalCount/deliveredCount/failedCount
    - Re-entry (a late duplicate DLR after COMPLETED) does NOT write a second CampaignCompleted row (guarded by existing `campaign.getStatus() != COMPLETED` check)
    - Outbox row carries aggregateType=Campaign, aggregateId=campaignId, eventType=CampaignCompleted, routed to messaging.events/messaging.CampaignCompleted by OutboxRelay
  </behavior>
  <action>Inside checkCampaignCompletion, after the existing campaign.setStatus(COMPLETED)+save+log.info, compute delivered/failed counts from the message list and write an OutboxEntry (eventId=random UUID, aggregateType="Campaign", aggregateId=campaignId, eventType="CampaignCompleted", payload={eventId, campaignId, userId, totalCount, deliveredCount, failedCount}) within the same @Transactional method so OutboxRelay publishes it to messaging.events. Keep the emit inside the existing `if (status != COMPLETED)` guard so it fires exactly once. Make the CampaignCompletedIT (Wave 0 RED) GREEN. RED→GREEN→REFACTOR.</action>
  <verify>
    <automated>./gradlew :services:messaging-service:test --tests "*CampaignCompletedIT*"</automated>
  </verify>
  <acceptance_criteria>
    - CampaignCompletedIT passes: completing a campaign produces one messaging.events / messaging.CampaignCompleted message with delivered+failed counts
    - DeliveryReceiptService source contains `eventType` "CampaignCompleted" within the COMPLETED guard
    - Duplicate completion attempt asserts a single outbox row
  </acceptance_criteria>
  <done>messaging-service emits CampaignCompleted exactly once — NOTF-05 prerequisite met.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: ANLX-01 campaign stats + ANLX-03 operator-rate analytics endpoints</name>
  <files>services/messaging-service/src/main/java/com/smsreseller/messaging/analytics/CampaignAnalyticsController.java, services/messaging-service/src/main/java/com/smsreseller/messaging/analytics/CampaignAnalyticsService.java, services/messaging-service/src/main/java/com/smsreseller/messaging/analytics/CampaignStatsDto.java, services/messaging-service/src/main/java/com/smsreseller/messaging/analytics/OperatorRateDto.java, services/messaging-service/src/main/java/com/smsreseller/messaging/message/OutboundMessageRepository.java, services/messaging-service/src/main/java/com/smsreseller/messaging/config/SecurityConfig.java</files>
  <read_first>
    - 05-PATTERNS.md §Analytics controllers (lines 507–550 — JWT-scoped controller + JPQL aggregate)
    - services/payment-service/.../bundle/BundleController.java (controller analog)
    - services/messaging-service/.../config/SecurityConfig.java (path matcher to add `/api/v1/analytics/**` authenticated)
    - 05-RESEARCH.md Pitfall 3 (analytics endpoints are JWT-subject-scoped, NOT admin)
  </read_first>
  <behavior>
    - GET /api/v1/analytics/campaigns/{id}/stats returns {sent, delivered, failed} for a campaign owned by the JWT subject
    - Requesting another user's campaign returns 404/403 (IDOR guard via userId scope), never another user's data
    - GET /api/v1/analytics/operator-rates returns rows grouped by operator+status for the JWT subject only
  </behavior>
  <action>Add OperatorRateRow projection + findOperatorRatesByUser JPQL (GROUP BY m.operator, m.status WHERE m.userId=:userId) to OutboundMessageRepository, reusing the existing aggregate count query (MESG-06) for campaign stats scoped by userId. Create CampaignAnalyticsService composing these into CampaignStatsDto and List<OperatorRateDto>. Create CampaignAnalyticsController under /api/v1/analytics injecting JwtAuthenticationToken (method injection, not SecurityContextHolder — virtual threads) and deriving userId from auth.getToken().getSubject(). Add `/api/v1/analytics/**` → authenticated() to SecurityConfig (subject-scoped, NOT hasRole ADMIN). Make CampaignAnalyticsIT + OperatorRateAnalyticsIT GREEN. RED→GREEN→REFACTOR.</action>
  <verify>
    <automated>./gradlew :services:messaging-service:test --tests "*CampaignAnalyticsIT*" --tests "*OperatorRateAnalyticsIT*"</automated>
  </verify>
  <acceptance_criteria>
    - CampaignAnalyticsIT + OperatorRateAnalyticsIT pass
    - Controller derives userId from JWT subject (source contains `getSubject()`), no cross-service join
    - SecurityConfig maps `/api/v1/analytics/**` to authenticated (not hasRole ADMIN)
  </acceptance_criteria>
  <done>ANLX-01 + ANLX-03 served as JWT-scoped read APIs with no IDOR exposure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client JWT → /api/v1/analytics/** | Authenticated user requests; must see only their own data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | CampaignAnalyticsController (IDOR) | mitigate | All queries scoped to auth.getToken().getSubject(); other-user campaign → 404 |
| T-05-03 | Tampering | CampaignCompleted duplicate emit | mitigate | Emit inside existing `status != COMPLETED` guard; consumer idempotent in 05-06 |
| T-05-04 | Denial of Service | CarrierResolver on malformed E.164 | mitigate | Returns "UNKNOWN" instead of throwing — analytics/DLR never break |
</threat_model>

<verification>
- CampaignCompleted emitted exactly once per campaign on completion.
- operator stored + backfilled; CarrierResolver robust.
- ANLX-01/03 endpoints return owner-scoped aggregates.
</verification>

<success_criteria>
- `./gradlew :services:messaging-service:test` green for CampaignCompletedIT, CampaignAnalyticsIT, OperatorRateAnalyticsIT, CarrierResolver tests
- V6 migration adds + backfills operator
- No cross-service joins; analytics scoped to JWT subject
</success_criteria>

<output>
Create `.planning/phases/05-notifications-admin-analytics/05-02-SUMMARY.md` when done.
</output>
