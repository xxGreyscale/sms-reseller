---
phase: 05-notifications-admin-analytics
plan: 08
type: tdd
wave: 3
depends_on: [05-01, 05-03, 05-04]
files_modified:
  - apps/admin-web/lib/api.ts
  - apps/admin-web/lib/auth.ts
  - apps/admin-web/middleware.ts
  - apps/admin-web/app/(auth)/login/page.tsx
  - apps/admin-web/app/(auth)/login/actions.ts
  - apps/admin-web/app/(admin)/layout.tsx
  - apps/admin-web/app/(admin)/users/page.tsx
  - apps/admin-web/app/(admin)/users/UserSearch.tsx
  - apps/admin-web/app/(admin)/ledger/[userId]/page.tsx
  - apps/admin-web/components/AppSidebar.tsx
  - apps/admin-web/lib/format.ts
  - apps/admin-web/lib/format.test.ts
  - apps/admin-web/app/(auth)/login/actions.test.ts
  - apps/admin-web/e2e/auth.spec.ts
  - apps/admin-web/e2e/user-search.spec.ts
autonomous: true
requirements: [ADMN-01, ADMN-02, ADMN-03]

must_haves:
  truths:
    - "An operator can sign in at /login via a Server Action that stores the admin JWT in an httpOnly cookie and is redirected to /sender-ids"
    - "middleware.ts redirects any request to an admin route without a valid admin_token cookie to /login"
    - "The admin shell renders the 6-item sidebar nav (Users, Ledger, Sender IDs, Refunds, Bundle Catalog, Audit Log) per UI-SPEC"
    - "User search lists matching users and links to a per-user ledger inspection page"
  artifacts:
    - path: "apps/admin-web/middleware.ts"
      provides: "ADMN-01 route protection"
      contains: "admin_token"
    - path: "apps/admin-web/app/(auth)/login/actions.ts"
      provides: "httpOnly cookie login Server Action"
      contains: "httpOnly"
    - path: "apps/admin-web/app/(admin)/users/page.tsx"
      provides: "ADMN-02 user search screen"
  key_links:
    - from: "login/actions.ts"
      to: "identity-service POST /api/v1/auth/admin/login"
      via: "Server Action fetch + cookies().set"
      pattern: "/auth/admin/login"
    - from: "ledger/[userId]/page.tsx"
      to: "wallet-service GET /api/v1/admin/ledger/{userId}"
      via: "Server Component fetch with Bearer cookie token"
      pattern: "/api/v1/admin/ledger"

user_setup: []
---

<objective>
Build the admin-web authentication shell and the two read screens (user search + ledger inspection).

Purpose: D-10/D-11 — first Next.js app; httpOnly cookie admin JWT, middleware route protection,
Server Actions for mutations, Server Components for reads. Establishes the shared shell, lib/api,
and lib/auth that the action screens (05-09) build on. Covers ADMN-01 (login UI), ADMN-02, ADMN-03 UI.

Output: lib/api + lib/auth + format util; middleware; login page + Server Action; admin layout +
sidebar; users + ledger screens; Vitest unit tests + Playwright auth/search E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-notifications-admin-analytics/05-RESEARCH.md
@.planning/phases/05-notifications-admin-analytics/05-UI-SPEC.md
@.planning/phases/05-notifications-admin-analytics/05-PATTERNS.md

<interfaces>
NOVEL files — no codebase analog. Use RESEARCH.md patterns + UI-SPEC contract:
  - RESEARCH.md Pattern 2 (httpOnly cookie login Server Action, lines 323–360) — maxAge 60*60, sameSite lax, secure in prod
  - RESEARCH.md Pattern 2 (middleware.ts edge protection, matcher ['/(admin)/:path*'])
  - RESEARCH.md Pattern 3 (Server Component calling backend with Bearer cookie, cache:'no-store')
  - RESEARCH.md Pitfall 2 (cookies().set only in Server Action/Route Handler) + Pitfall 5 (no async-RSC Vitest → Playwright)
Backend endpoints (from 05-03 / 05-04):
  POST /api/v1/auth/admin/login → { accessToken }
  GET  /api/v1/admin/users?q=&page=&size= → Page<UserSummaryDto{id,fullName,email,phone,verificationStatus,createdAt}>
  GET  /api/v1/admin/ledger/{userId}?page=&size= → Page<LedgerEntryDto{date,type,description,delta,balanceAfter}>
UI-SPEC bindings:
  - §Layout (240px sidebar, 56px top bar, 6 nav items, default redirect /sender-ids)
  - §Login (centered 400px card, "Sign in", error copy, redirect target)
  - §User Search (columns, "Search Users", empty states, Pagination 20/page)
  - §Ledger Inspection (Back link, user summary Card, columns, ScrollArea, empty state)
  - §Color (zinc base, accent zinc-900, amber-700 for warning text) / §Typography (14/12/18/24, Inter)
  - shadcn components installed in 05-01: button input table badge dialog form label separator card skeleton scroll-area select pagination sonner
middleware.test.ts placeholder RED test created in 05-01 — make it GREEN here.
BACKEND_URL env consumed by lib/api.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: auth foundation — lib/api, lib/auth, middleware, login page + Server Action</name>
  <files>apps/admin-web/lib/api.ts, apps/admin-web/lib/auth.ts, apps/admin-web/lib/format.ts, apps/admin-web/lib/format.test.ts, apps/admin-web/middleware.ts, apps/admin-web/app/(auth)/login/page.tsx, apps/admin-web/app/(auth)/login/actions.ts, apps/admin-web/app/(auth)/login/actions.test.ts</files>
  <read_first>
    - RESEARCH.md Pattern 2 (login Server Action + middleware) — lines 323–360
    - RESEARCH.md Pitfall 2 (cookie mutation scope) + Pitfall 5 (Vitest scope)
    - UI-SPEC §Login (card 400px, fields, "Sign in", error copy "Invalid email or password…", redirect /sender-ids, auth-wall away from /login)
    - apps/admin-web/middleware.test.ts (Wave 0 RED placeholder to satisfy)
  </read_first>
  <behavior>
    - middleware: request to an admin route with no admin_token cookie → redirect to /login; with cookie present → next() (satisfies middleware.test.ts)
    - adminLogin Server Action: on backend 200 sets httpOnly admin_token cookie (sameSite lax, secure in prod, maxAge 3600) and redirects to /sender-ids; on non-200 returns { error } with the UI-SPEC copy
    - format.formatTzs(12000) returns "12,000 TZS" (used by ledger/bundles); format util unit-tested
  </behavior>
  <action>Create lib/api.ts (typed fetch wrapper reading BACKEND_URL, attaching Bearer from the admin_token cookie for server-side calls, cache:'no-store', throwing typed errors on non-2xx). Create lib/auth.ts (cookie read helper getToken()). Create lib/format.ts (formatTzs with comma grouping + "TZS" suffix; relative-date helper) + format.test.ts (Vitest unit). Create middleware.ts per RESEARCH.md Pattern 2 with matcher for the (admin) group, redirecting unauthenticated requests to /login and bouncing authenticated requests away from /login — make middleware.test.ts GREEN. Create app/(auth)/login/page.tsx (centered 400px Card, Email/Password inputs with autocomplete, full-width "Sign in" Button, inline error area) and actions.ts ('use server' adminLogin posting to /api/v1/auth/admin/login, cookies().set per Pattern 2, redirect('/sender-ids')) + actions.test.ts (Vitest: mock fetch, assert cookie set on 200 / error returned on 401). RED→GREEN→REFACTOR for format + actions + middleware unit tests.</action>
  <verify>
    <automated>cd apps/admin-web && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run` passes including middleware.test.ts, format.test.ts, actions.test.ts
    - actions.ts contains `httpOnly` and posts to `/api/v1/auth/admin/login`
    - middleware.ts references `admin_token` and redirects to `/login`
  </acceptance_criteria>
  <done>Operators can authenticate; cookie-based route protection enforced; util tested.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: admin shell + sidebar + user search + ledger screens + Playwright E2E</name>
  <files>apps/admin-web/app/(admin)/layout.tsx, apps/admin-web/components/AppSidebar.tsx, apps/admin-web/app/(admin)/users/page.tsx, apps/admin-web/app/(admin)/users/UserSearch.tsx, apps/admin-web/app/(admin)/ledger/[userId]/page.tsx, apps/admin-web/e2e/auth.spec.ts, apps/admin-web/e2e/user-search.spec.ts</files>
  <read_first>
    - RESEARCH.md Pattern 3 (Server Component fetch with Bearer cookie)
    - UI-SPEC §Layout (240px sidebar, 6 nav items, 56px top bar, default redirect) + §User Search + §Ledger Inspection + §State Patterns (Skeleton, empty states) + §Color + §Typography
    - RESEARCH.md Pitfall 5 (test async Server Components with Playwright, not Vitest)
  </read_first>
  <behavior>
    - The (admin) layout renders AppSidebar with exactly 6 nav items (Users, Ledger, Sender IDs, Refunds, Bundle Catalog, Audit Log) + admin email/logout at bottom, and a 56px top bar
    - /users: search Input + "Search Users" Button; results Table with UI-SPEC columns + status Badge + "View Ledger" link; empty states ("Search for a user" / "No users found") + 20/page Pagination
    - /ledger/[userId]: "← Back to users" link, user summary Card, transaction Table (Date/Type/Description/Delta colored/Balance After) in a ScrollArea; "No transactions" empty state
    - Playwright: login flow redirects to /sender-ids; user search finds a user by email (against a running stack or mocked backend route)
  </behavior>
  <action>Create app/(admin)/layout.tsx as the shell (two-column: 240px AppSidebar + fluid main with 56px top bar, 32px top / 24px horizontal padding) using shadcn Separator + zinc tokens. Create components/AppSidebar.tsx with the 6 nav items + active-state accent (zinc-900) + admin email/logout footer. Create app/(admin)/users/page.tsx (async Server Component: read q from searchParams, fetch /api/v1/admin/users via lib/api, render Table + Pagination + empty states; Skeleton while loading) and a UserSearch.tsx client component for the search box. Create app/(admin)/ledger/[userId]/page.tsx (Server Component fetching /api/v1/admin/ledger/{userId}, user summary Card, Table in ScrollArea with formatTzs deltas/colors). Use only shadcn components from the installed inventory; apply UI-SPEC color/type/spacing tokens and exact copy. Create e2e/auth.spec.ts (login → /sender-ids) + e2e/user-search.spec.ts (search by email shows result). RED→GREEN→REFACTOR (E2E specs are the failing tests made green).</action>
  <verify>
    <automated>cd apps/admin-web && npm run build && npx playwright test e2e/auth.spec.ts e2e/user-search.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds with the new routes
    - AppSidebar renders exactly the 6 UI-SPEC nav items (grep nav labels)
    - Playwright auth.spec + user-search.spec pass (login redirect + search result)
    - users/page.tsx fetches `/api/v1/admin/users`; ledger page fetches `/api/v1/admin/ledger`
  </acceptance_criteria>
  <done>Authenticated admin shell with user search + ledger inspection per UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → admin-web (login form) | Unauthenticated credential entry |
| admin-web Server Action/Component → backend REST | Token-bearing privileged calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-21 | Information Disclosure | admin JWT theft via XSS | mitigate | httpOnly cookie (JS cannot read); no localStorage |
| T-05-22 | Elevation of Privilege | direct access to admin routes without auth | mitigate | middleware.ts redirects unauthenticated requests; backend hasRole("ADMIN") is the source of truth |
| T-05-23 | Tampering | cookie set outside Server Action | mitigate | cookies().set only inside 'use server' adminLogin (Pitfall 2) |
</threat_model>

<verification>
- Login sets httpOnly cookie + redirects; middleware gates routes; shell + 2 read screens render per UI-SPEC.
</verification>

<success_criteria>
- `cd apps/admin-web && npm run test -- --run` green (middleware, format, actions)
- `npm run build` green; Playwright auth + user-search specs pass
- httpOnly cookie auth; 6-item sidebar; UI-SPEC copy/tokens applied
</success_criteria>

<output>
Create `.planning/phases/05-notifications-admin-analytics/05-08-SUMMARY.md` when done.
</output>
