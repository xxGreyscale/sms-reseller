---
phase: 05-notifications-admin-analytics
plan: 09
type: tdd
wave: 4
depends_on: [05-08, 05-04, 05-05, 05-07]
files_modified:
  - apps/admin-web/app/(admin)/sender-ids/page.tsx
  - apps/admin-web/app/(admin)/sender-ids/actions.ts
  - apps/admin-web/app/(admin)/sender-ids/RejectDialog.tsx
  - apps/admin-web/app/(admin)/refunds/page.tsx
  - apps/admin-web/app/(admin)/refunds/actions.ts
  - apps/admin-web/app/(admin)/bundles/page.tsx
  - apps/admin-web/app/(admin)/bundles/actions.ts
  - apps/admin-web/app/(admin)/bundles/BundleDialog.tsx
  - apps/admin-web/app/(admin)/audit/page.tsx
  - apps/admin-web/app/(admin)/audit/AuditFilters.tsx
  - apps/admin-web/app/(admin)/sender-ids/actions.test.ts
  - apps/admin-web/app/(admin)/bundles/actions.test.ts
  - apps/admin-web/e2e/sender-id-approval.spec.ts
autonomous: true
requirements: [ADMN-04, ADMN-05, ADMN-06, ADMN-07]

must_haves:
  truths:
    - "Operator can view the sender-ID queue and approve or reject (with reason) a request, calling the Phase 4 internal API; the list refreshes via revalidatePath"
    - "Operator can issue a manual refund (with confirmation dialog) via the wallet refund API"
    - "Operator can create, edit, and delete SMS bundles via the payment admin API, with TZS prices formatted"
    - "Operator can view and filter the platform audit log"
  artifacts:
    - path: "apps/admin-web/app/(admin)/sender-ids/page.tsx"
      provides: "ADMN-04 approval queue"
    - path: "apps/admin-web/app/(admin)/refunds/page.tsx"
      provides: "ADMN-05 manual refund"
    - path: "apps/admin-web/app/(admin)/bundles/page.tsx"
      provides: "ADMN-07 bundle catalog"
    - path: "apps/admin-web/app/(admin)/audit/page.tsx"
      provides: "ADMN-06 audit viewer"
  key_links:
    - from: "sender-ids/actions.ts"
      to: "messaging-service /api/v1/internal/sender-ids/{id}/approve|reject"
      via: "Server Action POST with admin Bearer token + revalidatePath"
      pattern: "/internal/sender-ids"
    - from: "bundles/actions.ts"
      to: "payment-service /api/v1/admin/bundles"
      via: "Server Action CRUD"
      pattern: "/api/v1/admin/bundles"

user_setup: []
---

<objective>
Build the four admin-web action screens: sender-ID approval queue, manual refund, bundle catalog
management, and audit-log viewer — completing the 6-screen operator panel (D-08).

Purpose: ADMN-04/05/06/07 — operators act on the platform through Server Actions calling the backend
admin/internal APIs. Builds on the 05-08 shell, lib/api, lib/auth, and format util.

Output: 4 screens with Server Actions, confirmation Dialogs, Toast feedback, and revalidatePath
refresh; Vitest unit tests for action logic + Playwright sender-ID approval E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-notifications-admin-analytics/05-RESEARCH.md
@.planning/phases/05-notifications-admin-analytics/05-UI-SPEC.md
@.planning/phases/04-contacts-messaging/04-08-SUMMARY.md
@.planning/phases/03-wallet-payments/03-06-SUMMARY.md

<interfaces>
Shared foundation from 05-08: lib/api.ts (typed fetch + Bearer cookie), lib/auth.ts, lib/format.ts (formatTzs), app/(admin)/layout.tsx, components/AppSidebar.tsx.
Backend endpoints:
  - GET  messaging-service /api/v1/internal/sender-ids (queue) ; POST /api/v1/internal/sender-ids/{id}/approve ; POST /{id}/reject {reason} (Phase 4, 04-08 — ADMIN-guarded; admin JWT now satisfies it)
  - POST wallet-service /api/v1/wallet/refunds {userId|email, amount, reason} (Phase 3, 03-06; admin-reachable per 05-04)
  - GET/POST/PUT/DELETE payment-service /api/v1/admin/bundles (05-05)
  - GET  admin-service /api/v1/admin/audit?from=&to=&actor=&page=&size= (05-07)
UI-SPEC bindings (BINDING — match copy, columns, tokens):
  - §Sender-ID Approval Queue (stat Card "Pending Review: N", Select filter default Pending, columns, Approve accent / Reject destructive, Reject Dialog with Textarea reason 500-char "Confirm Rejection", success Toast, empty "Queue is clear", revalidatePath('/sender-ids'))
  - §Manual Refund (form fields, confirmation Dialog "Refund {amount} TZS to {userEmail}? This cannot be undone.", success Toast, inline errors)
  - §Bundle Catalog ("Add Bundle", columns, Edit/Delete, Add/Edit Dialog fields min=1, Delete confirm Dialog, price always formatted "12,000 TZS")
  - §Audit Log (date-range + actor Select filters, columns incl. monospace Action + inline JSON details expand, ScrollArea, 50/page Pagination, empty state)
  - §State Patterns + §Copywriting Contract + §Destructive Actions + §Color (destructive red-600, success green-600) + §Accessibility (aria-label on icon-only buttons)
RESEARCH.md Pattern 2/3, Pitfall 2 (cookies in Server Action only), Pitfall 5 (Playwright for async RSC).
shadcn components already installed in 05-01 (dialog, textarea, select, sonner, card, pagination, scroll-area, etc.).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Sender-ID approval queue + manual refund screens</name>
  <files>apps/admin-web/app/(admin)/sender-ids/page.tsx, apps/admin-web/app/(admin)/sender-ids/actions.ts, apps/admin-web/app/(admin)/sender-ids/RejectDialog.tsx, apps/admin-web/app/(admin)/sender-ids/actions.test.ts, apps/admin-web/app/(admin)/refunds/page.tsx, apps/admin-web/app/(admin)/refunds/actions.ts, apps/admin-web/e2e/sender-id-approval.spec.ts</files>
  <read_first>
    - UI-SPEC §Sender-ID Approval Queue + §Manual Refund (exact copy, columns, Dialog text, Toast)
    - 04-08-SUMMARY.md (sender-ID approve/reject internal API shape + SenderIdDecided)
    - 03-06-SUMMARY.md (refund request shape + idempotency)
    - RESEARCH.md Pattern 2/3 + Pitfall 2 + UI-SPEC §Destructive Actions / §State Patterns
  </read_first>
  <behavior>
    - /sender-ids: stat Card "Pending Review: N", Select filter (default Pending), Table of requests; Approve calls the approve action then shows success Toast and revalidatePath('/sender-ids'); Reject opens a Dialog requiring a reason (≤500 chars) then calls reject; empty Pending → "Queue is clear"
    - approveSenderId / rejectSenderId Server Actions POST to /api/v1/internal/sender-ids/{id}/approve|reject with the admin Bearer cookie; unit-tested with mocked fetch
    - /refunds: form (user email/id, amount min=1, reason 10–500 chars) → confirmation Dialog "Refund {amount} TZS to {userEmail}? This cannot be undone." → POST refund → success Toast + form reset; backend errors shown inline ("No user found…", policy-limit copy)
    - Playwright sender-id-approval.spec: approving a pending request removes it from the Pending filter
  </behavior>
  <action>Create app/(admin)/sender-ids/page.tsx (Server Component fetching the queue, stat Card, Select filter, Table with Approve(accent)/Reject(destructive) per pending row) + RejectDialog.tsx (shadcn Dialog + Textarea, "Confirm Rejection") + actions.ts ('use server' approveSenderId/rejectSenderId posting to the Phase 4 internal API via lib/api, revalidatePath('/sender-ids'), Toast via sonner) + actions.test.ts (Vitest mocked fetch). Create app/(admin)/refunds/page.tsx (form + confirmation Dialog) + actions.ts (issueRefund posting to /api/v1/wallet/refunds; map backend errors to UI-SPEC inline copy). Apply UI-SPEC tokens/copy exactly; add aria-labels to icon-only buttons. Create e2e/sender-id-approval.spec.ts. RED→GREEN→REFACTOR.</action>
  <verify>
    <automated>cd apps/admin-web && npm run test -- --run && npm run build && npx playwright test e2e/sender-id-approval.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - sender-ids/actions.test.ts passes; build green; Playwright sender-id-approval.spec passes
    - actions.ts posts to `/api/v1/internal/sender-ids` and calls revalidatePath('/sender-ids')
    - refunds confirmation Dialog uses the exact UI-SPEC copy ("This cannot be undone.")
  </acceptance_criteria>
  <done>Sender-ID queue and manual refund screens act through Server Actions per UI-SPEC.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Bundle catalog management + audit-log viewer screens</name>
  <files>apps/admin-web/app/(admin)/bundles/page.tsx, apps/admin-web/app/(admin)/bundles/actions.ts, apps/admin-web/app/(admin)/bundles/BundleDialog.tsx, apps/admin-web/app/(admin)/bundles/actions.test.ts, apps/admin-web/app/(admin)/audit/page.tsx, apps/admin-web/app/(admin)/audit/AuditFilters.tsx</files>
  <read_first>
    - UI-SPEC §Bundle Catalog + §Audit Log (exact columns, Dialog fields, filters, JSON details expansion, Pagination densities)
    - 05-05 plan (AdminBundleController contract) + 05-07 plan (AuditController contract)
    - UI-SPEC §State Patterns + §Color (price formatting via lib/format.formatTzs) + §Accessibility
    - RESEARCH.md Pitfall 5 (Playwright for async RSC) — bundle/audit pages are async Server Components
  </read_first>
  <behavior>
    - /bundles: "Add Bundle" opens BundleDialog (Name, SMS Count min=1, Price TZS min=1, Active); Table lists bundles with formatted "12,000 TZS" prices, Edit + Delete(destructive, confirm Dialog "Delete {bundleName}? …"); create/update/delete via Server Actions + revalidatePath + Toast
    - createBundle/updateBundle/deleteBundle Server Actions call /api/v1/admin/bundles (POST/PUT/DELETE); client-side + server validation reject non-positive price/smsCount; unit-tested with mocked fetch
    - /audit: date-range (From/To Inputs) + actor Select filters; Table with monospace Action label, Target, inline-expandable JSON Details (<pre> zinc-100); ScrollArea; 50/page Pagination; "No audit events" empty state
  </behavior>
  <action>Create app/(admin)/bundles/page.tsx (Server Component listing /api/v1/admin/bundles, formatTzs prices) + BundleDialog.tsx (shadcn Form/Dialog with the four fields + validation) + actions.ts (createBundle/updateBundle/deleteBundle via lib/api, revalidatePath('/bundles'), Toast, delete confirmation) + actions.test.ts (Vitest mocked fetch incl. validation rejection). Create app/(admin)/audit/page.tsx (Server Component fetching /api/v1/admin/audit with filter searchParams, Table in ScrollArea, inline JSON expand) + AuditFilters.tsx (client date-range + actor Select submitting via query params). Apply UI-SPEC copy/columns/tokens exactly; format prices through lib/format.formatTzs; add aria-labels to icon-only Delete buttons. RED→GREEN→REFACTOR (bundle actions unit tests).</action>
  <verify>
    <automated>cd apps/admin-web && npm run test -- --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - bundles/actions.test.ts passes (CRUD calls + non-positive validation rejected); build green
    - bundles/actions.ts targets `/api/v1/admin/bundles` with POST/PUT/DELETE; prices rendered via formatTzs
    - audit/page.tsx fetches `/api/v1/admin/audit` and renders the UI-SPEC columns with inline JSON expansion
  </acceptance_criteria>
  <done>Bundle catalog and audit-log screens complete the 6-screen operator panel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin-web Server Action → backend admin/internal APIs | Privileged mutations (approve, refund, bundle CRUD) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24 | Elevation of Privilege | mutations without admin token | mitigate | Server Actions attach admin_token cookie; backend hasRole("ADMIN") enforces |
| T-05-25 | Tampering | bundle price set to 0/negative via UI | mitigate | Client validation + backend @Positive/@Min(1) (05-05) |
| T-05-26 | Repudiation | admin action not attributable | mitigate | Admin mutations recorded to the audit log (05-07) |
| T-05-27 | Tampering | refund double-submit | mitigate | Confirmation Dialog + existing idempotent refund mechanism (03-06) |
</threat_model>

<verification>
- All 4 action screens call the correct backend APIs via Server Actions with confirmation + Toast + revalidate; UI-SPEC copy/tokens applied.
</verification>

<success_criteria>
- `cd apps/admin-web && npm run test -- --run && npm run build` green; sender-id-approval Playwright spec passes
- 6-screen panel complete (with 05-08); destructive actions gated by confirmation Dialogs
- All Server Actions target the documented backend endpoints
</success_criteria>

<output>
Create `.planning/phases/05-notifications-admin-analytics/05-09-SUMMARY.md` when done.
</output>
