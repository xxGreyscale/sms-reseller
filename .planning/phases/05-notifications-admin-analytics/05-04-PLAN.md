---
phase: 05-notifications-admin-analytics
plan: 04
type: tdd
wave: 1
depends_on: [05-01]
files_modified:
  - services/wallet-service/src/main/java/com/opendesk/wallet/admin/AdminLedgerController.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/admin/AdminLedgerService.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/admin/LedgerEntryDto.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/analytics/CreditUsageController.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/analytics/CreditUsageService.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/analytics/CreditUsageDto.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/transaction/CreditTransactionRepository.java
  - services/wallet-service/src/main/java/com/opendesk/wallet/config/SecurityConfig.java
autonomous: true
requirements: [ADMN-03, ADMN-05, ANLX-02]

must_haves:
  truths:
    - "GET /api/v1/admin/ledger/{userId} returns any user's credit transaction history to a ROLE_ADMIN caller"
    - "GET /api/v1/analytics/credit-usage returns daily credit-consumption aggregates scoped to the JWT subject"
    - "The existing POST /api/v1/wallet/refunds is reachable by a ROLE_ADMIN token (ADMN-05 manual refund) with no code change to refund logic"
    - "A ROLE_USER token cannot call /api/v1/admin/ledger/** (403)"
  artifacts:
    - path: "services/wallet-service/src/main/java/com/opendesk/wallet/admin/AdminLedgerController.java"
      provides: "ADMN-03 ledger inspection"
      contains: "/api/v1/admin/ledger"
    - path: "services/wallet-service/src/main/java/com/opendesk/wallet/analytics/CreditUsageController.java"
      provides: "ANLX-02 spend trend"
      contains: "/api/v1/analytics/credit-usage"
  key_links:
    - from: "AdminLedgerController"
      to: "CreditTransactionRepository (by userId, paged)"
      via: "ADMIN-guarded read of any user's ledger"
      pattern: "findByUserId"
    - from: "CreditUsageController"
      to: "CreditTransactionRepository.findDailyUsageByUser"
      via: "JWT-subject-scoped aggregate"
      pattern: "getSubject"

user_setup: []
---

<objective>
Add admin ledger inspection (ADMN-03), credit-usage analytics (ANLX-02), and confirm the existing
refund endpoint serves admin manual refunds (ADMN-05) in wallet-service.

Purpose: D-14 — ledger inspection lives in the service that owns credit_transactions. D-06 — credit
usage trend is a per-owner aggregate served by wallet-service with no cross-service join. ADMN-05
reuses the Phase 3 RefundController unchanged.

Output: ADMIN-guarded ledger controller; JWT-scoped credit-usage analytics; SecurityConfig admin +
analytics paths; verification that refund is admin-reachable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-notifications-admin-analytics/05-RESEARCH.md
@.planning/phases/05-notifications-admin-analytics/05-PATTERNS.md
@.planning/phases/03-wallet-payments/03-06-SUMMARY.md

<interfaces>
Existing entity:
  services/wallet-service/.../transaction/CreditTransaction.java (fields incl. userId, delta, type, description, createdAt; append-only)
Existing endpoint (reused for ADMN-05, no change):
  services/wallet-service/.../refund/RefundController.java — POST /api/v1/wallet/refunds
Controller analog: services/messaging-service/.../senderid/SenderIdAdminController.java
Analytics pattern: 05-PATTERNS.md §Analytics controllers (JWT-scoped, JwtAuthenticationToken injection)
ANLX-02 JPQL (RESEARCH.md Pattern 5): GROUP BY CAST(createdAt AS LocalDate), SUM(ABS(delta)) WHERE delta<0, LIMIT 90
SecurityConfig analog: 05-PATTERNS.md §SecurityConfig.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: ADMIN ledger inspection + SecurityConfig admin/analytics paths (ADMN-03, ADMN-05 reach)</name>
  <files>services/wallet-service/src/main/java/com/opendesk/wallet/admin/AdminLedgerController.java, services/wallet-service/src/main/java/com/opendesk/wallet/admin/AdminLedgerService.java, services/wallet-service/src/main/java/com/opendesk/wallet/admin/LedgerEntryDto.java, services/wallet-service/src/main/java/com/opendesk/wallet/transaction/CreditTransactionRepository.java, services/wallet-service/src/main/java/com/opendesk/wallet/config/SecurityConfig.java</files>
  <read_first>
    - 05-PATTERNS.md §AdminUserSearchController / AdminLedgerController (lines 288–316) and §SecurityConfig
    - services/wallet-service/.../config/SecurityConfig.java (existing chain — confirm refund endpoint is authenticated())
    - services/wallet-service/.../transaction/CreditTransaction.java + its repository
    - UI-SPEC §Ledger Inspection (columns: Date, Type, Description, Delta, Balance After)
  </read_first>
  <behavior>
    - GET /api/v1/admin/ledger/{userId}?page=0&size=50 with ROLE_ADMIN returns that user's transactions newest-first as LedgerEntryDto
    - ROLE_USER calling /api/v1/admin/ledger/** → 403
    - POST /api/v1/wallet/refunds with a ROLE_ADMIN token succeeds (admin reachability; the refund logic itself is unchanged from 03-06)
  </behavior>
  <action>Add a paged findByUserId ordered by createdAt DESC to CreditTransactionRepository if absent. Create LedgerEntryDto exposing the UI-SPEC ledger columns (date, type credit/debit, description, delta, balanceAfter — compute/derive balanceAfter if the entity carries a running figure; otherwise expose delta + a rollup field). Create AdminLedgerService + AdminLedgerController GET /api/v1/admin/ledger/{userId} (ADMIN-guarded, NOT subject-scoped — admin inspects any user). Extend SecurityConfig: `/api/v1/admin/**` → hasRole("ADMIN"); add `/api/v1/analytics/**` → authenticated() (used by Task 2); ensure POST /api/v1/wallet/refunds stays authenticated() so a ROLE_ADMIN token reaches it (no refund code change — ADMN-05). Make AdminLedgerIT GREEN. RED→GREEN→REFACTOR.</action>
  <verify>
    <automated>./gradlew :services:wallet-service:test --tests "*AdminLedgerIT*" --tests "*RefundIT*"</automated>
  </verify>
  <acceptance_criteria>
    - AdminLedgerIT passes: admin reads any user's ledger; user token → 403
    - RefundIT (03-06) still passes — refund logic untouched and admin-reachable
    - SecurityConfig contains `/api/v1/admin/**` hasRole("ADMIN") and `/api/v1/analytics/**` authenticated
  </acceptance_criteria>
  <done>ADMN-03 ledger inspection works; ADMN-05 refund is admin-reachable with no regression.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: ANLX-02 credit-usage trend analytics (JWT-scoped)</name>
  <files>services/wallet-service/src/main/java/com/opendesk/wallet/analytics/CreditUsageController.java, services/wallet-service/src/main/java/com/opendesk/wallet/analytics/CreditUsageService.java, services/wallet-service/src/main/java/com/opendesk/wallet/analytics/CreditUsageDto.java, services/wallet-service/src/main/java/com/opendesk/wallet/transaction/CreditTransactionRepository.java</files>
  <read_first>
    - 05-PATTERNS.md §Analytics controllers (JWT-scoped controller + JPQL aggregate, lines 507–550)
    - 05-RESEARCH.md Pattern 5 (ANLX-02 JPQL: daily SUM(ABS(delta)) WHERE delta<0) + Pitfall 3 (subject scoping)
    - services/wallet-service/.../transaction/CreditTransaction.java
  </read_first>
  <behavior>
    - GET /api/v1/analytics/credit-usage returns a list of {date, consumed} daily aggregates for the JWT subject, newest-first, capped (e.g. last 90 days)
    - Only the caller's own transactions are aggregated (no IDOR) — another user's data never appears
    - Days with no consumption are simply absent (gap-fill is the client's concern at MVP)
  </behavior>
  <action>Add findDailyUsageByUser JPQL to CreditTransactionRepository (GROUP BY CAST(createdAt AS LocalDate), SUM(ABS(delta)) WHERE userId=:userId AND delta<0, ORDER BY date DESC, LIMIT 90) returning a CreditUsageRow projection. Create CreditUsageService mapping to CreditUsageDto (date, consumed). Create CreditUsageController GET /api/v1/analytics/credit-usage injecting JwtAuthenticationToken and deriving userId from getSubject() (method injection, not SecurityContextHolder). Rely on the `/api/v1/analytics/**` authenticated() matcher added in Task 1. Make CreditUsageAnalyticsIT GREEN. RED→GREEN→REFACTOR.</action>
  <verify>
    <automated>./gradlew :services:wallet-service:test --tests "*CreditUsageAnalyticsIT*"</automated>
  </verify>
  <acceptance_criteria>
    - CreditUsageAnalyticsIT passes: daily aggregates returned for the caller; another user's debits excluded
    - Controller derives userId from JWT subject (source contains `getSubject()`)
    - Query groups by date and sums absolute consumed credits
  </acceptance_criteria>
  <done>ANLX-02 spend trend served as a JWT-scoped per-owner aggregate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client JWT → /api/v1/admin/ledger/** | Privileged cross-user read |
| client JWT → /api/v1/analytics/credit-usage | Per-user financial data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Elevation of Privilege | /api/v1/admin/ledger/** with user token | mitigate | hasRole("ADMIN") in SecurityConfig |
| T-05-10 | Information Disclosure | credit-usage IDOR | mitigate | Aggregate scoped to auth.getToken().getSubject() |
| T-05-11 | Tampering | refund replay via admin path | accept | Existing 03-06 idempotent refund mechanism unchanged |
</threat_model>

<verification>
- Admin reads any ledger; user blocked.
- Credit-usage aggregates scoped to caller.
- Refund admin-reachable, no regression.
</verification>

<success_criteria>
- `./gradlew :services:wallet-service:test` green for AdminLedgerIT, CreditUsageAnalyticsIT, RefundIT
- /api/v1/admin/** ADMIN-gated; /api/v1/analytics/** subject-scoped
- No cross-service joins
</success_criteria>

<output>
Create `.planning/phases/05-notifications-admin-analytics/05-04-SUMMARY.md` when done.
</output>
