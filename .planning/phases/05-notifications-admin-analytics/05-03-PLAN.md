---
phase: 05-notifications-admin-analytics
plan: 03
type: tdd
wave: 1
depends_on: [05-01]
files_modified:
  - services/identity-service/src/main/java/com/opendesk/identity/token/JwtIssuer.java
  - services/identity-service/src/main/java/com/opendesk/identity/admin/AdminLoginController.java
  - services/identity-service/src/main/java/com/opendesk/identity/admin/AdminLoginService.java
  - services/identity-service/src/main/java/com/opendesk/identity/admin/AdminLoginRequest.java
  - services/identity-service/src/main/java/com/opendesk/identity/admin/AdminUserSearchController.java
  - services/identity-service/src/main/java/com/opendesk/identity/admin/AdminUserService.java
  - services/identity-service/src/main/java/com/opendesk/identity/admin/UserSummaryDto.java
  - services/identity-service/src/main/java/com/opendesk/identity/config/SecurityConfig.java
  - services/identity-service/src/main/resources/db/migration/V5__seed_admin_user.sql
  - services/identity-service/src/main/resources/application.yml
autonomous: true
requirements: [ADMN-01, ADMN-02]

must_haves:
  truths:
    - "A seeded ADMIN account can POST /api/v1/auth/admin/login with email+password and receive a JWT carrying roles:[ROLE_ADMIN] with a 60-minute TTL"
    - "GET /api/v1/admin/users?q= returns paged user summaries to ROLE_ADMIN callers, matching by email or phone"
    - "A non-admin (ROLE_USER) token calling /api/v1/admin/** is rejected (403)"
    - "Admin password hash is injected via Flyway placeholder (env/secret), never hardcoded in VCS"
  artifacts:
    - path: "services/identity-service/src/main/java/com/opendesk/identity/admin/AdminLoginController.java"
      provides: "ADMN-01 admin login endpoint"
      contains: "/api/v1/auth/admin"
    - path: "services/identity-service/src/main/resources/db/migration/V5__seed_admin_user.sql"
      provides: "seeded admin account (D-02)"
      contains: "ON CONFLICT"
  key_links:
    - from: "AdminLoginService"
      to: "JwtIssuer.issueAdminToken"
      via: "BCrypt verify then mint ROLE_ADMIN token"
      pattern: "issueAdminToken"
    - from: "SecurityConfig"
      to: "/api/v1/admin/**"
      via: "hasRole(ADMIN)"
      pattern: "hasRole"

user_setup:
  - service: admin-account
    why: "Seeded operator admin login (D-02) requires a BCrypt password hash and email supplied as secrets, not committed to Git"
    env_vars:
      - name: ADMIN_EMAIL
        source: "Operator-chosen admin email; set as K8s Secret / local env"
      - name: ADMIN_PASSWORD_HASH
        source: "BCrypt hash ({bcrypt}$2a$...) of the operator admin password; generate with a BCrypt tool, store as K8s Secret"
---

<objective>
Add ROLE_ADMIN authentication and user search to identity-service so the admin-web panel can log in
and operators can find accounts.

Purpose: D-01 — reuse Phase 2 RSA JWT machinery to mint admin tokens (satisfies existing
hasRole("ADMIN") guards in messaging-service with no change). D-02 — admin is a seeded privileged
account, not self-registered. ADMN-02 — user search underpins the ledger inspection flow.

Output: JwtIssuer.issueAdminToken (60-min TTL); admin login endpoint + service; seeded admin via
Flyway placeholder; ADMIN-guarded user search; SecurityConfig admin paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-notifications-admin-analytics/05-RESEARCH.md
@.planning/phases/05-notifications-admin-analytics/05-PATTERNS.md
@.planning/phases/02-identity-auth/02-02-SUMMARY.md

<interfaces>
JwtIssuer (modify — add issueAdminToken):
  services/identity-service/.../token/JwtIssuer.java#issueAccessToken(UUID, VerificationStatus) — mirror this; roles claim already present
Existing user entity has a `role` field (ADMIN seeded with role=ADMIN, verification_status=VERIFIED).
DelegatingPasswordEncoder already configured (Phase 2) — verify {bcrypt} hashes.
Controller analog: services/messaging-service/.../senderid/SenderIdAdminController.java
Flyway seed analog: services/payment-service/.../db/migration/V2__seed_sms_bundles.sql (ON CONFLICT DO NOTHING)
SecurityConfig jwtAuthenticationConverter (roles → authorities) already exists in identity-service.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: JwtIssuer.issueAdminToken + admin login endpoint/service + seed</name>
  <files>services/identity-service/src/main/java/com/opendesk/identity/token/JwtIssuer.java, services/identity-service/src/main/java/com/opendesk/identity/admin/AdminLoginController.java, services/identity-service/src/main/java/com/opendesk/identity/admin/AdminLoginService.java, services/identity-service/src/main/java/com/opendesk/identity/admin/AdminLoginRequest.java, services/identity-service/src/main/resources/db/migration/V5__seed_admin_user.sql, services/identity-service/src/main/resources/application.yml</files>
  <read_first>
    - 05-PATTERNS.md §JwtIssuer modification (lines 207–247 — issueAdminToken exact)
    - 05-PATTERNS.md §AdminLoginController (lines 251–284) and §V5__seed_admin_user.sql (lines 406–431)
    - services/identity-service/.../token/JwtIssuer.java (issueAccessToken to mirror)
    - 05-RESEARCH.md Pattern 6 + Pitfall 6 (60-min TTL, no admin refresh) + Open Question 4 (placeholder seed)
  </read_first>
  <behavior>
    - POST /api/v1/auth/admin/login with seeded admin credentials returns 200 + {accessToken} whose decoded roles claim contains ROLE_ADMIN and exp ≈ now+60min
    - Wrong password returns 401 (no token)
    - A user with role=USER cannot obtain an admin token via this endpoint
  </behavior>
  <action>Add issueAdminToken(UUID) to JwtIssuer per the PATTERNS exact pattern: same RSA encoder, subject=adminId, roles:["ROLE_ADMIN"], TTL from app.jwt.admin-token-ttl-minutes (default 60). Create AdminLoginRequest (email, password — @Email/@NotBlank), AdminLoginService that loads the user by email, asserts role=ADMIN, verifies password via the existing DelegatingPasswordEncoder, and on success calls jwtIssuer.issueAdminToken; throws on bad credentials (mapped to 401). Create AdminLoginController POST /api/v1/auth/admin/login returning {accessToken}. Add V5__seed_admin_user.sql inserting an ADMIN user using Flyway placeholders ${adminEmail} and ${adminPasswordHash} (configured in application.yml from env ADMIN_EMAIL / ADMIN_PASSWORD_HASH), verification_status=VERIFIED, ON CONFLICT (email) DO NOTHING — no hardcoded hash. Add app.jwt.admin-token-ttl-minutes and flyway.placeholders config to application.yml. Make AdminLoginIT GREEN. RED→GREEN→REFACTOR.</action>
  <verify>
    <automated>./gradlew :services:identity-service:test --tests "*AdminLoginIT*"</automated>
  </verify>
  <acceptance_criteria>
    - AdminLoginIT passes: seeded admin login yields a token decoding to roles ROLE_ADMIN, ~60-min exp
    - V5 migration uses placeholders (grep for `${adminPasswordHash}`) and ON CONFLICT DO NOTHING; no literal `$2a$` hash in the SQL
    - Wrong-password path returns 401
  </acceptance_criteria>
  <done>Seeded admin can log in and receive a ROLE_ADMIN JWT; credentials are not in VCS.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: ADMIN-guarded user search + SecurityConfig admin paths</name>
  <files>services/identity-service/src/main/java/com/opendesk/identity/admin/AdminUserSearchController.java, services/identity-service/src/main/java/com/opendesk/identity/admin/AdminUserService.java, services/identity-service/src/main/java/com/opendesk/identity/admin/UserSummaryDto.java, services/identity-service/src/main/java/com/opendesk/identity/config/SecurityConfig.java</files>
  <read_first>
    - 05-PATTERNS.md §AdminUserSearchController (lines 288–303) and §SecurityConfig (lines 156–203)
    - services/identity-service/.../config/SecurityConfig.java (existing chain to extend)
    - 05-RESEARCH.md Pitfall 3 (admin endpoints hasRole ADMIN, NOT subject-scoped)
    - UI-SPEC §User Search (columns: Full Name, Email, Phone, Status, Registered)
  </read_first>
  <behavior>
    - GET /api/v1/admin/users?q={term}&page=0&size=20 with a ROLE_ADMIN token returns a page of UserSummaryDto matching email or phone substring
    - The same request with a ROLE_USER token returns 403
    - UserSummaryDto exposes id, fullName, email, phone, verificationStatus, createdAt (the UI-SPEC columns) — no password hash
  </behavior>
  <action>Create UserSummaryDto with exactly the UI-SPEC user-table columns (id, fullName, email, phone, verificationStatus, createdAt). Add a search method to AdminUserService backed by a repository query matching email OR phone (case-insensitive LIKE), paged. Create AdminUserSearchController GET /api/v1/admin/users (q, page, size) returning Page<UserSummaryDto> — no subject scoping (admin sees all). Extend SecurityConfig: permitAll on /api/v1/auth/admin/login; `/api/v1/admin/**` → hasRole("ADMIN"); keep existing user routes; reuse the existing jwtAuthenticationConverter reading the roles claim. Make AdminUserSearchIT GREEN. RED→GREEN→REFACTOR.</action>
  <verify>
    <automated>./gradlew :services:identity-service:test --tests "*AdminUserSearchIT*"</automated>
  </verify>
  <acceptance_criteria>
    - AdminUserSearchIT passes: admin token finds a seeded user by email; user token → 403
    - SecurityConfig contains `/api/v1/admin/**` with hasRole("ADMIN") and permitAll on the admin login path
    - UserSummaryDto does not expose password fields
  </acceptance_criteria>
  <done>Operators can search users; admin routes are ROLE_ADMIN-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/admin-web → /api/v1/auth/admin/login | Unauthenticated credential submission |
| client JWT → /api/v1/admin/** | Privileged operator surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Elevation of Privilege | /api/v1/admin/** with a user token | mitigate | hasRole("ADMIN") in SecurityConfig; jwtAuthenticationConverter reads roles claim |
| T-05-06 | Spoofing | Admin login brute force | accept | MVP: relies on existing identity-service controls; admin is single seeded internal account behind Traefik |
| T-05-07 | Information Disclosure | Seeded admin password in VCS | mitigate | Flyway placeholder from env/K8s Secret; ON CONFLICT DO NOTHING |
| T-05-08 | Information Disclosure | UserSummaryDto leaking hash/PII | mitigate | DTO exposes only UI-SPEC columns; no password fields |
</threat_model>

<verification>
- Admin login yields ROLE_ADMIN 60-min token; wrong password 401.
- User search ADMIN-gated; user token 403.
- Admin credentials sourced from env, not VCS.
</verification>

<success_criteria>
- `./gradlew :services:identity-service:test` green for AdminLoginIT + AdminUserSearchIT
- /api/v1/admin/** is hasRole("ADMIN"); admin login path permitAll
- V5 seed uses placeholders + ON CONFLICT
</success_criteria>

<output>
Create `.planning/phases/05-notifications-admin-analytics/05-03-SUMMARY.md` when done.
</output>
